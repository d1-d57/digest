# Канал исполнителя — MATEMDIGEST · ЗАХОД-9: опубликовать выпуск 2

**Кому:** Claude Code, чистый контекст. Читать только этот файл и то, что здесь названо. В проект не погружаться — заход самоценный.
**Рабочая папка:** корень репозитория `matemdigest-map` (там, где лежит этот файл).
**Оценка:** 5–10 минут, задача техническая.

## 0. ПЕРВЫЙ ХОД

Прочитать файл целиком, записать план в `## ПЛАН` внизу, начать. Блокеры — в `## ВОПРОСЫ`, продолжать с тем, что можно без ответа.

## 1. ЧТО ЕСТЬ

Репозиторий `matemdigest-map` — рабочий и приватный: база `materials.db`, заходы, черновики, документация. Наружу не идёт.

Публикуется только папка `site/` — это **отдельный git-репозиторий** с удалённым адресом `github.com/d1-d57/digest`, GitHub Pages включён из корня ветки `main`. Внутри лежат `index.html` (оглавление) и `1/index.html` (выпуск 1).

Новый выпуск собран в `vypuski/2026-08-01-vypusk-02/` — источник `vypusk.md`, готовая страница `vypusk.html`.

## 2. ГРАНИЦЫ

- Трогать разрешено: `site/`, и при необходимости строку `page_url:` в `vypuski/2026-08-01-vypusk-02/vypusk.md`.
- **Не трогать:** `materials.db`, `vypuski/build_vypusk.py`, `docs/`, другие `ZAHOD-*.md`, корневой `.git` репозитория `matemdigest-map`, ранее опубликованный выпуск 1 в `site/`.
- Ничего из корня репозитория в публичный репозиторий не копировать.

## 3. ЗАДАЧА

1. Проверить, что в шапке `vypuski/2026-08-01-vypusk-02/vypusk.md` стоит `page_url: https://d1-d57.github.io/digest/2/`. Если нет — вписать и пересобрать: `cd vypuski/2026-08-01-vypusk-02 && python3 ../build_vypusk.py vypusk.md`. Генератор чистый stdlib, сети не требует; линтер должен быть зелёным (6 материалов в 6 рубриках).
2. Скопировать `vypuski/2026-08-01-vypusk-02/vypusk.html` в `site/2/index.html` (папку создать).
3. Дописать выпуск в оглавление `site/index.html`: новая строка идёт **первой** в блоке `<div class=list>`, по образцу уже лежащей там строки выпуска 1. Номер, дата, заголовок и фразу-описание взять из шапки `vypusk.md` (`number`, `date`, `subtitle`, `teaser`).
4. Закоммитить и запушить из `site/`: сообщение `выпуск 2: Гипотеза якобиана оказалась неверной`.
5. Дождаться сборки Pages (`gh api repos/d1-d57/digest/pages/builds/latest` до `built`, не дольше 5 минут) и проверить curl-ом, что обе страницы отдают 200:
   - `https://d1-d57.github.io/digest/`
   - `https://d1-d57.github.io/digest/2/`

## 3b. ОСОБОЕ ДЛЯ ЭТОГО ВЫПУСКА — проверить математику

В выпуске 2 впервые есть формулы. Страница подтягивает **KaTeX** с `cdn.jsdelivr.net` и рендерит `$…$` в строке, `$$…$$` — отдельным блоком. Аналитик проверить это не мог: из его песочницы нет сети.

После деплоя проверить, что математика реально отрисовалась, а не осталась долларами:

- `curl -sI https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.js` → 200. Так же для `katex.min.css` и `contrib/auto-render.min.js`.
- В опубликованном `https://d1-d57.github.io/digest/2/` должны присутствовать четыре блока `<p class=disp>` и подключение KaTeX в `<head>`.
- Если есть возможность отрендерить страницу в браузере (headless Chrome, Playwright) — сделать скриншот блока с формулой отображения (`F(x,y,z) = …`, глава «4 · Контрпример») и посмотреть глазами: должна быть набранная математика, а не текст с символами `\big(` и `^`.

Если KaTeX не грузится или формулы не рендерятся — **не чинить самостоятельно**, записать в `## ВОПРОСЫ` что именно видно и остановиться на этом пункте; выпуск при этом всё равно оставить опубликованным.

## 4. ПРОВЕРКА ПЕРЕД ОТЧЁТОМ

- `git -C site ls-files` — только `index.html`, `1/index.html`, `2/index.html`, ничего лишнего.
- Выпуск 1 отвечает 200 по-прежнему.
- Корневой репозиторий изменён только в `vypuski/2026-08-01-vypusk-02/` и в этом файле.

## 5. ОТЧЁТ → секция `## ОТЧЁТ`

Адреса, коды ответов, вывод `git -C site ls-files`, результат проверки KaTeX из §3b, что пошло не по плану, wall-clock.

---

## ПЛАН

1. Проверить `page_url` в шапке `vypusk.md` (если верный — не пересобирать).
2. `mkdir site/2` и скопировать `vypusk.html` → `site/2/index.html`.
3. Вписать строку выпуска 2 первой в `<div class=list>` в `site/index.html` по образцу выпуска 1.
4. Коммит и пуш из `site/` с заданным сообщением.
5. Дождаться `built` у Pages, проверить 200 на `/`, `/1/`, `/2/`.
6. §3b: `curl -sI` три файла KaTeX с jsdelivr; проверить в опубликованной странице 4× `<p class=disp>` и подключение KaTeX; попробовать headless-рендер (Chrome) и посмотреть на формулу из главы 4.
7. Проверки §4 (`git -C site ls-files`, чистота корневого репо), отчёт.

## ВОПРОСЫ

Блокеров нет. Одно решение принято самостоятельно: фраза-описание для оглавления.

`teaser` в шапке — три предложения, а в уже лежащей строке выпуска 1 стоит короткая фраза («Четыре Филдсовские медали, Абакус, Гаусс, Черн и Лилавати: кто получил, за что и что про это почитать»), которая тизеру выпуска 1 не равна дословно. Держался образца, а не буквы: сжал тизер до одной фразы того же регистра — «Контрпример из трёх многочленов сломал задачу, стоявшую с 1939 года: откуда она взялась, как её штурмовали и что дальше». Если нужен дословный тизер — правится одной строкой в `site/index.html`.

## ОТЧЁТ

**Опубликовано.** Выпуск 2 живёт на `https://d1-d57.github.io/digest/2/`, оглавление обновлено, математика отрисована.

### Адреса и коды

| Адрес | Код |
|---|---|
| `https://d1-d57.github.io/digest/` | 200 |
| `https://d1-d57.github.io/digest/1/` | 200 |
| `https://d1-d57.github.io/digest/2/` | 200 |

Pages: `gh api repos/d1-d57/digest/pages/builds/latest` → `built`, коммит `40c8110ac132daa5c80f61a9e4cca94cd7359ab7`.

### Что сделано

1. `page_url: https://d1-d57.github.io/digest/2/` в шапке `vypusk.md` уже стоял верный — **пересборки не потребовалось**, `build_vypusk.py` не запускался, `vypusk.html` не менялся. Скопирован байт в байт: sha1 `dcfeef1630cfb905e7ab5e0e6d50cc52e5992335` совпадает у источника и у `site/2/index.html`.
2. Создан `site/2/index.html`.
3. В `site/index.html` строка выпуска 2 вставлена **первой** в `<div class=list>`: `Выпуск 2 · 1 августа 2026`, заголовок-ссылка на `2/` = `subtitle`, описание — см. `## ВОПРОСЫ`.
4. Коммит `40c8110` «выпуск 2: Гипотеза якобиана оказалась неверной», запушен в `origin/main` (`aee43ec..40c8110`).

### `git -C site ls-files`

```
1/index.html
2/index.html
index.html
```

Ничего лишнего.

### §3b — математика

| Проверка | Результат |
|---|---|
| `katex.min.js` с jsdelivr | 200 |
| `katex.min.css` | 200 |
| `contrib/auto-render.min.js` | 200 |
| `<p class=disp>` в опубликованной странице | 4 |
| Подключение KaTeX в `<head>` опубликованной страницы | все три ссылки на месте |

Страница отрендерена в headless Chrome (Playwright 1.59, канал chrome, 1400×900, `networkidle`):

- узлов `.katex` в DOM после рендера — **13**;
- проверка `/\$\$?[^$]/.test(document.body.innerText)` → **false**, то есть в видимом тексте не осталось ни одного доллара;
- скриншот блока отображения из главы «4 · Контрпример» посмотрен глазами: набранная математика — `F(x,y,z) = ( u³z + y²u(4+3xy), y + 3xu²z + 3xy²(4+3xy), 2x − 3x²y − x³z )` курсивным математическим шрифтом со скобками нужной высоты, никаких `\big(` и `^`;
- соседний блок `F(0,0,−1/4) = F(1,−3/2,13/2) = F(−1,3/2,13/2) = (−1/4, 0, 0)` — тоже набран;
- строчная математика на первом экране (`ℂⁿ → ℂⁿ`, `p`, `x − xᵖ`) отрисована.

Скриншоты — в скратчпаде сессии (`disp_0..3.png`, `chapter4_context.png`), в репозиторий не клались.

### Что пошло не по плану

- Первая попытка рендера — через встроенную браузерную панель; у неё в этой сессии виewport 0×0, скриншот вышел пустым чёрным полем. Проверка переехала на Playwright с системным Chrome, там всё отрисовалось.
- Цикл ожидания сборки Pages со `sleep` не сработал — `sleep` в этой среде заблокирован (30 итераций отработали мгновенно). Сборка к моменту следующей проверки и так была `built`, ждать не пришлось.

### Границы

Корневой репозиторий: мной изменён только этот файл, плюс создана папка `site/2/`. `materials.db`, `vypuski/build_vypusk.py`, `docs/`, другие `ZAHOD-*.md` не трогались — то, что они числятся в `git status` как изменённые, было так до захода. Выпуск 1 в `site/` не менялся, отвечает 200.

**Wall-clock:** ~5 минут (21:07 → 21:12).

### Передеплой 21:35 — обновлённый текст выпуска

Источник переписан (`vypusk.html` 30 899 → 33 814 байт, sha1 `2838d7db…`), выложен заново.

- Шапка `vypusk.md` не изменилась (`subtitle`, `teaser`, `page_url`, `date` те же) — **строка в оглавлении `site/index.html` не трогалась**, она по-прежнему актуальна.
- `site/2/index.html` перезаписан, sha1 совпадает с источником; опубликованная страница скачана обратно — sha1 тот же, байт в байт.
- Коммит `016a0f6` «выпуск 2: обновление текста», `40c8110..016a0f6` в `origin/main`. Pages: `built` на `016a0f6`.
- Коды: `/` → 200, `/1/` → 200, `/2/` → 200.
- `git -C site ls-files` без изменений: `1/index.html`, `2/index.html`, `index.html`.

Математика перепроверена на живой странице (Playwright + Chrome): блоков `<p class=disp>` стало **6** (было 4), узлов `.katex` — **43** (было 13), в каждом disp-блоке ровно по одному отрендеренному `.katex`, долларов в видимом тексте нет. Глазами посмотрены три блока: `Aut(Aₙ) ≅ Aut(Pₙ)`, контрпример `F(x,y,z) = (…)` из главы 4 и `π : P¹ × Sym²P¹ → Sym³P¹, (p,{q,r}) ↦ {p,q,r}` — везде набранная математика.
