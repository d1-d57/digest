---
tab: Отчёт ночи — сборка скилла math-style-editor
status: rabochiy
poryadok: 8
---

# ОТЧЁТ НОЧИ — цепочка stil-noch

🔴 **РАБОЧАЯ ПАПКА (абсолютный путь):** `/Users/ivanyakovlev/Documents/GitHub/matemdigest-map-wt/stil-noch`
Ветка рабочей папки — `zahod/stil-noch`. Основная папка `/Users/ivanyakovlev/Documents/GitHub/matemdigest-map`
осталась на ветке `rabota`, проверено командой.

Пишется ПО ХОДУ. Последним ходом копируется в основную папку.

---

## 0. ГИТ-КОНТУР НА ВХОДЕ (субагент §0.1) — шесть строк дословно

1. заявок было 0, закрыто 0, застряло 0 — очередь пуста (`git_zona.py zayavki` → «заявок нет»)
2. влито в основную 0 веток из 0 названных — локальных `zahod/*` веток нет вовсе (`git --no-optional-locks branch --no-merged | grep -c 'zahod/'` → 0, перепроверено независимо). Основная ветка — `main` (`git symbolic-ref refs/remotes/origin/HEAD` → `refs/remotes/origin/main`)
3. хвост Cowork пунктом 0: забрано 0 путей — кандидаты `_studio/zhurnal/_INFRA-git/INCIDENTY.md`, `_studio/zhurnal/_INFRA-git/VERDIKTY.md`, `docs/verifikatory/karta.md`, `docs/fazy/karta.md` все чистые, хвоста в этом заходе нет; осталось вне git 28 путей (`git_zona.py check`): `docs/DOLGI.md`, `vypuski/reestr_korpusa.tsv`, `vypuski/tablica_sochetaemosti.json.gz`, `zhurnal/2026-08-15_vypusk-04/{ZAYAVKA-NA-KOMMIT.md,kod_git-podmesti-vse.md,kod_korpus-wiki.md}`, `zhurnal/2026-08-17_instrument-stilya/{NAVIGATOR.md,PLAN.md,SESSIYA.md,NOCH/README.md,ORKESTRATOR-noch.md,RESERCH-chto-mozhno-postroit.md,RESHENIYA-o-skille.md,kod_stil-1..9-*.md,kod_stil-noch-orkestrator.md,zamery/{chistyj_vyvod.py,zamer_marginalij.py,zamer_utechki.py,zamer_utechki2.py}}`, `zhurnal/_INFRA-git/ZAYAVKA-2026-08-18-reserch-instrumenta.md` — все содержательные, доложены заходу для коммита последним ходом
4. погашено 0 — `git worktree list` показывает только главную папку, `mogily` пуст (надгробий нет), веток кроме `main`/`rabota` не существует; `zahod/stil-noch` не существует ни как ветка, ни как рабочая папка — трогать было нечего (запрет соблюдён тривиально)
5. необратимого не делал — ни одного коммита, merge, push или удаления ветки за весь заход
6. выдано 28 позиций из 28 найденных

**ДОБАВЛЕНО ОРКЕСТРАТОРОМ, потому что субагент этого делать не имел права.** Комплект ночной
цепочки был неотслеживаемым — 18 путей, включая все девять `kod_stil-*.md`. Завести рабочую папку
на этом состоянии означало родить её пустой (прямое предупреждение §0.1). Закоммитил сам, двумя
ходами, своими путями:

```
git --no-optional-locks add -- zhurnal/2026-08-17_instrument-stilya docs/DOLGI.md zhurnal/_INFRA-git
git --no-optional-locks commit -m "arka-stil: ночная цепочка, решения владельца, ресёрч, замеры" -- <те же пути>
→ [rabota 88f1975] 23 files changed, 5406 insertions(+), 6 deletions(-)
```
После коммита `git status --porcelain zhurnal/2026-08-17_instrument-stilya` → **0 строк**.
Хук `pre-commit` прошёл ЗЕЛЁНЫМ, обход `--no-verify` не понадобился.

---

## 1. ЧИСЛА «ДО» — сняты командами, вывод дословно

Конкурента за файлы нет: `ps aux | grep -v grep | grep sobrat_korpus` → пусто (rc=1). Скачка
корпуса завершена, таблицу не пересобирал.

### `python3 vypuski/check_idioma.py vypuski/2026-08-15-vypusk-04 | head -5`
```
ТЕКСТ: публикуемой прозы 15834 знаков из 25245 в файле — 62 %; служебная разметка блоков не судится
ФАЙЛ: vypuski/2026-08-15-vypusk-04/vypusk.md
ТАБЛИЦА собрана 2026-08-16, три слоя по статусу источника:
   kniga    —  95192 пар · 117 русских математических книг (PDF, папка «Книги»)
   chelovek —  18018 пар · посты канала КТ · деки слайдов · выпуски 02-03 · листки spetsmat · сайт и материалы фестиваля
```

Строка охвата из того же прогона (6-я строка, приведена, потому что именно она сравнима «до/после»):
```
ОХВАТ: разных пар в тексте 336 — подтверждено человеком/книгой 153, ТОЛЬКО в нейросетевом слое 32, неизвестно 151, вне охвата 0
🔴 ПАРЫ, ЖИВУЩИЕ ТОЛЬКО В НЕЙРОСЕТЕВОМ СЛОЕ — 32. Смотреть первыми
```
Полный вывод — 170 строк.

### `python3 vypuski/check_fraza.py vypuski/2026-08-15-vypusk-04`
```
ФАЙЛ: vypuski/2026-08-15-vypusk-04/vypusk.md
A · ФРАЗА О СТРУКТУРЕ ТЕКСТА, А НЕ О МАТЕМАТИКЕ — 0
  нет

B · ОБРУБЛЕННОЕ НАЧАЛО: глагол первым словом, подлежащее позади или отсутствует — 0
  нет

⚠ Гейт показывает места, а не выносит вердикты. Класс A неполон (список зачинов), класс B на пограничных случаях спорен — решает человек.
```

---

## 2. ФАЗЫ — по одной строке на фазу

*(заполняется по ходу)*

| № | фаза | вердикт | чем проверено |
|---|---|---|---|
| 1 | починки отсева `check_idioma.py` (утечка судимого файла, маргиналии, слой wiki) | **ПРОШЛА** — исполнитель поставил себе «доработку» на устаревших данных; на верных данных гейт зелёный: `ТОЛЬКО в нейросетевом слое 85` < 100 на `LENTA-L2.md`. Проверено МОЕЙ командой, не по отчёту | `check_idioma.py LENTA-L2.md --top 5` → rc=0, 85<100; выпуск 4: 32→14 только-нейро |

| 2 | класс «длинное предложение» — новый гейт `check_dlina.py` | **ПРОШЛА** — инструмент есть, порог именно ПАРАМЕТР (`--norma 20` → 38 мест, `--norma 38` → 1 место), встроен в реестр `proverki.py`. П.3 критерия («на 02/03 заметно меньше») не подтверждён, но это не провал фазы: исполнитель показал замером, что сама предпосылка ложна — средняя длина у выпуска 4 равна 15.8 против 15.2 / 16.8 у принятых 02/03, а не 26.7 из `profil_stilya.json`. Вопрос владельцу ниже | мой прогон: rc=0, `1 из 137` при норме 38; проверка параметра двумя значениями |
| 3 | класс А по ФОРМЕ вместо списка фраз (`check_fraza.py`) | **ПРОШЛА** — список из 14 буквальных зачинов заменён функцией `klass_a()` по морфологии; на лекции `LENTA-L2` даёт 9 мест (было 0), на выпуске 4 — 2 (было 0), все 15 старых фраз по-прежнему ловятся. Ключевая находка исполнителя: лемму обязан давать pymorphy3, а не spaCy («дальше»→«далёкий» ломало разбор) | мой прогон: rc=0, `A — 9` на лекции, `A — 2` на выпуске 4; `grep def klass_a` — форма, а не регулярка |
| 7 | многословные обороты — новый гейт `check_oboroty.py` | **ПРОШЛА** — ловит 14 из 21 стенда против базовых 6; метод сменён с окна ±4 токена на связь в дереве зависимостей (`nsubj`/`obj`/`iobj`), потому расстояние в тексте больше не мешает. Переиспользует фазу 1 импортом (`from check_idioma import proza, STOP_OBJEKT, sudimyj_v_korpuse`), а не копией. 7 непойманных названы поимённо с причиной | мой прогон `--stend`: rc=0, `поймано 14 из 21`; `grep` подтвердил импорт и запись в реестре `proverki.py` |
| 8 | `math-russian-terminology` перестаёт ресёрчить известное | **ПРОШЛА ЧАСТИЧНО** — три заказанные вещи сделаны и проверены мной: Шаг 2 «сначала файл терминологии» стоит ДО Шага 3 (`web_search`), добавлен Шаг 3.5 «дописать найденное» (без него следующий запуск всё равно шёл бы в веб), формат файла зафиксирован + живой пример на 7 терминах. 🔴 Гейт `ocenka_skilla.py` КРАСНЫЙ (rc=1) — но все 5 красных осей пред-существовали фазе и лежат вне её периметра «ровно три вещи». Чинила АРХИВНУЮ копию (`disciplina/arhiv-skillov/`), не живой скилл — вопрос подъёма в `skills/` владельцу | мой прогон `ocenka_skilla.py --skill arhiv-skillov/math-russian-terminology` → rc=1, 5 красных осей; `grep '^### Шаг'` подтвердил порядок «файл → веб» |
| 4 | замер полноты без эталона — `zamer_elusion.py` | **ПРОШЛА** — elusion-тест: строит популяцию фраз, гоняет все четыре гейта как внешние инструменты, делит на «показано» / «отсеяно всеми» и выдаёт слепую выборку 20+20 с фиксированным зерном; `--svesti` считает точность и elusion с интервалом Уилсона. Проверил: `В СТРОЮ: 4 из 4`. 🔴 Его же число — сигнал для фазы 5: гейты вместе помечают **81 фразу из 136** | мой прогон `--vydat` → rc=0, 136/81/55, зерно 20260817. (Первый мой вызов дал rc=2 — это моя ошибка вызова, инструменту нужен режим `--vydat`/`--svesti`, а не дефект) |
| 5 | свод гейтов → **карта правок** (главный артефакт) | **ПРОШЛА ЧАСТИЧНО** — карта собрана и формат §10 держится буквально: 77 мест, все 10 полей на месте, оговорка «замена — справка» в шапке, `.jsonl` ↔ `.md` сверены командой (77↔77, расхождений нет), вердикты пусты у 77 из 77. Ранжирование по весу сильнейшего сигнала (12 · 45 · 20), а не по сумме — обосновано замером на принятых выпусках. 🔴 Свой П.2 (самодостаточность формы) исполнитель провалил честно: судья дал 1·2·1·2 из 5. Я перепроверил ВЕРХНИЙ эшелон сам и не согласен с приговором целиком — по записям веса 5 вердикт выносится не открывая текст. Разбор ниже | мои команды: `--sverit` → rc=0, `✅ расхождений нет`; разбор полей питоном — 10 из 10 ключей; чтение 6 записей веса 5 глазами |
| 9 | внесение принятых правок, замыкание цикла | **ПРОШЛА** — по моему решению на стыке (см. ниже): механизм построен и доказан на песочнице, живой текст не тронут. `vypuski/vnesti_pravki.py` берёт только `verdikt == "да"`, точка отката, идемпотентность, откат `--otkat`, конфликт двух правок на одной строке пропускает ОБЕ. На фикстуре с 7 выдуманными вердиктами: внесено 4, `diff` ровно 4 строки, повторный свод 77→76 мест и 12→8 в верхнем эшелоне. Правок в живой выпуск **0**, потому что вердиктов «да» в карте **0** | мои команды: `git status --porcelain vypuski/2026-08-15-vypusk-04/` → 0 строк, `git diff HEAD --stat` → 0; карта: 77 из 77 вердиктов ПУСТО; `grep` подтвердил фильтр `verdikt == 'да'` |
| 6 | сборка скилла + сбор лексики из чужих скиллов | **ПРОШЛА** — критерий оркестратора выполнен и проверен МОЕЙ командой: `SKILL.md` на месте, `ocenka_skilla.py --skill skills/math-style-editor` → **rc=0, «✅ красного нет», 17 осей из 17**, тело **160** строк при лимите 500. Архитектура пятифазная, у каждой фазы назван артефакт и гейт; цикл замкнут прогоном end-to-end (77 мест → 3 вердикта → внесены 2 → второй круг: вес ≥5 упал с 12 до 10). Собрано 15 лексических правил из 34 найденных; 19 про длину/объём/ритм намеренно оставлены на месте. Ничего не удалено, 11 файлов помечены | мой прогон `ocenka_skilla.py` → rc=0; `wc -l` тела → 160 < 500 |

---

## 🔴 РАЗБИРАТЕЛЬСТВО ОРКЕСТРАТОРА №1 — рабочая папка родилась со СТАРОЙ таблицей

Нашла фаза 1, но объяснила себе неверно («трогать запрещено контрактом») и записала себе доработку.
Разобрал я, и это касалось не её одной, а **всей цепочки**.

**Что было.** `worktree add` переносит только то, что git знает. Свежий корпус Википедии
(`vypuski/tablica_sochetaemosti.json.gz`) закоммичен НЕ был — Cowork в `.git` не пишет, а скачка
дописала файл после последнего коммита. Значит рабочая папка получила версию из HEAD:

| | слой `wiki` | размер |
|---|---|---|
| рабочая папка `stil-noch` (из HEAD) | **1 253** пар | 1.19 МБ |
| основная папка (свежая, вне git) | **639 401** пара | 5.78 МБ |

Ровно тот 500-кратный рост, который заход описывает как «таблица теперь ДРУГАЯ». То есть все
детекторы (2, 3, 7), замер (4) и — главное — **карта правок фазы 5** считались бы на данных,
которых заход не имел в виду. Это не «числа-ориентиры разошлись», это неверный вход.

**Что сделал.** Скопировал свежую таблицу и `reestr_korpusa.tsv` в рабочую папку и пометил оба пути
`git update-index --skip-worktree`, чтобы ни одна фаза не застейджила 5.8 МБ двоичного файла своим
`add -- vypuski`. Рабочее дерево после этого чисто (`status --porcelain` → 0 строк).

**Почему это не нарушение запрета «не трогай таблицу».** Запрет в заходе обоснован дословно так:
«их держит работающая скачка». Это условие ИСТЕКЛО и проверено дважды — `ps aux | grep sobrat_korpus`
пусто, сам заход пишет «прогон завершён, последняя запись в чекпойнт 18.08 в 09:24». Копирование
идёт В рабочую папку; файл в основной папке не изменён (проверено `ls -l`: те же 5781846 байт,
та же метка 18 авг 10:22). Таблицу я не пересобирал — это отдельный запрет, и он соблюдён.

**Цена, если бы прошло незамеченным:** фаза 5 выдала бы владельцу карту правок, раздутую ложными
находками — ровно та болезнь «378 находок вместо 5», ради лечения которой затевалась ночь.

**Проверка эффекта на выпуске 4** (код фазы 1, одна и та же команда, разные таблицы):
| | подтверждено | ТОЛЬКО нейро | неизвестно |
|---|---|---|---|
| старая таблица | 154 | 32 | 150 |
| **свежая таблица** | **236** | **14** | **86** |

Числа «до» в разделе 1 остаются действительными: код ДО фазы 1 слоем `wiki` не пользовался вовсе,
и на свежей таблице даёт те же 153 / 32 / 151 (проверено прогоном `git show 88f1975:` версии).

---

## 🔴 РАЗБИРАТЕЛЬСТВО ОРКЕСТРАТОРА №2 — у фазы 9 не было чего вносить

**Что.** Карта фазы 5 родилась с 77 пустыми вердиктами — их ставит человек, а по формату §10 пустой
вердикт значит «не решено», и фаза 9 трогает только `да`. То есть буквальное «внести принятые
правки» ночью невыполнимо в принципе: некому принимать, будить владельца запрещено.

**Решение (моё, за владельца).** Указал фазе 9 прямо: вердикты не проставлять и согласие не
выдумывать — внесение в живой текст на основании пустого вердикта это порча текста, а не замыкание
цикла. Задача переформулирована на «докажи МЕХАНИЗМ»: инструмент, который читает карту, берёт
только `да`, вносит, откатывается — и проверяется на СВОЕЙ фикстуре, а не на живом выпуске.
Основание — прямые слова владельца про архитектуру: «чтобы все механизмы работали на каком-то
уровне», «между шагами понятное чёткое соединение».

**Проверил фактом:** `git status --porcelain vypuski/2026-08-15-vypusk-04/` → 0 строк,
`git diff HEAD --stat` → 0. Живой выпуск не тронут, карта по-прежнему 77 из 77 пустых.

---

## 3. ЧИСЛА «ПОСЛЕ» — те же команды, что и «до»

### `check_idioma.py` на выпуске 4
```
ТЕКСТ: публикуемой прозы 15834 знаков из 25245 в файле — 62 %; служебная разметка блоков не судится
ФАЙЛ: vypuski/2026-08-15-vypusk-04/vypusk.md
ОХВАТ: разных пар в тексте 336 — подтверждено человеком/книгой/вики 236, ТОЛЬКО в нейросетевом слое 14, неизвестно 86, вне охвата 0
```

| | подтверждено | ТОЛЬКО нейро | неизвестно |
|---|---|---|---|
| **до** | 153 | **32** | 151 |
| **после** | 236 | **14** | 86 |

«Только в нейросетевом слое» — это и есть ловимый класс: он сократился более чем вдвое, а
«неизвестно» (шум, который приходится смотреть глазами) упало со 151 до 86.

### `check_fraza.py` на выпуске 4
```
A · ФРАЗА О СТРУКТУРЕ ТЕКСТА, А НЕ О МАТЕМАТИКЕ — 2
  [проза] Речь только о бессилии метода: сам случай размерности 3 не открыт, ответ известен и получен иначе.
  [проза] Дальше десятилетиями улучшали только константу формула : сверхлинейный множитель во всех размерностях сразу не давался никому.

B · ОБРУБЛЕННОЕ НАЧАЛО: глагол первым словом, подлежащее позади или отсутствует — 0
  нет
```
Было `A — 0` при списке из 14 буквальных зачинов: класс не ловил ничего на новом тексте. Стало 2 — и
это настоящие места, а не шум.

---

## 4. 🔴 АБСОЛЮТНЫЙ ПУТЬ К СКИЛЛУ

```
/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/math-style-editor/SKILL.md
```
Каталог целиком: `SKILL.md` · `DOLG.md` (Д1–Д6) · `ISTOCHNIKI.md` · `references/leksika.md` ·
`references/granicy.md` · `evals/evals.json`.
Дом инцидентов: `/Users/ivanyakovlev/Documents/GitHub/disciplina/doma/stil/POKRYTIE.md` (СТ-1…СТ-6).

Гейт зелёный — **мой** прогон, не слова исполнителя:
```
$ python3 ~/Documents/GitHub/disciplina/tools/ocenka_skilla.py --skill skills/math-style-editor
  ✅ красного нет
ИТОГ: с красным 0 из 1; проверено осей 17 из 17
rc=0
```
Тело `SKILL.md` — **160 строк** при лимите 500.

🔴 **СПИСОК «ОТКУДА ЧТО ВЗЯТО» — условие утреннего удаления, а не отчётность:**
`/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/math-style-editor/ISTOCHNIKI.md`
(§1 — что взято, §2 — что рассмотрено и НЕ взято (оттуда удалять нельзя), §3 — взятое наполовину).
Помечены, но **не тронуты**, 11 файлов: `kt-channel-editor` (4 файла), `slajdy` (3),
`reserch-cikl` (3), `matemdigest-map/docs/fazy/stil.md`. Взято 15 лексических правил из 34
найденных; 19 остальных — про длину, объём, ритм, вёрстку и жанр — намеренно оставлены на месте.

---

## 5. ЧТО СКИЛЛ НАПЕЧАТАЛ НА ВЫПУСКЕ 4 — вывод, а не пересказ

```
$ python3 vypuski/stil_svod.py vypuski/2026-08-15-vypusk-04
ФАЙЛ: vypuski/2026-08-15-vypusk-04/vypusk.md
ГЕЙТЫ:
  check_idioma   — в строю, находок: 99
  check_fraza    — в строю, находок: 2
  check_dlina    — в строю, находок: 1
  check_oboroty  — в строю, находок: 79
В СТРОЮ: 4 из 4
ФРАЗ В ТЕКСТЕ (публикуемая проза, без заголовков, дедуп): 136
МЕСТ В КАРТЕ: 77
  вес ≥ 5 — смотреть первыми: 12
  вес 1–4 — второй эшелон:    45
  вес 0  — к сведению:        20
ВТОРОЙ ПРОХОД (MQM-APE) понизил в вес 0 сигналов с тривиальной заменой: 2
СИГНАЛОВ снято как судящих подстановку «формула», а не текст автора: 4
СИГНАЛОВ, чьих слов нет в показанной фразе (артефакт границы предложения, вес снят в 0): 22
МЕСТ вычеркнуто целиком (все их сигналы — артефакты границы предложения): 8
КАРТА: .../NOCH/karta-pravok.jsonl
ДЛЯ ЧЕЛОВЕКА: .../NOCH/karta-pravok.md
rc=0
```
Прогон повторяем: карта после моего прогона побайтово совпала с той, что оставила фаза 5.

**С чего начинать утром: 12 мест веса ≥ 5.** Я прочёл их сам и подтверждаю, что вердикт по ним
выносится не открывая исходный текст — это и есть критерий формы.

---

## 6. ВЕРИФИКАТОР (§3) — свежий субагент, другой метод

Читал ровно два файла (`SKILL.md` и `RESHENIYA-o-skille.md` §1, §4), истории сборки не видел.

- **А. Отвечает ли скилл на главный вопрос владельца** («В каких местах носитель математического
  русского так бы никогда не сказал?») — **отвечает.** Вопрос процитирован в скилле дословно как
  ось документа; машинерия производит список МЕСТ, а не редактуру.
- **Б. Нет ли правил про длину и объём, которые туда не входят** — **не найдено; проверено
  7 правил «ЗАКОН ПОЛЬЗОВАНИЯ» + 15 лексических Л1–Л15 + 5 строк фазовой таблицы (27 пунктов), плюс
  все 5 упоминаний слов «длина/объём/ритм» по файлу.** Все пять мест — либо указание границы
  («НЕ для сокращения и объёма — это `russian-editor`»), либо вызов инструмента-подсказки
  (`check_dlina.py` с порогом-параметром), а не правило стиля. Скилл отдельно запрещает судить по
  длине, даже когда гейт про неё сработал.
- Ответ кончился строкой охвата.

---

## 7. 🔴 НЕОБРАТИМОЕ — отдельным списком

Я запущен без запроса разрешений: вы не видели ни одного из этих действий в момент, когда оно
происходило. Что · где · чем восстанавливается:

1. **Заменена таблица сочетаемости в РАБОЧЕЙ папке** (не в вашей). Что: `vypuski/tablica_sochetaemosti.json.gz`
   и `vypuski/reestr_korpusa.tsv` в `matemdigest-map-wt/stil-noch` перезаписаны свежими копиями из
   основной папки. Где: только внутри рабочей папки. Чем восстанавливается: `git checkout --
   vypuski/tablica_sochetaemosti.json.gz vypuski/reestr_korpusa.tsv` в рабочей папке (версии в HEAD —
   `4459ff5e8488242dd35817c267d034ce6847e2eb` и `8c2c546c61f6a627a205c33fd8991f580e21c238`), предварительно
   сняв `git update-index --no-skip-worktree` с обоих путей.
   ⚠ **Файлы в вашей основной папке НЕ изменены** — проверено `ls -l`: те же 5 781 846 байт, метка 18 авг 10:22.
2. **Закоммичен комплект ночной цепочки** — 23 файла, коммит `88f1975` на ветке `rabota` в основной
   папке. Восстанавливается `git revert 88f1975`. Сделано вынужденно: без этого рабочая папка
   родилась бы пустой (§0.1).
3. Больше необратимого нет. Ни одного `git reset`, ни одного удаления, ни одной правки за пределами
   зон, ни одного `--no-verify`. Живой `vypuski/2026-08-15-vypusk-04/vypusk.md` не тронут —
   проверено командой дважды.

---

## 8. ВОПРОСЫ ВЛАДЕЛЬЦУ — что я решил за вас и стоит перепроверить

1. 🔴 **Свежая таблица корпуса существует ровно в одном месте и вне git.**
   `matemdigest-map/vypuski/tablica_sochetaemosti.json.gz` (5.8 МБ, слой `wiki` — 639 401 пара) не
   закоммичена. Вся калибровка этой ночи снята на ней. Пропадёт диск — пропадёт и она, а
   пересборка запрещена и стоит суток скачки. Решите, как её хранить: коммит 5.8 МБ двоичного
   файла, LFS или бэкап вне репозитория. **Это самый дорогой из открытых вопросов.**
2. **Вердикты в карте никто не проставил — их 77 пустых.** Цикл замкнётся, когда вы проставите `да`
   в `karta-pravok.md` и запустите `stil_svod.py --sverit` → `vnesti_pravki.py`. Начинать с 12 мест
   веса ≥ 5.
3. **Находка фазы 2, подрывающая предпосылку самой фазы.** `profil_stilya.json` дал выпуску 4
   26.7 токенов на предложение — из-за этого числа и строился класс длинных фраз. Независимый замер
   тремя способами даёт **15.8**, против 15.2 и 16.8 у принятых выпусков 02 и 03. Похоже, исходные
   26.7 — артефакт измерения (сентенсайзер не считал перевод строки границей предложения). Инструмент
   всё равно построен и полезен, но повод пересмотреть, был ли вообще предмет.
4. **`math-russian-terminology` чинился в АРХИВЕ** (`disciplina/arhiv-skillov/`), а не в живых
   скиллах — там его нет. Поднимать ли его в `skills/`, фаза не решала: это ваш вопрос (§8 решений).
5. **Границы предложения у гейтов и у свода расходятся.** В карте 22 сигнала указывают на слова,
   которых в показанной фразе нет; свод их честно помечает и снимает вес в 0, но чинить надо в
   гейтах. На лекции масштаб больше — 115 сигналов из 265.
6. **Механическая подстановка замены ломает фразу** (находка пробы фазы 6): «Пакет … открывается
   главой» → «Пакет … посвятить главой». Согласование не проверяется. Ровно тот случай, ради
   которого вы потребовали статус справки, — но `vnesti_pravki.py` подставляет словоформу буквально.
7. **`disciplina/skills/reserch-cikl/SKILL.md` был 501 строка тела при лимите 500 ещё до нас**
   (уже красный), пометка о переезде сделала 503. Утреннее удаление уводит обратно с запасом.
8. **Не вывозил на origin ничего** — см. §10. Влитие `zahod/stil-noch` за вами, как и договорено.

---

## 9. УРОКИ ФАБРИКЕ

### Задание гит-контура запрещает субагенту ровно то, что поручает ему строкой `--kommitit`
Промпт субагента (печатает `bootstrap_zahod.py --zadanie-subagentu`) в пунктах 0 и 3 запрещает
коммитить «содержательную работу» и велит доложить её заходу. Но флаг `--kommitit` в том же промпте
называет содержательной работой ровно то, что надо закоммитить, — комплект ночной цепочки. Субагент
прочёл запрет буквально (и по-своему правильно) и не закоммитил ничего.
**ЦЕНА:** комплект из 23 файлов остался вне git. Заведи я рабочую папку сразу после субагента —
она бы родилась без единого из девяти файлов-заданий, и ночь встала бы на первой же фазе. Спасло
только то, что §0.1 отдельно велит перепроверить это своей командой. Правка: либо разрешить
субагенту коммитить именно то, что названо в `--kommitit`, либо убрать флаг и оставить это заходу.

### `bootstrap_zahod.py --zadanie-subagentu` печатает задание с незаполненным плейсхолдером
В пункте 2 стоит `🔴 <НЕ ЗАПОЛНЕНО — чем заполнять: ветки, которые надо влить…>`. Команда собрана
из флагов захода, но флага для этого пункта среди них нет.
**ЦЕНА:** исполнитель обязан заметить и заполнить сам; передай он текст как есть — субагент получил
бы инструкцию, где вместо списка веток стоит указание аналитику. У меня это стоило отдельной сверки
`branch --no-merged`; у менее внимательного исполнителя стоило бы либо влитого не того, либо
невлитого нужного.

### `worktree add` не переносит некоммиченные данные, и заход об этом молчит там, где это решает всё
§0.1 предупреждает про неотслеживаемые `.md`-задания, и это предупреждение сработало. Но тем же
механизмом в рабочую папку не попала свежая таблица корпуса — а на ней стоит калибровка ВСЕХ гейтов.
**ЦЕНА:** фаза 1 прогнала свой гейт на таблице пятикратно меньшей (`wiki` 1 253 против 639 401),
получила 148 вместо `<100`, объяснила это «контракт запрещает трогать таблицу» и записала себе
доработку. На верных данных её гейт даёт 85 — зелёный с запасом. Один потерянный вердикт фазы, и
ещё пять фаз считали бы карту на неверных данных, если бы я не полез проверять. Правка: в §0.1
рядом с проверкой `status --porcelain` по зоне добавить проверку на некоммиченные ДАННЫЕ, от
которых зависят гейты.

### На macOS нет команды `timeout`, а заходы её предлагают
**ЦЕНА:** один прогон гейта фазы 1 упал с `rc=127` и был мной сначала прочитан как провал гейта.
Мелочь, но она ровно того рода, про которую §1 говорит «код возврата читай первым»: `rc` от
`timeout` и `rc` от инструмента неразличимы, если не посмотреть stderr.

---

## 10. ГИТ-ГИГИЕНА — три числа по каждому репозиторию, командой

Порядок владельца: сначала все коммиты → потом ветки → потом вывоз.

| репозиторий | вне git | невлитых | невывезено |
|---|---|---|---|
| `matemdigest-map-wt/stil-noch` (ветка `zahod/stil-noch`) | **0** | 0 (`main` — витрина, не вливается) | upstream нет |
| `matemdigest-map` (ветка `rabota`) | **6** | 1 (`zahod/stil-noch`) | **2** |
| `disciplina` (ветка `main`) | **7** | 0 | **28** |

**Каждое ненулевое число — поимённо:**

- **`matemdigest-map`, вне git 6.** Ни одного моего. `vypuski/reestr_korpusa.tsv` и
  `vypuski/tablica_sochetaemosti.json.gz` — след завершившейся скачки, были грязными ещё на входе
  (зафиксировано субагентом гит-контура до начала работы); `_studio/zhurnal/_INFRA-git/INCIDENTY.md`
  — автолог; `zhurnal/2026-08-15_vypusk-04/{ZAYAVKA-NA-KOMMIT,kod_git-podmesti-vse,kod_korpus-wiki}.md`
  — чужая содержательная работа вне моей зоны. Не мои — не коммичу, называю строкой.
- **`matemdigest-map`, невлитая `zahod/stil-noch`.** Это ветка этой ночи. **Не вливаю намеренно** —
  прямое указание захода: влитие решает владелец утром. Рабочую папку `stil-noch` не удалял.
  `main` не вливается никогда: это ветка-витрина.
- **`disciplina`, вне git 7.** Всё чужое: `README.md`, `ARHITEKTURA.md`, `DOLGI.md`,
  `_studio/.commit-plan`, `_studio/zhurnal/2026-08-20_poryadok-v-metaskillah/`,
  `zhurnal/VHOD-2026-08-13_disciplina-zahodov.md`,
  `_studio/zhurnal/2026-08-19_vynos-fabriki-arok/kod_pochinka-i-uborka.md`. В этом репозитории
  **параллельно работает другая сессия** (её незакоммиченные файлы датированы сегодняшним числом).
  Чужую содержательную работу не трогаю — это прямой запрет.
- **Невывезено: 1 и 28.** 🔴 **Не вывозил сознательно, и вот причина.** В `disciplina` 28 коммитов —
  почти все чужие, из параллельной сессии, которая прямо сейчас пишет в тот же репозиторий; вывозить
  чужую незаконченную работу за автора я не вправе. В `matemdigest-map` невывезенных коммита два, и оба
  мои: `88f1975` (комплект цепочки) и `cc12fc5` (этот отчёт и карта, скопированные в основную
  папку). Оба на ветке `rabota`, чью судьбу вы решаете утром вместе с влитием.
  Вывоз — действие наружу; ночью, без вас, я его не делаю. **Оба числа не «забыл», а «решил не
  делать», и вот они названы.**

---

## 11. ПОВТОРЯЕМОСТЬ НАХОДОК — что повторится на СЛЕДУЮЩЕЙ единице работы

**Повторятся (значит это заход ДО следующего прогона, а не пункт очереди):**
- **Расхождение границ предложения** между гейтами и сводом. На выпуске 4 — 22 сигнала и 8
  вычеркнутых мест; на лекции уже 115 сигналов из 265 и 55 мест из 139. Чем длиннее текст, тем хуже:
  повторится на КАЖДОМ следующем тексте и будет расти.
- **Механическая подстановка замены, ломающая согласование** («открывается» → «посвятить главой»).
  Повторится на первой же принятой правке — то есть в момент, когда вы начнёте пользоваться картой.
- **Рабочая папка со старой таблицей.** Повторится при следующем же `worktree add`, пока свежая
  таблица не в git (вопрос 1).

**Не повторятся — законно уходят пунктом очереди:**
- Незаполненный плейсхолдер в `--zadanie-subagentu` — свойство одного инструмента, чинится один раз.
- `reserch-cikl/SKILL.md` на 503 строки — снимется утренним удалением.
- Подозрение на артефакт в `profil_stilya.json` — разовая проверка одного числа.

---

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/disciplina/skills/math-style-editor/SKILL.md` — открывать текстовым редактором; гейт `python3 ~/Documents/GitHub/disciplina/tools/ocenka_skilla.py --skill skills/math-style-editor` → rc=0, зелёный.
**АРТЕФАКТ-2 (то, чем пользуются утром):** `/Users/ivanyakovlev/Documents/GitHub/matemdigest-map-wt/stil-noch/zhurnal/2026-08-17_instrument-stilya/NOCH/karta-pravok.md` — 77 мест, начинать с 12 веса ≥ 5; машинная пара — `karta-pravok.jsonl` рядом.
**ВРЕМЯ ПРОГОНА + ТОКЕНЫ:** на канале `app` НЕПРИМЕНИМО, снимать неоткуда.

**КОММИТ:** `f1cc944` — «orkestrator stil-noch: отчёт ночи — девять фаз, числа до/после, разбирательства, уроки фабрике» (ветка `zahod/stil-noch`) · и `cc12fc5` — «arka-stil: отчёт ночи и карта правок — копия в основную папку» (ветка `rabota`) · `git_zona.py check` → ✅ по всем четырём путям зоны в обеих папках · `git show --stat` — только мои пути.

---

## ИТОГ ОДНОЙ ТАБЛИЦЕЙ

| фаза | вердикт |
|---|---|
| 1 · починки отсева | прошла |
| 2 · класс длинной фразы | прошла |
| 3 · класс А по форме | прошла |
| 7 · многословные обороты | прошла |
| 8 · терминология | прошла частично (три заказанные вещи сделаны; гейт красный пред-существовавшими осями) |
| 4 · инструмент замера | прошла |
| 5 · карта правок | прошла частично (форма §10 держится, верхний эшелон годен; свой П.2 провален честно) |
| 9 · внесение | прошла (механизм доказан на фикстуре; в живой текст 0 правок — вердиктов «да» 0) |
| 6 · сборка скилла | прошла |

**Пропущенных фаз нет — все девять запущены и приняты.** Двум поставлено «частично», и в обоих
случаях названо ровно то, что не сделано, и почему.
