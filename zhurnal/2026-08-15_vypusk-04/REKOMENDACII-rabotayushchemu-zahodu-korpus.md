---
tab: Рекомендации работающему заходу корпуса — не править его файл
status: рабочий
poryadok: 5
---

# РЕКОМЕНДАЦИИ ИСПОЛНИТЕЛЮ `kod_korpus-wiki.md` — от 2026-08-17, 19:30

> **ПОЧЕМУ ОТДЕЛЬНЫЙ ФАЙЛ, А НЕ ПРАВКА ЗАХОДА.** Владелец предложил вписать рекомендации прямо в
> `kod_korpus-wiki.md`. Так делать нельзя: исполнитель в этот файл ПИШЕТ отчёт прямо сейчас. Ровно
> этим приёмом в этой арке уже потеряна работа — фоновый заход снёс правки аналитика над теми же
> путями. Поэтому рекомендации лежат здесь, а исполнителю даётся путь к этому файлу.
>
> **КАК ПЕРЕДАТЬ:** одной строкой в чат исполнителя —
> «Прочитай `zhurnal/2026-08-15_vypusk-04/REKOMENDACII-rabotayushchemu-zahodu-korpus.md` и выполни
> его целиком, прогон при этом не останавливай».

## 1. ПРОГОН НЕ ОСТАНАВЛИВАТЬ — решение владельца 17.08, подтверждено повторно

Скачано 10 632 статьи из 19 572 в списке (54 %), 89.5 млн знаков. Довести до конца.

## 2. ПЕРЕСОБИРАТЬ ТАБЛИЦУ ПАРАЛЛЕЛЬНО, НЕ ДОЖИДАЯСЬ КОНЦА ПРОГОНА

🔴 **Это главное.** `vypuski/tablica_sochetaemosti.json.gz` собрана 16.08 в 22:23 и с тех пор не
пересобиралась: прогон копит `.korpus/wiki_teksty.json`, а таблица про это не знает. Значит,
`check_idioma.py` восемнадцать часов судит текст по вчерашнему корпусу.

Что сделать: пересборку таблицы из накопленного сырья запускать **по чекпойнту**, не останавливая
докачку. Читать `wiki_teksty.json` на копии либо под тем же чекпойнтом, что и докачка, — race тут
реален, файл растёт во время чтения.

⚠ Потолок 25 МБ на таблицу — решение владельца, он в силе. При 165 млн знаков вики слой вырастет;
если потолок близко, отсекать по частоте пары, а не выбрасывать статьи.

## 3. ВИКИ — ОТДЕЛЬНЫЙ СЛОЙ `wiki`, НЕ ВЛИВАТЬ В `kniga` И НЕ ВЛИВАТЬ В `chelovek`

Решение владельца 16.08: вики — источник лексики, но не эталон стиля («является» 229 против наших 55
на миллион знаков). Слоёв становится четыре, и статус у `wiki` промежуточный: подтверждает, что
оборот **засвидетельствован**, но не подтверждает, что он **принят**. Кто читает таблицу, должен
уметь спросить оба вопроса раздельно — как `check_idioma.py` уже спрашивает про `nejroset`.

## 4. ПОСЛЕ КАЖДОЙ ПЕРЕСБОРКИ — ПРОГНАТЬ ЗАМЕР ПОКРЫТИЯ И ПРИЛОЖИТЬ ЧИСЛО К ОТЧЁТУ

```
python3 vypuski/zamer_pokrytiya.py vypuski/2026-08-15-vypusk-04
```

Он печатает кривую покрытия — покрытие как функцию размера корпуса — и сохраняет числа в
`vypuski/.korpus/zamer_pokrytiya.json`. Замер написан 17.08, живёт в git, зоны прогона не касается.

🔴 **Зачем это в отчёте.** Владелец 17.08 усомнился в экстраполяции аналитика («я не уверен, что твоя
плата так работает») — и правильно усомнился: экстраполяция это экстраполяция. Числа на 89 МБ дают
наклон **+3.4 процентных пункта на удвоение корпуса** (52 % при 3.4 МБ → 68 % при 88 МБ, это 4.7
удвоения). Отсюда прогноз на конец прогона (~165 МБ): **около 71 %**. Прогноз надо **проверить
замером**, а не поверить: если фактическое покрытие выйдет заметно выше 71 %, наклон не
логарифмический и вывод «растить корпус дальше бесполезно» (`#Д27`) придётся переписать. Приложить к
отчёту фактическое число рядом с прогнозом 71 %.

## 5. ЧЕГО НЕ ДЕЛАТЬ

- Не трогать `vypuski/check_idioma.py` — он правлен аналитиком 17.08 вечером (выброшена служебная
  разметка блоков, сохранены переводы строк ради границ предложений). Конфликт по этому файлу
  вероятен, если исполнитель тоже его правит.
- Не трогать `vypuski/zamer_pokrytiya.py` — новый файл аналитика, только запускать.
- Не расширять список категорий Википедии ради объёма: замер говорит, что объём упирается в
  логарифм. Если добирать — добирать ЖАНР (научпоп, «Квант»), а не размер.
