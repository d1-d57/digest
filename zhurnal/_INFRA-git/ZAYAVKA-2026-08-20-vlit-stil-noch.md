---
tab: Заявка — влить ветку zahod/stil-noch, без неё скилл не работает
status: открыта
poryadok: 10
---

# ЗАЯВКА — влить `zahod/stil-noch` в `rabota`, закоммитить хвост, вывезти

> Оформлено заявкой, а не командой в чат: Cowork работает из песочницы и в `.git` писать не может,
> а выдавать владельцу shell запрещено (`GIT-disciplina §0`). Закрывает заход host-side.

## 🔴 ПОЧЕМУ СРОЧНО — скилл собран, но НЕ РАБОТАЕТ

`disciplina/skills/math-style-editor` закоммичен и объявляет своей точкой входа
`matemdigest-map/vypuski/stil_svod.py` (`SKILL.md`, строки 50, 57, 58). Самого инструмента в
основной ветке **нет**: все восемь инструментов ночи лежат только в ветке `zahod/stil-noch`.

Проверено командой 20.08:
```
ls vypuski/{check_dlina,check_oboroty,stil_svod,vnesti_pravki,zamer_elusion}.py → нет ни одного
git --no-optional-locks branch --no-merged rabota                                → zahod/stil-noch
```

⇒ Позвать скилл сегодня значит упереться в отсутствующий файл. Ночная работа и утренний вынос
приняты, но пользоваться результатом нельзя, пока ветка не влита.

## ЧТО СДЕЛАТЬ — порядок жёсткий: коммиты → влитие → вывоз

**1 · Хвост Cowork в git.** Вне git на момент заявки (снято `status --porcelain`):
```
vypuski/reestr_korpusa.tsv
zhurnal/2026-08-15_vypusk-04/kod_korpus-wiki.md
zhurnal/2026-08-17_instrument-stilya/kod_stil-vynos-zavershit.md
zhurnal/_INFRA-git/ZAYAVKA-2026-08-20-vlit-stil-noch.md
```
⚠ `reestr_korpusa.tsv` — пара к уже закоммиченной таблице корпуса (`872f253`): без него таблица не
читается как история. Его отсутствие в git названо самим заходом выноса строкой 3 отчёта субагента.

**2 · Влить `zahod/stil-noch` в `rabota`.** Ветка несёт 24 коммита и 50 путей: восемь инструментов
`vypuski/*.py`, реестр проверок, восемь файлов-заданий `kod_stil-*.md` и папку `NOCH/`.

🔴 **Прошлая попытка отказала, и причина известна — не воспроизводи её.** Субагент гит-контура
20.08 звал влитие с зоной из четырёх путей; инструмент отказал целиком (`rc=1`, «не сливаю
НИЧЕГО»), потому что `--zone` обязана покрывать ВСЕ пути ветки, а покрывала 4 из 50. Зону задавал
аналитик, и заход правильно не стал расширять её самовольно. **Здесь зона расширена намеренно и
покрывает всё:** `vypuski` · `zhurnal/2026-08-17_instrument-stilya` · `docs`.

⚠ Форму вызова бери из живого `--help` инструмента, а не из этой заявки: Cowork однажды уже
написал по памяти флаг `--vetka`, которого не существует, и команда упала у владельца
(`GIT-disciplina §0`, цена — один круг терминала). Дом слияния — `GIT-disciplina §4б`.

**3 · Вывоз.** `--push` обязателен: без него работа доезжает до `.git` и останавливается там,
единственная копия — диск владельца (`GIT-disciplina §2`, цена 23.07 — три коммита остались на
машине).

## КРИТЕРИЙ — три проверки командой, не глазами

1. `ls vypuski/stil_svod.py` → файл есть. **Это и есть смысл заявки**: скилл получает свою точку входа.
2. `git --no-optional-locks branch --no-merged rabota` → `zahod/stil-noch` в выводе больше нет.
3. `git --no-optional-locks log --oneline @{u}.. | wc -l` → `0`, работа вывезена.
4. Скилл ожил end-to-end: прогон `stil_svod.py` по выпуску 4 печатает путь карты правок. Вывод —
   в отчёт дословно.

## ЧЕГО НЕ ДЕЛАТЬ

- **Ветку после влития НЕ гасить** — решение владельца о закрытии принимается отдельно.
- **Не пересобирать таблицу корпуса.** Она в git (`872f253`), 5.8 МБ, слой `wiki` 639 401 пара;
  пересборка стоит суток скачки и запрещена.
- **Не трогать `vypuski/.korpus/`** — 190 МБ сырья, намеренно вне git.

---
*Заведена Cowork 2026-08-20. Приёмка захода выноса — `zhurnal/2026-08-17_instrument-stilya/kod_stil-vynos-zavershit.md`, раздел ПРИЁМКА. Дом git-дисциплины — `disciplina/skills/disciplina-git`.*
