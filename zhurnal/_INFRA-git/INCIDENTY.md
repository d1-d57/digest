# Автолог git-инцидентов — matemdigest-map

Входящая корзина сырых симптомов. Формат строки: `дата время · ветка · симптом · → подсказка · статус`.

**Зачем файл.** По git-дисциплине неуспешный коммит обязан оставлять след сам, а не жить в чате: заявка «работа не доехала в git» из чата исчезает вместе с сессией, и следующий заход о ней не знает. В монорепо `materials` этот дом — `_studio/zhurnal/_INFRA-git/INCIDENTY.md`; здесь его не было, заведён 15.08.2026 по требованию владельца — «такие заявки надо оставлять в специальной папке, чтобы следующий Claude-заход их закрыл».

⚠ **Строки здесь вписываются РУКОЙ** — но не по той причине, что стояла тут раньше. ~~«Инструмент `git_zona.py` в этот репозиторий автолог не пишет»~~ — **неверно, установлено заходом `kod_odin-repozitorij` 16.08.** Пишет: при неуспешном коммите `log_incident()` сработал сам. Только адрес он берёт по раскладке `materials` — `_studio/zhurnal/_INFRA-git/INCIDENTY.md`, — а здесь автолог живёт корнем, в `zhurnal/`. То есть строка появляется, но не в этом файле, и никто её не читает. Пока так, рукой; в отчёте называй, каким способом строка попала в лог.

**Разобранный инцидент** уезжает уроком с ценой в `UROKI-FABRIKE.md` своей арки, здесь получает статус `→ УРОКИ`.

---

## Открытые

`2026-08-16 05:48 · rabota · commit --zone . не значит «весь репозиторий»: in_zone() матчит только путь `.` или префикс `./`, у корневых путей такого префикса не бывает — молчаливый ложный «зона чиста» · → патч git_zona.py: либо матчить весь репозиторий, либо явно отказывать вместо тихого «нечего коммитить»; заявка git-zona-py-commit-2026-08 адресована заходу-разработчику · ОТКРЫТ`

⚠ **Это единственный оставшийся открытый инцидент, и он про ИНСТРУМЕНТ, а не про этот репозиторий.**
Ждёт захода-разработчика: гит-контур авторить код не вправе, отказов по этому адресу уже больше
десятка.

## Закрытые — 2026-08-17

`2026-08-16 05:12 · rabota · 23 staged-удаления мусора среды не коммитятся: git_zona.py commit делает add перед commit, а add отказывает на пути под .gitignore · ЗАКРЫТ 2026-08-17`

**Чем закрыт — и это важно, потому что закрыт он НЕ починкой.** Заход `kod_git-podmesti-vse`
снял состояние командой: `git --no-optional-locks diff --cached HEAD` → пусто, rc=0;
`git --no-optional-locks ls-files | grep -cE '\.fuse_hidden|\.DS_Store|db-shm|db-wal'` → `0`.
Мусора среды в индексе больше нет — значит, коммитить нечего и упираться не во что. Баг
инструмента, из-за которого три захода подряд писали «застряло», при этом НЕ исправлен: он живёт
отдельной строкой выше и отдельной заявкой к заходу-разработчику. *Урок: инцидент «не могу
закоммитить X» умирает двумя разными способами — X закоммичен или X перестал существовать.
Три круга подряд его пытались закрыть только первым способом, а сработал второй, и проверка
на это стоила одну команду.*

`2026-08-16 05:12 · rabota · мёртвый .git/index.lock, оставлен Cowork при приёмке (голый git add --dry-run — нарушение §0 git-дисциплины, из песочницы лок не снять) · ЗАКРЫТ 2026-08-16`

**Чем закрыт.** `git_zona.py clean` первым ходом захода `kod_odin-repozitorij`. Проверено заново
17.08: `doctor` печатает «Локи в .git: ✅ свободно».

### Замер 2026-08-17: кросс-репозиторный `GIT_ZONA_REPO` на ЗАПИСИ — работает

Этот файл трижды нёс предупреждение «на `commit` не проверен, если не работает — это долг
инструмента». Проверено заходом `kod_git-podmesti-vse`: через `GIT_ZONA_REPO` в трёх ЧУЖИХ
репозиториях сделаны коммиты (`58d09e3`, `33fa39e`, `54cb23d`), закрытие четырёх заявок с
переносом файлов, гашение четырёх веток в `disciplina`, `vyvezti --yes` и `opublikovat --yes`.
Ни одного отказа по причине кросс-репозиторности. Предупреждение снято, долга нет.

`2026-08-16 05:29 · rabota · коммит прошёл не целиком: пути не доехали или коммиты упали (та же строка «23 staged-удаления не коммитятся» выше — П1 и П3 заявки kod-odin-repozitorij исполнены гит-контуром, лок снят, zhurnal-коммит 1167c06 вывезен; П2 остался тем же открытым инцидентом) · → см. строку выше · статус: → см. выше, дубль закрыт` — строка сама попала не в этот файл, а в `_studio/zhurnal/_INFRA-git/INCIDENTY.md` (раскладка `materials`) тем же кросс-репозиторным багом `log_incident()`, перенесена сюда рукой гит-контуром, стray-файл удалён.

`2026-08-16 05:48 · rabota · коммит прошёл не целиком: пути не доехали или коммиты упали — повторная попытка П2 по прямому указанию координатора (`commit --zone . -m ... --push`, затем `plan`→`commit --push`), обе упали: `--zone .` не значит «весь репозиторий» (`in_zone()` матчит только путь `.` или префикс `./`, у корневых путей такого префикса не бывает — молчаливый ложный "зона чиста"), а plan-based commit воспроизвёл тот же `git add` отказ на .gitignore-путях, что и раньше · → патч `git_zona.py` нужен по обеим причинам: (1) `commit --zone .` должен либо матчить весь репозиторий, либо явно отказывать вместо тихого "нечего коммитить"; (2) add на staged-deletion гитигнорируемых путях — прежний диагноз, заявка `git-zona-py-commit-2026-08` уже переадресована заходу-разработчику · статус: открыт, дубль стray-файла снова снесён руками

Обе строки вписаны рукой.

## Закрытые — 2026-08-16

`2026-08-16 04:12 · main · проект живёт в ТРЁХ копиях, кухня не коммитилась с 16.07 (вне git 99 путей) · → заход kod_odin-repozitorij · ЗАКРЫТ`

**Чем закрыт.** 13 коммитов (`e990a83` … `3502af5`), origin `d1-d57/digest` заведён, `main` переименована в `rabota` и вывезена, `site/` переехал подпапкой `sayt/` — вложенного репозитория больше нет, третья копия `~/Documents/GitHub/digest` снесена, сайт задеплоен и открывается: `https://d1-d57.github.io/digest/`, три выпуска на месте. Проверено приёмкой командами, а не по отчёту. Бэкап всей истории — `~/matemdigest-BACKUP-2026-08-16.bundle`.

**Что НЕ закрылось:** 23 пути мусора среды — они переехали в открытую строку выше, это другой инцидент с другой причиной.

Блоки по сессиям ниже — след того, что писала каждая сессия. Они больше не долг, а история.

🔴 **Главное, чего не знали блоки по сессиям ниже.** Они перечисляли, что не доехало
в каждой отдельной сессии арки «выпуск 04», и по ним читалось, будто долг — это работа
15–16.08. Замер 16.08 показал другое, и дважды шире.

**Во-первых, кухня не коммитилась с 16 июля.** Вне git выпуски 01, 02 и 03 целиком, весь
`vypuski/` с генератором и четырьмя гейтами, месяц `docs/` и обе инфраструктурные папки.
Мёртвый `index.lock` в `.git` — тех же 732 часов.

**Во-вторых, репозиториев три, а не один.** Кухня `matemdigest-map/.git` — **без origin
вовсе**, то есть месяц работы существует в одной копии. Витрина `site/.git` — вложенный
репозиторий с origin `d1-d57/digest`. И `~/Documents/GitHub/digest` — вторая рабочая копия
той же витрины, отставшая на неделю. Кухня перестала коммититься ровно тогда, когда дайджест
переехал на Hugo и работа ушла в `site/`: это один брошенный репозиторий, а не два замысла.

**Решение владельца 16.08:** свести в ОДИН репозиторий (`d1-d57/digest`, имя и адрес Pages
не меняем), одна рабочая папка, ветка `rabota` — весь проект вместе с сайтом подпапкой `sayt/`,
ветка `main` — витрина, перенос командой `opublikovat`. Пока публичный целиком; разделение
на приватную и публичную часть отложено сознательно.

Восемь шагов, двенадцать сообщений коммитов и что назвать в отчёте — в заявке
`ZAYAVKA-2026-08-16-mesyachnyy-dolg.md` рядом. Блоки по сессиям ниже остаются как след:
они точны в том, ЧТО именно писала каждая сессия, и полезны для сообщений коммитов.

`2026-08-15 · ветка не проверялась · вся работа сессии осталась вне git: корневой .git под fuse, коммиты из песочницы Cowork падают · → закрыть заходом Claude Code либо командой владельца (ниже) · ОТКРЫТ`

**Что именно вне git** (арка `2026-08-15_vypusk-04`, сессия 1):

| путь | что |
|---|---|
| `docs/razvedka-upakovka-sharov.md` | новый — разведка выпуска 4 целиком |
| `docs/reestr-syuzhetov.md` | новый — живой реестр сюжетов, прошедших гейт |
| `vypuski/OCHERED.md` | правка — слот выпуска 4, статусы кандидатов, указатель на реестр |
| `zhurnal/2026-08-15_vypusk-04/NAVIGATOR.md` | новый |
| `zhurnal/2026-08-15_vypusk-04/PLAN.md` | новый |
| `zhurnal/2026-08-15_vypusk-04/SESSIYA.md` | новый |
| `zhurnal/2026-08-15_vypusk-04/UROKI-FABRIKE.md` | новый |
| `zhurnal/2026-08-15_vypusk-04/HANDOFF-2026-08-15.md` | новый |
| `zhurnal/_INFRA-git/INCIDENTY.md` | новый — этот файл |

**Что именно вне git** (та же арка, сессия 2 — 15.08, интервью и перестройка сетки):

| путь | что |
|---|---|
| `docs/razvedka-upakovka-sharov.md` | крупная правка — новый §11 (действующая сетка, отменяет §4), переписан §7 (пп. 2, 13, 14), пополнен §10 |
| `docs/O-PROEKTE.md` | правка — новый раздел «Слоган: мы пишем современную историю математических идей» |
| `docs/pravila-vypuska.md` | правка — врезка со слоганом в §0 |
| `zhurnal/2026-08-15_vypusk-04/PLAN.md` | правка — «СЕЙЧАС» переведён на калибровку стиля и текст, обновлены галочки |
| `zhurnal/2026-08-15_vypusk-04/SESSIYA.md` | правка — разнесена сессия 2, отсечка на 2026-08-15 |
| `zhurnal/2026-08-15_vypusk-04/HANDOFF-2026-08-15-2.md` | новый — вторая передача за день |
| `zhurnal/_INFRA-git/INCIDENTY.md` | правка — этот блок |

**Что именно вне git** (та же арка, сессия 3 — 15.08, калибровка стиля):

| путь | что |
|---|---|
| `docs/pravila-vypuska.md` | правка — новый §15g «Стилевая калибровка: хроника или лестница», вставлен перед §15 |
| `docs/protsedura-vypuska.md` | правка — в §4 «Тексты» первым ходом встала калибровка со ссылкой на §15g |
| `docs/O-PROEKTE.md` | правка — в разделе «Слоган» новая строка таблицы и подраздел «Человеческий штрих остаётся» |
| `zhurnal/2026-08-15_vypusk-04/NAVIGATOR.md` | правка — две строки в «Указателях в долгую» |
| `zhurnal/2026-08-15_vypusk-04/PLAN.md` | правка — «СЕЙЧАС» переведён с калибровки на текст, две галочки |
| `zhurnal/2026-08-15_vypusk-04/SESSIYA.md` | правка — разнесена сессия 3 |
| `vypuski/2026-08-15-vypusk-04/vypusk.md` | новый — полный текст выпуска 4, шесть разделов |
| `vypuski/2026-08-15-vypusk-04/vypusk.html` | новый — сборка генератором |
| `vypuski/2026-08-15-vypusk-04/vypusk.telegram.txt` | новый — телеграм-вид |
| `vypuski/2026-08-15-vypusk-04/vypusk.teaser.txt` | новый — тизер |
| `docs/PROTOKOL-VYPUSKA.md` | новый — протокол письма, две фазы, каноны К1–К6 |
| `docs/slovar-stop.md` | новый — слова, которых мы не употребляем |
| `vypuski/bootstrap_vypuska.py` | новый — анкета раскладки и отчёт о проверках |
| `vypuski/check_raskladka.py` | новый — гейт фазы 1 |
| `vypuski/check_vypusk.py` | правка — К1–К4, гейт отчёта, лид включён в проходы |
| `vypuski/2026-08-15-vypusk-04/{RASKLADKA,OTCHET}.md` | новые — порождены скриптом, не заполнены |
| `vypuski/proverki.py` | новый — лист обязательных проверок с привязкой к хешу |
| `docs/verifikatory/raskladka.md` · `tekst.md` | новые — промпты верификаторов |
| `docs/PROTOKOL-VYPUSKA.md` | правка — Н5/Н6, раздел «Верификаторы», К7, К8, правки К1/К3/К5, таблица сведения, долги |
| `docs/pravila-vypuska.md` | правка — три пометки ⛔ ОТМЕНЕНО / 🔁 механизм заменён |
| `vypuski/check_raskladka.py` · `check_vypusk.py` | правка — автоотметка и гейт листа проверок |
| `vypuski/2026-08-15-vypusk-04/RASKLADKA.md` · `.proverki.json` | новые — раскладка фазы 1, принята |
| `zhurnal/_INFRA-git/INCIDENTY.md` | правка — этот блок |

Строка вписана рукой (инструмент `git_zona.py` в этот репозиторий не пишет — см. предупреждение выше).

**Что именно вне git** (та же арка, сессия 4 — 16.08, протокол письма и разметка блоков):

| путь | что |
|---|---|
| `docs/PROTOKOL-VYPUSKA.md` | новый — дисциплина письма, каноны К1–К13, долги Д1–Д8 |
| `docs/slovar-stop.md` · `docs/verifikatory/{raskladka,tekst}.md` | новые |
| `vypuski/{proverki,check_bloki,check_raskladka,istochniki,bootstrap_vypuska}.py` | новые |
| `vypuski/{build_vypusk,check_vypusk}.py` | правка — формат К12, каноны К1–К4, гейт листа |
| `vypuski/2026-08-15-vypusk-04/{vypusk.md,RASKLADKA.md,OTCHET.md,.proverki.json}` | новые |
| `vypuski/2026-0*-vypusk-0{1,2,3}/vypusk.razmechen.md` | новые — корпус в формате К12 |
| `docs/{pravila-vypuska,protsedura-vypuska,O-PROEKTE,razvedka-upakovka-sharov}.md` | правки |
| `zhurnal/2026-08-15_vypusk-04/{SESSIYA,PLAN,NAVIGATOR,UROKI-FABRIKE}.md` | правки |
| `zhurnal/2026-08-15_vypusk-04/HANDOFF-2026-08-17.md` + выгрузка транскрипта | новые |

🔴 **Закрывает это Claude-заход, а не владелец.** Прямое слово владельца 15.08: «это не за мной — всю работу с git по дисциплине ты записываешь в специальное место, и следующий Claude-заход оттуда её берёт». Ошибка сессии 2: в хэндоффе было написано «версионировать на машине владельца» — так формулировать нельзя, это перекладывание долга. Строка ниже — не задание владельцу, а материал для захода.

**Команда для захода.** Инструмент живёт в `materials`, репозиторий работы называется отдельной переменной:

```
GIT_ZONA_REPO=/Users/ivanyakovlev/Documents/GitHub/matemdigest-map python3 /Users/ivanyakovlev/Documents/GitHub/materials/_generator/tools/git_zona.py doctor
```

⚠ **Кросс-репозиторный вызов через `GIT_ZONA_REPO` в дисциплине показан только для `check`; на `commit` он не проверен.** Поэтому порядок такой: сперва `doctor` — он назовёт ветку и состояние, — и уже по его выводу собирается команда коммита. Если `GIT_ZONA_REPO` на записи не работает, это долг инструмента, и закрывать его надо заходом, а не голым git из чата.

⛔ **ОТМЕНЕНО 16.08.** ~~Сайт `site/` — отдельный репозиторий, последний коммит `5f3f754` («выпуск 3»).~~ Отдельным репозиторием он быть перестаёт: по решению владельца переезжает подпапкой `sayt/` внутрь единственного репозитория проекта. Механика — шаг 5 заявки.

*(Раздел «Закрытые — 2026-08-16» стоит выше, сразу после открытых: закрытое читают чаще, чем архив по сессиям.)*
