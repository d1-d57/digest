# NOCH — рабочая папка ночной цепочки 18.08

Сюда каждая фаза кладёт свой отчёт `FAZA-<N>.md` и промежуточные артефакты, у которых нет
собственного дома: карту правок, файл-ключ выборки замера, список «откуда что взято».

Папка заведена ЗАРАНЕЕ и намеренно: она входит в зону всех девяти заходов, а `git add` по
несуществующему пути падает `fatal: pathspec did not match any files` и не стейджит НИЧЕГО —
то есть коммит зоны срывается целиком, а не частично.
