# Канал исполнителя — MATEMDIGEST · ЗАХОД-4: широкий невод (подкасты/интервью + иноязычный текст + лауреатские каналы)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> ПЛАН / ВОПРОСЫ / ОТЧЁТ — внизу этого файла.
>
> **Модель: Sonnet 5** (разведка + чтение/перевод + скоринг). Субагенты ≤5, 2 волны.

**ЦЕЛЬ — КАЧЕСТВО, не количество.** Объёма почти хватает (~275 проходящих, ~5/нед). Теперь добрать ~20% базы, **целясь в верхние 20% — бриллианты**: материалы уровня `score_total≥15–16` И с **заметной значимостью (G4≥2)** — интересные математику, а не просто ясные школьные тексты (высокий composite можно набрать одной ясностью — это НЕ цель). За один заход: **широко искать НОВЫЕ источники** (не только seed-список — в любые стороны, включая текстовые интервью) + провалидировать + **добэкфиллить** за окно (2025-07-01…2026-07-10) теми же гейтами. Пол гейта `≥10` не смягчаем (годное не режем), НО приоритет усилий и метрика успеха — **бриллианты**, а не пограничное. Источник, дающий только 10–12 без значимости, — низкий приоритет, не тратить на него время.

**КОНТЕКСТ.** `materials.db` (сейчас 785 строк, проходящий слой 269 после ЗАХОД-3), гейт-слой заполнен. Проходящий слой = `scored_from IN ('meta','text','precise') AND score_total≥10 AND g2_bridge≥1`. Приоритетные пробелы: **интервью/подкасты** (у нас мало) и **континуальные тонкие разделы**.

**Приёмка — по ОТЧЁТУ.** Что провалидировано/отклонено; сколько добэкфилл-строк прошло/добавлено по классам; новый проходящий слой и ~/нед.

---

## 0. ПЕРВЫЙ ХОД
- `git commit -am "pre-zahod4 snapshot"` ПЕРЕД стартом.
- Прочитать ПОИМЁННО: `docs/istochniki-kandidaty.md` §«Второй невод» (**seed-список и приоритеты — главный вход**); `ZAHOD-1c-cal-*.md` §3 (5 гейтов) + §3b (канон area); `ZAHOD-1c-precise-*.md` §4 + `ZAHOD-3-*.md` (проверенные рецепты доступа: `youtube_transcript_api`, `pdftotext`, curl+UA, arXiv-двойник, **Wayback Machine для заблокированных**). Больше проект не изучать.
- Сеть НУЖНА. Кэш — `data/fulltext_cache/<id>.txt`.

## 1. ЧТО ИЩЕМ И БЕРЁМ (три класса; политика языков строгая)
**Seed-список ниже — СТАРТ, не забор.** Активно ищи НОВЫЕ источники в любые стороны, где могут жить бриллианты: текстовые интервью с крупными математиками (в качественной прессе, на сайтах институтов), авторские экспозиции больших учёных, сильные редкие площадки — не только перечисленное. Концептуально: бриллианты живут там, где **большой математик объясняет значимое** широко и ясно. Именно это ищем.

**Политика языков:** иноязычный **ТЕКСТ** — берём и переводим для оценки. Иноязычные **видео/подкаст — ТОЛЬКО при наличии расшифровки/субтитров**. `read_note` — по-русски; исходный язык — в `notes`.

1. **Подкасты и интервью (ГЛАВНЫЙ приоритет).** Из seed: My Favorite Theorem, Numberphile Podcast, The Cartesian Café, Relatively Prime, Carry the Two, Breaking Math (+ искать ещё). Скорить по расшифровке (YouTube-субтитры / сайт-транскрипт). Нет расшифровки → не берём (честно pending).
2. **Иноязычные ТЕКСТОВЫЕ медиа.** Из seed: Images des Mathématiques (FR), El País «Café y Teoremas» (ES), La Gaceta de la RSME (ES), Mitteilungen der DMV (DE), MaddMaths! (IT), Nieuw Archief (NL), кит. 数学文化/返朴 (проверить живьём). Читать полный текст, переводить, скорить.
3. **Лауреатские / индивидуальные каналы (watchlist).** Borcherds (YouTube-курсы, по автосубтитрам) + искать аналоги. Институциональные видео (IHÉS/IAS/Simons/ICTP/Newton/BIMSA/CIRM) — только с транскриптом.

## 2. МЕТОД (на каждый кандидат-источник)
1. **Валидировать живьём:** рабочий механизм доступа + ≥2 реальных недавних примера. Нет → в «отклонённые» с причиной, не бэкфиллим.
2. Для валидных — достать записи **за окно**, **дедуп** против существующих (url + нормализованный title).
3. **Прочитать полностью** (транскрипт/текст; иностранный — перевести для суждения). Недоступно целиком → `content_read=0`, честно по аннотации либо не брать.
4. Оценить 5 гейтов 0–3 (рубрика cal §3 + уточнения). **Порог `score_total≥10 И G2≥1` — не смягчать.**
5. Проходит → **INSERT** новой строкой. Не проходит → не добавлять (посчитать).

## 3. ГРАНИЦЫ и СХЕМА
- Только INSERT новых строк; существующие 785 — НЕ трогать. `fetch_*`, `sources.yaml` — не трогать.
- Новые валидные источники дописать в **`sources_proposed.yaml`** (не переписывать старое; +поля class/mechanism/access_status/examples).
- Схема новой строки — как в ЗАХОД-3 §3: полный набор метаданных; `category` ближайшая A–E (подкаст/интервью→E или C; иноязычный текст-журнал→C; лауреатский канал→B); гейт-слой строго (g 0–3, `areas_multi` в 17-каноне + `образование`, `interdisc`, русский `read_note`, `content_read`, `scored_from='precise'`, `keep=1`, `fit_reason`; язык-оригинал в `notes`). Разделитель тегов — `;` без пробела.
- Каждая строка — на реально дёрнутом URL/транскрипте. Не выдумывать.

## 4. КРИТЕРИЙ ГОТОВНОСТИ (может провалиться)
- По каждому классу: провалидировано / отклонено (с причиной) / добэкфилл: найдено/прочитано/прошло/добавлено — числом.
- **≥1 подкаст/интервью-источник** реально провалидирован и, если есть окно-материал, добэкфиллен (это приоритет владельца).
- Все добавленные строки: валидный гейт-слой, канон-теги, русский `read_note`, реальный `url`; дедуп 0 задвоенных; `count(*)` вырос ровно на добавленные; старые 785 не тронуты (сверить с `git show`).
- **Влияние — по КАЧЕСТВУ (главное):** сколько добавлено и их **распределение по баллам** — сколько `≥15` и `≥16` (это цель, верхние 20%), сколько с `G4≥2`. Успех = много бриллиантов, а не большой счёт. Плюс: новый проходящий слой и ~/нед, сдвиг тонких недель, числа по интервью-типу.
- Верификатор (§5) зелёный.

## 5. ВЕРИФИКАТОР (свежий субагент, ДРУГОЙ метод)
Прямой SQL + независимая проверка: (a) целостность старых 785, `count(*)` = 785 + добавленные; (b) гейт-слой новых (0–3, формула, канон-теги, русский read_note); (c) 8 случайных новых — открыть url/транскрипт, подтвердить реальность и правдоподобность скора (для иноязычных — что перевод/суть верны); (d) 0 дублей. Список расхождений ПУСТ.

## 6. ОТЧЁТ → секция ниже
По классам: провалидировано/отклонено/добавлено · сильные примеры · **новый /нед + число интервью-материалов + сдвиг тонких** · что pending (нет транскрипта/недоступно) · стоимость · верификатор. **README/report.*/`karta-goda.html`/`docs/*` НЕ трогать** — консолидирует аналитик.

---
## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
