# Канал исполнителя — stil-noch-orkestrator (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — Opus: код пишут девять подагентов на Sonnet, а решения на стыках — принять фазу, перезапустить, разобрать грязь — рождаются по ходу и механически не прописываются..

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — ты не пишешь код, а судишь чужой результат и меняешь план по ходу; девять исполнителей под тобой идут на Sonnet.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/matemdigest-map.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/matemdigest-map/zhurnal/2026-08-17_instrument-stilya/kod_stil-noch-orkestrator.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: git branch --show-current, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-19, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 $MAT/_generator/tools/schet_nezakrytogo.py zhurnal/2026-08-17_instrument-stilya`
Область: «zhurnal/2026-08-17_instrument-stilya» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/funny-affectionate-fermi/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. **«Матемдайджест» — рассылка про математику, которую пишет языковая модель, а вычитывает один человек.** Строится инструмент, находящий места, «режущие слух» математику. Работа шла несколько дней, инструментов семь, ресёрч сведён, решения владельца записаны. Ночью надо превратить это в **собранный скилл**.

**ТЫ — ОРКЕСТРАТОР, А НЕ ИСПОЛНИТЕЛЬ.** Ты не пишешь код: ты запускаешь девять готовых заходов по одному, принимаешь каждый и решаешь, что дальше. Все задания уже написаны и трижды проверены.

ЦЕЛЬ: к утру у владельца стоит скилл `~/Documents/GitHub/disciplina/skills/math-style-editor`, собранный по дисциплине скиллов, и все девять инструментов с гейтами.
Приёмка — по ОТЧЁТУ, без построчной сверки. 

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-19) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. финал — собранный скилл disciplina/skills/math-style-editor, а не два числа
2. делать ВСЁ: недоделанная фаза это долг, он дороже потраченной ночи
3. оркестратор на Opus, девять фаз-исполнителей на Sonnet, последовательно, в одной рабочей папке

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `rabota` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `zhurnal/2026-08-17_instrument-stilya` `docs/DOLGI.md` `zhurnal/_INFRA-git` `zhurnal/2026-08-17_instrument-stilya/kod_stil-noch-orkestrator.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 $MAT/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 $MAT/_generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone zhurnal/2026-08-17_instrument-stilya --zone docs/DOLGI.md --zone zhurnal/_INFRA-git --kommitit 'комплект ночной цепочки (девять kod_stil-*.md, RESHENIYA, RESERCH, NOCH, zamery) — ДО заведения рабочей папки, иначе она родится пустой' --zakryt 'ветку zahod/stil-noch НЕ вливать и НЕ гасить — влитие решает владелец утром; рабочую папку stil-noch не удалять'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-08-19, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.



- Проверь ветку: `git branch --show-current` — обязано быть `rabota`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `zhurnal/2026-08-17_instrument-stilya` `docs/DOLGI.md` `zhurnal/_INFRA-git` `zhurnal/2026-08-17_instrument-stilya/kod_stil-noch-orkestrator.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО:   1. 🔴 `zhurnal/2026-08-17_instrument-stilya/RESHENIYA-o-skille.md` — **целиком, до первого запуска.** Границы предмета, решённые владельцем: главный вопрос скилла, что входит и что нет, статус замен, формат карты правок (§10).
  2. Девять файлов `zhurnal/2026-08-17_instrument-stilya/kod_stil-*.md` — задания фаз. **Целиком их читаешь НЕ ты, а подагенты**, каждый свой. Тебе хватает таблицы фаз ниже.
  3. `docs/DOLGI.md`, раздел «ПЕРЕНОС НА ЛЕНТУ НЕ РАБОТАЕТ» — диагноз с числами, если понадобится решать спорное.. Проект не изучай.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 $MAT/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
### Кто ты и чем отличаешься от исполнителей

Ты **не пишешь код**. Ты запускаешь девять заходов по одному, принимаешь каждый и решаешь, что делать
дальше. Код пишут они. Твоя работа — решения на стыках: годится ли результат, надо ли переделать,
можно ли идти дальше, что перезапустить.

**Почему ты на Opus, а они на Sonnet.** Их задачи расписаны механически — понятно, что делать и чем
проверять. Твоя нет: тебе придётся судить о результате, которого никто не видел, и менять план по
ходу. Не экономь на этом рассуждении и не экономь токены там, где решение стоит дороже токенов.
Но **и не жги их зря**: проверяй результат командами, а не чтением файлов целиком (§ПРОВЕРКИ).

### ЧТО ВООБЩЕ ПРОИСХОДИТ

«Матемдайджест» — рассылка про математику, которую пишет языковая модель, а вычитывает один
человек. Строится инструмент, находящий места, «режущие слух» математику: обороты грамматически
верные, но по-русски так не говорят («граница ушла», «оценки пошли навстречу друг другу»).

Работа идёт давно, инструментов накопилось семь, и **18.08 выяснилось, что на новом тексте они
дают 378 «находок» вместо 5** — то есть неупотребимы. Разбор — `docs/DOLGI.md`, раздел «ПЕРЕНОС НА
ЛЕНТУ НЕ РАБОТАЕТ». Свод замыслов — `RESERCH-chto-mozhno-postroit.md`.

🔴 **`RESHENIYA-o-skille.md` — прочти ЦЕЛИКОМ до первого запуска.** Там границы предмета, решённые
владельцем в ночь на 18.08: главный вопрос скилла, что входит и что нет, статус замен, почему порог
длины обязан быть параметром. Заходы его тоже читают, но решения на стыках принимаешь ты.

**Цель ночи, словами владельца:** *«хочется довести до состояния, где я могу забыть весь контекст…
чтобы все механизмы работали на каком-то уровне, и дальше просто можно было бы их улучшать. Вся
архитектура на уровне версии 2.0 выстроена»*. Финал — **собранный скилл-редактор**.

🔴 **ЧЕГО ВЛАДЕЛЕЦ ХОЧЕТ И ЧЕГО НЕ ХОЧЕТ — прочти дважды, ранняя редакция этого файла говорила
ОБРАТНОЕ и была им отвергнута.**

**Хочет:** *«моя цель — не переписывать, не переделывать. Чтобы вся архитектура была полностью
простроена. В архитектуру входит не только последовательность инструментов или шагов скилла, но и
все артефакты между ними, гейты на каждом шаге, между шагами понятное чёткое соединение»*.

**Не хочет:** *«боль моя не в том, чтобы успеть к какому-то времени. Боль в том, чтобы доделать
кусок работы и больше к нему не возвращаться, не иметь проблем, что сделано что-то на 60 %. Я хочу,
чтобы были все инструменты сделаны и во всех проставлены гейты»*.

⇒ **Недоделанная фаза — это долг, за которым придётся возвращаться, и он дороже потраченной ночи.**
Не жертвуй полнотой ради скорости. Пропуск фазы — крайняя мера, а не рабочий ход, и каждый пропуск
объясняется в отчёте отдельной строкой с причиной.

⚠ Отсюда же и про токены: **не экономь их там, где экономия создаёт долг.** Экономь на чтении
файлов целиком там, где хватает команды (§ПРОВЕРКИ), а не на доделывании работы.

### 🔴 ПЕРВЫЙ ХОД — ЧТО ДЕЛАТЬ ПОСЛЕ §0.1

⚠ **§0.1 выше уже отдал весь гит-контур СУБАГЕНТУ, и это сделано нарочно: он разбирает состояние
репозиториев, не засоряя твой контекст.** Дождись его шести строк, вставь их в `## ОТЧЁТ` дословно
и **сам эти пункты не исполняй**. Ниже — только то, что относится к цепочке.

🔴 **ОДНО ДОПОЛНЕНИЕ К §0.1, БЕЗ КОТОРОГО НОЧЬ ПРОЙДЁТ ВХОЛОСТУЮ.** Комплект цепочки собран из
песочницы Cowork, которая в `.git` писать не может, — значит девять `kod_stil-*.md`,
`RESHENIYA-o-skille.md`, `RESERCH-…`, `NOCH/` и `zamery/` лежат **неотслеживаемыми**. `worktree add`
переносит только то, что git знает. **Убедись, что субагент их закоммитил** (это ему поручено
строкой `--kommitit`), и проверь сам:
```
git --no-optional-locks status --porcelain zhurnal/2026-08-17_instrument-stilya   # обязано быть ПУСТО
```
Непусто — закоммить сам, до заведения рабочей папки:
```
git --no-optional-locks add -- zhurnal/2026-08-17_instrument-stilya docs/DOLGI.md zhurnal/_INFRA-git
git --no-optional-locks commit -m "arka-stil: ночная цепочка, решения владельца, ресёрч, замеры" -- zhurnal/2026-08-17_instrument-stilya docs/DOLGI.md zhurnal/_INFRA-git
```
Не прошло — **СТОП**: заведёшь папку раньше, окажешься в ней без единого файла-задания.

### Скачка Википедии — уже закончилась

🔴 **Состояние на 19.08, снято командой:** прогон завершён, **18 818 статей** из 19 572, слой `wiki`
сведён в таблицу — **639 401 пара**. Последняя запись в чекпойнт 18.08 в 09:24. Конкурента за файлы
нет. Убедись одной командой и не трать время: `ps aux | grep -v grep | grep sobrat_korpus` → пусто.

⚠ **Таблица теперь ДРУГАЯ, и числа в заданиях фаз сняты на старой** (378 кандидатов на лекции, 46 %
покрытия). Сейчас слой `wiki` вырос в 500 раз. **Числа в заданиях — ориентиры, а не эталоны:**
прогон дал другое — это не провал фазы, а следствие выросшего корпуса. Назови фактическое и иди
дальше.

### Заведи ОДНУ рабочую папку на всю цепочку

Все девять заходов работают в **одной** папке, потому что наслаиваются друг на друга: фаза 5 сводит
то, что сделали детекторы, а фаза 9 правит по её карте. Разные папки означали бы, что каждый следующий не видит предыдущего.

```
export MAT=~/Documents/GitHub/materials
GIT_ZONA_REPO=$PWD python3 $MAT/_generator/tools/git_zona.py worktree add stil-noch
```

⚠ **Инструмент живёт в `materials`, а работаешь ты в `matemdigest-map` — это разные переменные.**
Голый `python3 $MAT/_generator/tools/git_zona.py` даст «can't open file» при живом инструменте. В
`matemdigest-map` каталога `_generator/tools/` нет вовсе. Форма вызова везде через `$MAT`.

Дальше `cd` в заведённую папку. **Убедись командой, что ты в ней**, и что основная папка осталась
на своей ветке нетронутой.

### Сними исходные числа — потом не с чем будет сравнить

```
python3 vypuski/check_idioma.py vypuski/2026-08-15-vypusk-04 | head -5
python3 vypuski/check_fraza.py  vypuski/2026-08-15-vypusk-04
```

Вывод целиком — в отчёт дословно. Без этого снимка «стало лучше» непроверяемо.

### ФАЗЫ — ПО ОДНОЙ, ПОСЛЕДОВАТЕЛЬНО

| № | файл-заход | модель | что делает |
|---|---|---|---|
| 1 | `kod_stil-1-pochinki-otseva.md` | Sonnet | `check_idioma` перестаёт судить сам себя и чужой формат |
| 2 | `kod_stil-2-klass-dlinnoy-frazy.md` | Sonnet | перегруженное предложение; порог — параметр жанра |
| 3 | `kod_stil-3-klass-a-po-forme.md` | Sonnet | фраза без грамматической структуры, по форме а не по списку |
| 7 | `kod_stil-7-mnogoslovnye.md` | Sonnet | многословные обороты: 13 из 21 стенда не ловятся |
| 8 | `kod_stil-8-terminologiya.md` | Sonnet | `math-russian-terminology` перестаёт ресёрчить известное |
| 4 | `kod_stil-4-instrument-zamera.md` | Sonnet | замер полноты без эталона |
| 5 | `kod_stil-5-porog-obozrimosti.md` | **Opus** | свод гейтов → **карта правок**, главный артефакт (имя файла историческое: порога больше нет) |
| 9 | `kod_stil-9-vnesenie.md` | Sonnet | внесение принятых правок, замыкание цикла |
| 6 | `kod_stil-6-sborka-skilla.md` | **Opus** | сборка скилла + сбор лексики из чужих скиллов |

**Запускать в порядке строк таблицы: 1 → 2 → 3 → 7 → 8 → 4 → 5 → 9 → 6.** Номера в именах файлов
историчны и порядку не соответствуют — это следствие того, что цепочка достраивалась; иди по
таблице, а не по именам.

**Порядок осмысленный, а не алфавитный.** 1 первая, потому что 3, 5 и 7 переиспользуют её
разделение текста на публикуемое и служебное. Детекторы (2, 3, 7) идут до свода. 8 стоит среди них,
потому что независима от всех и её владелец назвал самой важной по отдаче. 5 после всех детекторов,
потому что сводит их результат в карту. 9 сразу после 5, потому что вносит правки по её карте и
замыкает цикл. 6 последняя: собирает скилл вокруг готовой точки входа.

**Фазы 2, 3, 7, 8 между собой независимы** — провалилась одна, остальные всё равно запускай.

🔴 **ФАЗА 5 — УЗКОЕ МЕСТО ВСЕЙ ЦЕПОЧКИ.** Она делает карту правок, а карта и есть то, что владелец
увидит утром и чем будет пользоваться. Провалилась 5 — фазы 9 и 6 повисают: вносить нечего и скилл
собирать не вокруг чего. Если 5 не проходит гейт, **не пропускай её**: перезапусти с уточнением,
это тот случай, ради которого ты на Opus.

### Как запускать заход

Каждый файл-заход самодостаточен: в нём контракт зоны, что читать, задача и критерий готовности.
Запускай подагентом (Task), передав ему ровно одно указание: прочитать свой файл целиком и работать
строго по нему, проект сверх названного не изучая. Модель — из таблицы выше.

🔴 **Ответ любого подагента обязан кончаться строкой «выдано N позиций из M найденных».** Канал мог
оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки —
перезапроси, а не додумывай.

### ПРОВЕРКИ — КОМАНДАМИ, А НЕ ЧТЕНИЕМ

После каждой фазы проверяй результат **своими** командами, не веря отчёту на слово. Это дёшево и
это главный способ не сжечь ночь на вранье:

```
git --no-optional-locks status --porcelain        # что реально изменилось
git --no-optional-locks show --stat               # что попало в коммит
python3 vypuski/<новый или правленый файл> <тест> # инструмент реально работает?
python3 -c "import ast,sys; ast.parse(open('vypuski/X.py').read())"   # хотя бы парсится
```

⚠ **Прогоны по лекции `~/Documents/GitHub/materials/teorkat-vvedenie/LENTA-L2.md` идут долго** —
там синтаксический разбор и сотни кандидатов. Это нормально, не считай зависанием и не убивай
процесс раньше нескольких минут.

**Гейт каждой фазы — критерий готовности внутри её файла-захода.** Он написан так, чтобы мог
провалиться. Прогони его сам, не полагаясь на слова исполнителя. *Причина: за одну сессию работа не
доезжала трижды, каждый раз с честным «сделано» в отчёте.*

### КОГДА ЧТО ДЕЛАТЬ — РЕШЕНИЯ, КОТОРЫЕ ЗА ТОБОЙ

**Фаза прошла гейт** → следующая.

**Фаза провалила гейт** → у тебя три хода, выбирай по существу, а не по порядку:
1. **Перезапустить с уточнением** — если видно, ЧТО именно исполнитель понял не так. Уточнение
   допиши в файл-заход отдельным блоком «ПОПРАВКА ОРКЕСТРАТОРА», не переписывая задание.
   ⚠ Больше двух перезапусков одной фазы — не перезапускай третий раз, переходи к пункту 3.
2. **Принять частично** — если сделана половина и половина полезна. Тогда явно запиши, что не
   сделано, и **предупреди следующие фазы**, если они на это опирались.
3. **Пропустить** — и идти дальше. Законный ход: цепочка устроена так, что фазы 2, 3, 4 не
   обязательны для 5 и 6. Скилл без класса длинных фраз хуже, но он есть; скилла нет вовсе — ночь
   потрачена зря.

**Фаза сделала не то, о чём просили** (например, «заодно улучшила» чужой файл) → откати её правки
за пределами её зоны. ⚠ **Но сначала посмотри, не является ли «чужой путь» её полезным результатом.**
У фаз 6 и 8 зона намеренно выходит за `matemdigest-map`: они пишут в `~/Documents/GitHub/disciplina`,
и это правильно, а не самоуправство. Зона каждой фазы названа в её файле, блок «КОНТРАКТ ЗОНЫ» —
сверься с ним, а не с интуицией. Откатывать `git checkout -- <путь>` можно только то, чего в её зоне
нет; ошибочный откат здесь необратим и уничтожит ровно то, ради чего фаза запускалась.

🔴 **ОСОБО: не требуй от фазы 5 «уложиться в N мест».** Ранняя редакция этого файла ставила потолок
30 — **владелец его отменил**: *«20–30 правок — это было бы очень ценно… и 5 тоже не должно быть.
Совершенно не обязательно сужать это количество»*. Число мест не критерий. Критерий фазы 5 —
**форма карты**: можно ли по записи вынести вердикт, не открывая исходный текст.

### ЧЕГО НЕ ДЕЛАТЬ

- **Не трогай `vypuski/.korpus/` и `vypuski/tablica_sochetaemosti.json.gz`** — их держит работающая
  скачка.
- **Не пересобирай таблицу сочетаемости.** Соблазн будет: свежая таблица улучшила бы числа. Но
  пересборка конфликтует со скачкой, а выигрыш замерен и мал: **+1 процентный пункт покрытия на
  14 млн знаков**, кривая на плато.
- **Не переоткрывай отвергнутое.** Список — `RESERCH-chto-mozhno-postroit.md` §3. Там, в частности,
  дельта-перплексия двух моделей и готовые классификаторы приемлемости: оба закрыты числами.
- **Не расширяй задания фаз.** Заманчиво добавить «ещё один полезный гейт» — не надо: обозримость
  выхода важнее охвата.
- **Не буди владельца вопросами.** Ночь. Всё, что требует его решения, — строкой в отчёт, работа
  продолжается без ответа.

### ГИТ — В КОНЦЕ КАЖДОЙ ФАЗЫ, НЕ В КОНЦЕ НОЧИ

Каждый заход коммитит свою зону сам, последним ходом (это вшито в их файлы). Твоё дело — проверить,
что он это сделал:

```
git --no-optional-locks status --porcelain    # пусто по его путям
```

Не сделал — **разбираешь ТЫ, и вот как именно** (иначе цепочка встанет намертво, а ночью спросить
некого): посмотри `git status --porcelain`, и по каждому пути реши одно из двух. Путь входит в зону
упавшей фазы и её работа полезна — **коммить сам** её же сообщением, отметив в отчёте, что коммит
твой. Работа явно сломана или недоделана — **откати** `git checkout -- <путь>` и запиши, что
откатил. Не оставляй грязь и не жди: следующая фаза работает в той же папке и смешает своё с чужим.

В самом конце ночи: ветку `zahod/stil-noch` **не вливать и не гасить** — влитие решает владелец
утром. Рабочую папку `stil-noch` не удалять ни при каких условиях.

### ОТЧЁТ — ОДИН ФАЙЛ, КОТОРЫЙ ВЛАДЕЛЕЦ ОТКРОЕТ УТРОМ

Пиши в `zhurnal/2026-08-17_instrument-stilya/NOCH/OTCHET-nochi.md` **по ходу**, а не в конце: оборвётся
сессия — останется хоть что-то.

Что в нём обязано быть:

0. 🔴 **АБСОЛЮТНЫЙ ПУТЬ РАБОЧЕЙ ПАПКИ — первой строкой отчёта.** Ты работаешь в
   `~/Documents/GitHub/matemdigest-map-wt/stil-noch`, а владелец утром откроет
   `~/Documents/GitHub/matemdigest-map` и там ничего не найдёт. **Последним ходом ночи скопируй
   отчёт в основную папку:** `cp zhurnal/2026-08-17_instrument-stilya/NOCH/OTCHET-nochi.md ~/Documents/GitHub/matemdigest-map/zhurnal/2026-08-17_instrument-stilya/`
   — и назови в нём абсолютные пути ко всему, что собрано.
1. **Числа до и после** — вывод команд из §«Первый ход» пункт 4 и те же команды в конце.
2. **По каждой фазе одной строкой:** прошла / прошла частично / провалена / пропущена — и почему.
3. **АБСОЛЮТНЫЙ ПУТЬ К СКИЛЛУ** отдельной строкой, если фаза 6 дошла. *Не «скилл собран», а путь,
   который владелец скопирует и откроет. Цена: 31.07 за сессию собрано три артефакта, ни один путь
   не назван в отчёте, владелец не нашёл ни одного.*
4. **Что скилл напечатал** на выпуске 4 — вывод, а не пересказ.
5. **Необратимое отдельным списком** — что · где · чем восстанавливается. Не было — так и напиши
   «необратимого нет»; молчание читается как пустота.
6. **Вопросы владельцу** — всё, что ты решил за него и хочешь, чтобы он перепроверил.
7. **Уроки фабрике** — что в самих заданиях было плохо написано, с ценой. Пустая секция законна;
   выдуманный урок хуже отсутствующего.

### ЕСЛИ ЧТО-ТО НЕ СХОДИТСЯ

**Не сокращай объём работы — сокращай глубину отдельной фазы.** Скажем, гейт многословных оборотов
поймал 7 из 13 вместо желаемого — это результат, его записывают и идут дальше. А вот «пропустим
фазу 7 совсем» — это долг, за которым владелец вернётся.

**Порядок важности, если выбирать всё же придётся** — но только после того, как ты честно попробовал
сделать всё: 5 (карта — то, чем пользуются) → 6 (скилл — финал) → 1 (без неё инструмент врёт на
любом новом тексте) → 8 (самая дешёвая отдача) → 9 → 7 → 4 → 3 → 2.

**Утром владелец принимает отчёты и удаляет из старых мест то, что переехало.** Поэтому список
«откуда что взято» из фазы 6 — не отчётность, а условие следующего шага: без него удалять нечего.

---
*Собран Cowork 2026-08-18 по прямому заказу владельца. Свод замыслов — `RESERCH-chto-mozhno-postroit.md`.
Диагноз с числами — `docs/DOLGI.md`. Дисциплина сборки скилла — `disciplina/KAK-ZAVESTI-SKILL.md`.*


**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).**
1. **Скилл существует и работает:** `~/Documents/GitHub/disciplina/skills/math-style-editor/SKILL.md` на месте, `python3 ~/Documents/GitHub/disciplina/tools/ocenka_skilla.py <путь>` зелёный, `wc -l` тела меньше 500.
2. **Девять фаз пройдены**, по каждой строка в отчёте: прошла / прошла частично / провалена — и почему. Пропуск фазы объясняется отдельно.
3. **Карта правок собрана:** `zhurnal/2026-08-17_instrument-stilya/NOCH/karta-pravok.jsonl` существует, у каждой записи полная фраза и поле вердикта.
4. **Отчёт лежит там, где владелец его найдёт** — скопирован в основную папку, с абсолютными путями ко всему собранному первой строкой.
5. Числа «до» и «после» сняты командами и стоят в отчёте дословно.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Свежий субагент, ДРУГОЙ метод, сверка с бэкапом/`git show`, программно (список потерь ПУСТ; битых ссылок 0). 🔴 **ВЕРИФИКАТОР НУЖЕН — свежий субагент, ПОСЛЕ фазы 6.** Его вопрос: «отвечает ли собранный скилл на главный вопрос владельца — где носитель математического русского так бы не сказал, — и нет ли в нём правил про длину и объём, которые туда не входят». Метод другой, чем у тебя: он читает только `SKILL.md` и `RESHENIYA-o-skille.md` §1 и §4, не видя истории. Ответ обязан кончаться строкой «выдано N позиций из M найденных».

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- zhurnal/2026-08-17_instrument-stilya docs/DOLGI.md zhurnal/_INFRA-git zhurnal/2026-08-17_instrument-stilya/kod_stil-noch-orkestrator.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- zhurnal/2026-08-17_instrument-stilya docs/DOLGI.md zhurnal/_INFRA-git zhurnal/2026-08-17_instrument-stilya/kod_stil-noch-orkestrator.md   # отсекает всё чужое
python3 $MAT/_generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 $MAT/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `zhurnal/2026-08-17_instrument-stilya` `docs/DOLGI.md` `zhurnal/_INFRA-git` `zhurnal/2026-08-17_instrument-stilya/kod_stil-noch-orkestrator.md`

- **Г1. Зона доехала в git.** `python3 $MAT/_generator/tools/git_zona.py check --zone zhurnal/2026-08-17_instrument-stilya` → ✅; `python3 $MAT/_generator/tools/git_zona.py check --zone docs/DOLGI.md` → ✅; `python3 $MAT/_generator/tools/git_zona.py check --zone zhurnal/_INFRA-git` → ✅; `python3 $MAT/_generator/tools/git_zona.py check --zone zhurnal/2026-08-17_instrument-stilya/kod_stil-noch-orkestrator.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `matemdigest-map` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged rabota` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 $MAT/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** Требование владельца
2026-08-16, поводом стал заход, который сделал работу и не закоммитил НИЧЕГО: *«он ничего не
коммитит… всегда, когда собираешь по генератору, там должна быть фраза, что начинать надо с полной
проверки гигиены»*. Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: сначала все коммиты → потом все ветки → потом вывоз.**
Обратный порядок не работает технически: влитие отказывает на грязном дереве, вывоз — на невлитом.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВСЕ ВЕТКИ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged <основная>`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main` там,
где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**3 · ВЫВОЗ.** `git --no-optional-locks log --oneline @{u}.. | wc -l` → 0 по каждому репозиторию.
Ненулевое означает, что работа существует только на этом диске.

**4 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Три числа по каждому репозиторию — вне git · невлитых ·
невывезенных — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число, оставленное без
объяснения, приёмка читает как несделанную работу: она гоняет те же команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

> Заполнено оркестратором до первого действия. Прочитан заход целиком (529 строк).

**Роль.** Я не пишу код. Я запускаю девять заходов по одному, проверяю каждый СВОИМИ
командами (не веря отчёту), и решаю: дальше / перезапустить с «ПОПРАВКОЙ ОРКЕСТРАТОРА» /
принять частично / пропустить.

**Снято первым ходом, до всякой работы:**
- `git branch --show-current` → `rabota` ✅ (ветка та, `checkout` не нужен, `--worktree` не нужен)
- `schet_nezakrytogo.py` → инцидентов без вердикта 0 · уроков без ВЕРДИКТ 0 · очередь «ДОСТАВЛЕНО: нет» 0 · долгов ЖИВ **39** (глобально, не по этой записи)
- `git branch --no-merged | grep -c 'zahod/'` → **0** — вливать нечего, снимок захода подтверждён на живом репозитории
- `git status --porcelain zhurnal/2026-08-17_instrument-stilya` → **18 путей**, из них 15 неотслеживаемых:
  все девять `kod_stil-*.md`, `RESHENIYA-o-skille.md`, `RESERCH-…`, `NOCH/`, `zamery/`, `ORKESTRATOR-noch.md`.
  🔴 Это ровно тот случай, о котором предупреждает §0.1: заведи рабочую папку сейчас — она родится
  пустой, без единого файла-задания. Значит порядок «сначала коммит комплекта, потом worktree»
  обязателен и исполняется буквально.

**ПОРЯДОК ДЕЙСТВИЙ**
1. **Гит-контур — ОДНИМ субагентом** (§0.1), задание печатает `bootstrap_zahod.py --zadanie-subagentu`.
   В его пункт 2 (`<НЕ ЗАПОЛНЕНО>`) вписываю явно: **вливать нечего**, проверено командой, 0 веток
   `zahod/*` невлитых. Дожидаюсь его шести строк; последняя обязана быть «выдано N позиций из M
   найденных» — нет строки, перезапрашиваю, в отчёт не вставляю. Его пункты сам не исполняю.
2. **Проверяю сам**, что комплект цепочки доехал в git: `status --porcelain` по зоне обязан быть
   пуст. Непусто — коммичу сам двумя ходами (`add -- <пути>`, `commit -- <те же пути>`),
   `--no-optional-locks`. Не прошло — СТОП, папку не завожу.
3. **Рабочая папка** `git_zona.py worktree add stil-noch`, `cd` в неё, проверяю командой, что я в
   ней и что основная папка осталась на `rabota` нетронутой. Абсолютный путь — первой строкой отчёта.
4. **Читаю целиком `RESHENIYA-o-skille.md`** — до первого запуска фазы (границы предмета).
5. **Снимаю числа «до»**: `check_idioma.py` и `check_fraza.py` на `vypuski/2026-08-15-vypusk-04`,
   вывод дословно в отчёт. Проверяю `ps aux | grep sobrat_korpus` → пусто (скачка не конкурирует).
6. **Девять фаз по одной, в порядке таблицы: 1 → 2 → 3 → 7 → 8 → 4 → 5 → 9 → 6.** Модель из
   таблицы (Sonnet, кроме 5 и 6 — Opus). Каждому подагенту ровно одно указание: прочитать свой
   файл целиком и работать строго по нему. После каждой:
   - гоняю гейт из её файла-захода САМ (`echo $?` первым, до содержательного вывода);
   - `git status --porcelain` — что реально изменилось и закоммитила ли она свою зону;
   - грязь по её путям разбираю Я: полезное — коммичу сам её сообщением с отметкой в отчёте,
     сломанное — `git checkout -- <путь>` и строка в отчёт. Не оставляю грязь следующей фазе.
   - откат чужого пути — только сверившись с блоком «КОНТРАКТ ЗОНЫ» её файла: у фаз 6 и 8 зона
     намеренно выходит в `~/Documents/GitHub/disciplina`, и это не самоуправство.
7. **Фаза 5 — узкое место**, не пропускаю: провалит гейт — перезапуск с «ПОПРАВКОЙ ОРКЕСТРАТОРА»
   (не больше двух перезапусков). Потолка «N мест» ей НЕ ставлю — владелец его отменил; критерий —
   форма карты (можно ли вынести вердикт, не открывая исходный текст).
8. **Верификатор** — свежий субагент ПОСЛЕ фазы 6, другим методом: читает только `SKILL.md` и
   `RESHENIYA-o-skille.md` §1, §4, истории не видит.
9. **Отчёт пишу ПО ХОДУ** в `NOCH/OTCHET-nochi.md`, не в конце. Последним ходом копирую в основную
   папку. Затем полная гит-гигиена (коммиты → ветки → вывоз, три числа по каждому репозиторию),
   Г1–Г6, и коммит своей зоны двумя ходами.

**ЧЕГО НЕ ДЕЛАЮ:** не трогаю `vypuski/.korpus/` и `tablica_sochetaemosti.json.gz`, не пересобираю
таблицу, не расширяю задания фаз, не переоткрываю отвергнутое (§3 ресёрча), не бужу владельца —
всё спорное строкой в отчёт, работа идёт дальше.

**ПРЕДПОСЫЛКИ, КОТОРЫЕ НАЗЫВАЮ ВСЛУХ ДО РАБОТЫ:**
- Числа в заданиях фаз (378 кандидатов, 46 % покрытия) сняты на СТАРОЙ таблице; слой `wiki` вырос в
  ~500 раз (639 401 пара). Расхождение прогона с этими числами я буду читать как следствие
  выросшего корпуса, а НЕ как провал фазы — так велит сам заход. Провалом считаю только
  невыполнение критерия готовности фазы.
- Долгов «СТАТУС: ЖИВ» 39, но счёт глобальный и по записям не размечен — к этой ночи он не гейт
  (сам заход называет счёт печатью, а не гейтом). В работу их не беру.

**КРИТЕРИЙ ГОТОВНОСТИ — возражений по существу нет**, он проверяем и может провалиться. Одна
оговорка, которую фиксирую заранее, чтобы утром она не читалась как отговорка: пункт 1 требует
`ocenka_skilla.py` зелёным и тело `SKILL.md` меньше 500 строк — это гейт ФАЗЫ 6, и если она
провалится, скилла не будет, а вся работа фаз 1–5 и 9 всё равно останется на диске и в git.
В этом случае отчёт назовёт пофазно, что сделано, и путь к каждому артефакту — «скилла нет»
я напишу прямо, а не замажу.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
git --no-optional-locks branch --no-merged <основная>     # невлитые
git --no-optional-locks status --porcelain | wc -l        # не закоммичено
git --no-optional-locks log --oneline @{u}.. | wc -l      # не вывезено
python3 $MAT/_generator/tools/git_zona.py zayavki              # открытые заявки
```
<сюда — вывод, дословно>

**ЧТО СДЕЛАНО** *(с хэшами)*
<влито / закоммичено / вывезено / погашено / заявки закрыты — поимённо>

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `<да | нет>`
*(`нет` законно — но ТОЛЬКО со списком поимённо: что осталось и почему это непроходимо ТВОИМИ
правами (чужая живая рабочая папка, нужно решение владельца, конфликт, обеих сторон которого
не понимаешь). «Сложно» и «не моя тема» причинами не являются. `нет` без списка = красный.)*

## ОТЧЁТ — (заполняет исполнитель)
**АРТЕФАКТ:** `<АБСОЛЮТНЫЙ путь к собранному файлу, который владелец должен открыть>` — `<чем открывать>`
*(собрал HTML, документ, PDF, картинки — путь сюда. Собранного файла нет — напиши «артефакта нет: <почему>». Пустая строка = отчёт не принимается: гейт `check_uroki.py` краснеет на коммите.)*
**КОММИТ:** `<хэш>` — `<сообщение>` · `git_zona.py check --zone <зона>` → ✅
*(нет хэша — назови причину прямо здесь; пустая строка = отчёт не принимается)*

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 _generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `<принято | доработка | отклонено>` — `<почему именно так, одной фразой: что проверено и чем>`

**ВЕТКА РАБОТЫ:** `rabota`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 $MAT/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/matemdigest-map/_studio/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 $MAT/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 $MAT/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

- `<id заявки>` — `<род>` — `<суть одной строкой: влитие / коммит / вывоз / деплой / гашение>`

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
