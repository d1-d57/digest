# ЗАХОД-1 — чистка и счёт (поверх готовой карты)

> Второй заход по проекту MATEMDIGEST (основной — `ZAHOD-matemdigest-karta-goda.md`, база уже собрана: 746 кандидатов, критерий зелёный).
> Читать ТОЛЬКО этот файл + якоря: `materials.db`, `score.py`, `README.md`, `sources.yaml`. Проект не изучать.
> ПЛАН / ВОПРОСЫ / ОТЧЁТ — в секциях ниже. **Рамки: сырьё не удалять, число не подкручивать.**
> Точка отката: `git` HEAD = `e8d6d55` (коммит перед стартом захода).

## Задача
«Чистка и счёт»: аудит уже собранной базы на то, что не должно попадать в счёт (дубли, остаточный балласт, явный мусор, битые поля), корректная чистка **в духе §2 основного захода** (скорить/тегировать, а не выбрасывать; жёстко дропать только явный не-мат мусор/структурный балласт — со счётчиком; пограничное сохранять) и **честный пересчёт**.

---

## Аудит базы (это «счёт» — census по осям чистоты; всё — прямыми SQL к `materials.db`)

**Чисто (менять нечего):**
- Истинных дублей (совпадают и `url`, и `title`): **0**.
- Остаточного `ADMIN`-балласта (по регэкспу `score.py`): **0** — старый фильтр отработал.
- NULL/пусто в обязательных полях (`title/date/source_name/category/url/breadth_score`): **0**.
- Категорий вне A–E: **0**. Скоров вне 0–5: **0**. URL не-`http`: **0**.
- Дат вне окна (`< 2025-07-01`, полная дата): **0**. Даты «только год» — 22, «год-месяц» — 236 (известный бакет, не дирт).
- 51 «дубль по URL» — это **12 групп с общей посадочной страницей** (ICM plenary ×17, Bourbaki programme ×15, MAA Reviews ×6, Breakthrough News/98 ×5, JMM CEB ×4, Takagi 25th/26th ×3+3, плюс 5 малых пар: 2 книги Тао/Тонга на анонсе Simons, 2 премии Стила на IAS, 2 некролога на Wikipedia-Deaths, 2× статья-Quanta+эпизод «Joy of Why»). Это разные материалы с общим источником-ссылкой → **схлопывать нельзя** (иначе удаление сырья + занижение числа).

**Дирт (реальный):**

1. **Кодировочная грязь в `title` — 12 строк.** Сырой MathML / HTML-entities из Crossref прямо в заголовке (`&lt;em&gt;…&lt;/em&gt;`, `<mml:math xmlns:mml=…>`, `<italic>`). Нечитаемо.
   → Лечение = **нормализация текста** (unescape entities + вырезать теги/MathML → читаемый заголовок). **Строки не удаляются, счёт не меняется (746).**

2. **Структурный фронт-маттер журналов мимо `ADMIN`-регэкспа — 32 строки** (все `type=expository`, пустой `summary`, обычно без автора — служебные записи Crossref, не материалы):

   | Заголовок | Журнал | Строк | Класс |
   |---|---|---|---|
   | Editorial Board | Expositiones Mathematicae | 6 | масштхед — однозначно не материал |
   | Publisher's Note | Expositiones Mathematicae | 2 | служебное — однозначно не материал |
   | Mathematics People | Notices of the AMS | 8 | рубрика, пустые оболочки |
   | Bookshelf | Notices of the AMS | 11 | рубрика, пустые оболочки |
   | Letters to the Editor | Notices of the AMS | 5 | рубрика, пустые оболочки |

   → «Editorial Board» + «Publisher's Note» (**8**) — фронт-маттер журнала, не материал ни по какому чтению; это тот же класс, что уже дропает `ADMIN`-фильтр (список паттернов был неполон). «Mathematics People / Bookshelf / Letters» (**24**) — реальные рубрики Notices, но как затянуты — пустые оболочки (нет summary); по §2 это ближе к «пограничному» → по умолчанию сохранять/тегировать.

---

## ПЛАН

**Разночтение (записано, §1 «не гоняться за фантомом»):** заход-файл `ZAHOD-1-chistka-i-schet.md` в задании не существовал — создан этот. Основной заход — `ZAHOD-matemdigest-karta-goda.md`.

Единый детерминированный проход (одна веха, один коммит), артефакты консистентны:
1. **Нормализация 12 заголовков** — расширить `score.py` функцией `clean_title()` (unescape HTML entities, снять `<mml:*>`/`<italic>`/`<em>` и т.п., схлопнуть пробелы). Прогнать `UPDATE` по 12 строкам. Счёт неизменен.
2. **Балласт** — по ответу на ВОПРОС 1 ниже (объём дропа); реализация — дополнить `ADMIN`-регэксп в `score.py` недостающими паттернами (Editorial Board / Publisher's Note [/ Mathematics People / Bookshelf / Letters]) + один `DELETE` со счётчиком. Дроп логируется в `sources.yaml` (новый ключ `cleanup_zahod1`) — сырьё в `data/*` не трогаю.
3. **Пересборка производных** — `materials.csv` / `materials.json` из БД; `report.py` → `report.md`/`report.html`; `verify.py` (должен остаться зелёным на новом числе).
4. **Отчёт** — секция ниже: до/после, что и почему, новый честный счёт, результат `verify.py`.

Без сети, без новых источников, без трогания скоринговой логики за пределами чистки. Субагенты не нужны (объём мал, суждение локально).

## ВОПРОСЫ

**1. Объём дропа структурного балласта (меняет охраняемое число 746).** Варианты — см. вопрос в чате. Рекомендация: дропнуть только однозначный масштхед (Editorial Board 6 + Publisher's Note 2 = 8 → **746 → 738**), рубрики Notices сохранить (§2). Нормализацию 12 заголовков делаю в любом случае (счёт не трогает).

## ОТЧЁТ

**СТАТУС: чистка выполнена, верификатор зелёный (exit 0), список расхождений ПУСТ. Счёт 746 → 738.**
Обе рамки соблюдены: сырьё в `data/*` не тронуто; число не подкручено — изменилось только на честный дроп 8 масштхед-строк (решение согласовано), с логом и счётчиком.

### Что сделал и почему
1. **Аудит базы прямыми SQL** (см. секцию «Аудит» выше). Вывод: база уже очень чистая — 0 истинных дублей, 0 остаточного `ADMIN`-балласта, 0 NULL в обязательных полях, категории/скоры/URL/окно в норме. 51 «дубль по URL» — законные разные материалы на общих посадочных страницах (не трогал).
2. **Нормализация 20 заголовков** (`score.py::clean_title`, count-нейтрально): 12 с сырым MathML/HTML-entities из Crossref (`&lt;em&gt;…`, `<mml:math …>`, `<italic>`) → читаемый текст; +8 с пробельной грязью (`\xa0`, склейки переносов). Строки не удалялись.
3. **Дроп 8 масштхед-строк** (Editorial Board ×6 + Publisher's Note ×2, все Expositiones Mathematicae) — структурный фронт-маттер журнала (пустой `summary`, не материал), проскочивший мимо неполного `ADMIN`-регэкспа. Расширил `ADMIN` двумя паттернами, дроп — одним `DELETE` со счётчиком. Рубрики Notices (Mathematics People / Bookshelf / Letters, 24) **сохранил** как пограничное (§2).
4. **Пересобрал производные из БД без сети:** `materials.csv`/`materials.json` (738), `report.md`/`report.html` (`report.py`). Лог чистки — в `sources.yaml: cleanup_zahod1` (build-time `kept` не переписывал, добавил пояснение). Числа синхронизированы в `README.md`.

### Как проверил (ДРУГОЙ метод, §4)
`verify.py` (независимый, читает БД напрямую): (a) 9/9 метрик БД==report.md; (b) гейт — 0 NULL, 738≥250, 5/5 категорий, 18≥10 разделов; (c) **15/15** случайных URL → 200; (d) 3/3 FAILED-источника снова 404. **РАСХОЖДЕНИЙ НЕТ, exit 0.**

### Счёт (до → после)
| | до | после |
|---|---|---|
| Всего | 746 | **738** |
| A (журналы) | 267 | 259 |
| B / C / D / E | 43 / 86 / 278 / 72 | 43 / 86 / 278 / 72 |
| Разделов | 18 | 18 |
| breadth ≥ 3 | 539 | 531 |
| Quanta % | 10.5 | 10.6 |
| Материалов/нед | 13.8 | 13.7 |

### Что НЕ трогал
Сырьё `data/*` (кэш фетчей). Скоринговую логику за пределами `clean_title`+`ADMIN`. 51 shared-URL материал (ICM/Бурбаки/Takagi/CEB/книги/премии/некрологи). 2 пары Quanta-статья+эпизод «Joy of Why» (пограничное, разный медиум — оставлены, при желании — отдельный проход по тегам).
Git не коммитил (по умолчанию не коммичу без просьбы) — изменения в рабочем дереве, откат к `e8d6d55` доступен.

### Изменённые файлы
`score.py` (+`clean_title`, +2 паттерна `ADMIN`) · `materials.db`/`.csv`/`.json` · `report.md`/`.html` · `sources.yaml` (+`cleanup_zahod1`) · `README.md` (числа) · этот заход-файл (создан).

### Пограничное на будущее (не делал — вне «явного мусора»)
- Рубрики Notices Mathematics People/Bookshelf/Letters (24 пустых оболочки) — можно тегировать `structural` для фильтра куратора, не удаляя.
- 2 пары Quanta-статья/подкаст с общим URL — при желании развести URL подкаста или пометить тегом `same-topic`.
