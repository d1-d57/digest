# Канал исполнителя — MATEMDIGEST · ЗАХОД-7: обзорный слой — дозор, ICM-пленарки, «What is…» (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> ПЛАН / ВОПРОСЫ / ОТЧЁТ — в секции внизу этого файла. Метрика — КАЧЕСТВО. Часы — норма.
>
> **Модель: Sonnet 5** (весь reasoning в заходе; фетч + чтение + скоринг по готовой рубрике).

**КОНТЕКСТ.** MATEMDIGEST — двухнедельный дайджест математической экспозиции. База `materials.db` (858 строк), проходящий слой `scored_from IN ('meta','text','precise') AND score_total>=10 AND g2_bridge>=1` = 342. Владелец дважды жаловался: **в окне нет свежих обзоров** — survey-слой у выпуска тонкий. ICM-2026 стартует ~23–24 июля (через неделю) — флагманский повод.

**ЦЕЛЬ.** Закрыть дыру «обзоры»: (A) дозор журнального слоя за свежее окно, (B) дотянуть материал к 16 пленарным докладам ICM-2026, (C) сверить сезон Бурбаки, (D) выловить колонку «WHAT IS…?» Notices, (E) попутно починить врущие метаданные `probe_mathnet.py`. Плюс — **честный вердикт с числами**: есть ли в июне–июле 2026 проходящие свежие обзоры или их объективно нет.

**Приёмка — по ОТЧЁТУ, без построчной сверки. СТОП-ФЛАГ (громко):** заход пополняет и дочищает БАЗУ и даёт вердикт — он НЕ собирает выпуск и НЕ трогает `vypuski/`. Сборка выпуска — отдельно, аналитиком.

---

## 0. ПЕРВЫЙ ХОД
- Рабочая папка — корень `matemdigest-map`. Точка отката: `git commit -am "pre-zahod7 snapshot"` ПЕРЕД стартом. Если в `.git` лежат stale-локи (`index.lock` и т.п.) — снять `rm -f .git/*.lock .git/refs/heads/main.lock` и коммитить.
- Прочитать ПОИМЁННО и больше ничего: `ZAHOD-1c-cal-kalibrovka-geytov.md` §3 (рубрика 5 гейтов 0–3, формула `score_total = 2·G1 + 2·G3 + G2 + G4`) + §3b (канон 17 area-тегов); `ZAHOD-1c-precise-idealnaya-razmetka.md` §3–4 (что проставлять в строку + рецепты доступа); `ZAHOD-3-backfill-novyh-istochnikov.md` §3 (схема новой строки — эталон); `fetch_a.py` (какие журналы уже тянутся через Crossref — НЕ переизобретать); `probe_mathnet.py` (для задачи E).
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
- Назвать предпосылки и развилки в ПЛАНЕ; развилку не выбирать молча.
- **Оспорь ложную предпосылку, включая из этого захода, с доказательством.** Ориентиры ниже помечены «подтвердить» — проверяй по живым файлам/сети, не верь на слово.
- Минимум: ровно то, что заказано, без спекулятивных абстракций. Хирургия: каждая изменённая строка трассируется к пункту задания; чужой стиль сохранять.
- СУБАГЕНТЫ (аккуратный режим): ≤5 параллельно, по одному на направления A–D; E — в основном потоке. Рейт-лимит = отступить и доложить, не слепой ретрай. **Границы из §2 — ДОСЛОВНО в промпт КАЖДОГО субагента** (был инцидент: субагент границ из шапки не видел и переписал README).

**Предпосылки-ориентиры (подтвердить, не верить на слово):**
- Бурбаки в базе: 15 строк `source_name LIKE '%Bourbaki%'`, последняя сессия 2026-06-06, все `precise` (сверено аналитиком 16.07 по живой БД).
- ICM: 16 строк `title LIKE 'ICM 2026 plenary lecture — %'`, все `scored_from='heur-out'`, score NULL/2 — заглушки со списком имён, материала нет.
- «What is»: в базе 9 строк с таким паттерном в title; фильтр Notices в `fetch_a.py` режет статьи короче 4 страниц — колонка «WHAT IS…?» обычно 2–3 страницы, могла выпасть.
- **Сеть (измерено 16.07.2026): mathnet.ru с твоей машины НЕ открывается** (гео-стена, только российские IP; VPN владельца не российский). Задач, требующих живого mathnet, в заходе НЕТ — не ходи туда и не изобретай обход. Crossref/arXiv/bourbaki.fr/mathunion.org с машины владельца доступны (ЗАХОД-3 работал).

## 2. ГРАНИЦЫ (дословно в каждый суб-промпт)
«`docs/`, `README.md`, `vypuski/`, `sources.yaml`, `sources_proposed.yaml`, `data/mathnet_probe/`, `data/mathnet_live_raw/`, `report.md`, `report.html`, `karta-goda.html`, `analiz-vypuska.html`, `fetch_*.py`, `config.py`, `score.py`, `build.py`, `verify.py` — НЕ трогать. В `materials.db` существующие строки НЕ менять, единственное исключение — 16 строк `title LIKE 'ICM 2026 plenary lecture — %'` (задача B). Новые строки — только INSERT. Никаких `rm -rf`; откат — точечно через git.»
Разрешено создавать: новые одноразовые скрипты в корне (напр. `dozor_a.py`), кэш текстов `data/fulltext_cache/<id>.txt`, правки `probe_mathnet.py` (задача E).

## 3. ЗАДАЧА

**Общая механика для A–D:** кандидат → дедуп против существующих 858 по `url` и нормализованному `title` → прочитать ПОЛНЫЙ текст (PDF→pdftotext, видео→`youtube_transcript_api`, HTML→curl+UA; пейволл→arXiv-двойник; целиком недоступно → `content_read=0`, скор по abstract честно) → 5 гейтов 0–3 по рубрике cal §3, **планку НЕ смягчать** → проходит (`score_total≥10 И g2_bridge≥1`) → INSERT. Схема новой строки — как в ЗАХОД-3 §3: `id=secrets.token_hex(8)`, полные метаданные, `areas_multi` строго в каноне §3b, `read_note` по-русски 1–2 фразы, язык-оригинал в `notes`, `scored_from='precise'`, `keep=1`. Непроходящее не добавлять, посчитать в отчёте.

**A. Дозор журнального слоя (свежее окно).** Crossref по ISSN из списка `fetch_a.py` за окно **2026-06-15 … дата прогона** (перекрытие с прошлой тягой гасится дедупом). `fetch_a.py`/`config.py` не менять — новый одноразовый `dozor_a.py`, переиспользующий их логику (импорт или копия функций, окно своё). Для Notices в этом дозоре **длину страниц НЕ фильтровать** (см. D). Всё найденное свежее — прочитать и скорить.

**B. ICM-2026 пленарные (главное направление).** Для каждого из 16 докладчиков-заглушек: найти реальный материал доклада — текст в проceedings/препринт на arXiv (пленарные тексты к ICM обычно выложены заранее), страницу доклада на `mathunion.org`/сайте ICM-2026, либо видео с транскриптом. Нашёл → прочитать, скорить по рубрике, **UPDATE той же строки**: гейт-слой, `url` на материал, `summary`, `read_note` (русский), `content_read`, `scored_from='precise'`, дату уточнить, остальные метаданные добить. Не нашёл материала — строку НЕ трогать, посчитать. Это единственные существующие строки, которые заходу можно менять.

**C. Бурбаки — сверка сезона.** Со страницы `bourbaki.fr` (статическая программа; в Crossref по ISSN чисто не лежит) взять полный список экспозе сезона 2025–26 + анонс осени 2026, если опубликован. Сверить с 15 строками базы: пропущенные экспозе за окно 2025-07-01…дата прогона → добавить по общей механике (тексты PDF обычно на сайте семинара/numdam). Существующие 15 строк не трогать.

**D. «WHAT IS…?» (Notices).** По оглавлениям номеров Notices за окно 2025-07-01…дата прогона выловить все колонки «WHAT IS…?», которых нет в базе (фильтр длины их резал). Прочитать (они короткие), скорить, проходящие INSERT. Жанр — учебниковое попадание в мост-не-анонс: короткая экспозиция одного понятия для не-специалиста.

**E. `probe_mathnet.py` — врущие метаданные (попутный фикс, код-only).** После добавления `--live` скрипт: печатает «ВЕРДИКТ: Рычаг 3 (Wayback)» при `lever_used: live`; рапортует в `levers_failed` уже опровергнутый отказ; пишет живые байты поверх Wayback-фикстуры под её старым именем. Починить: вердикт, имена сохраняемых файлов и summary **выводятся из фактического режима прогона**, не из констант; `levers_failed` отражает текущий прогон. Файлы в `data/mathnet_probe/` НЕ переименовывать и не трогать (разгребёт аналитик) — только код. Проверка: прогон БЕЗ флагов (Wayback-путь, сеть на него есть) зелёный, вердикт согласован с `lever_used` в JSON. `--live` с твоей машины непроверяем (гео-стена) — проверь логику ветки сухо (код-ревью/юнит-прикидка) и скажи это в отчёте прямо.

**КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):**
1. По каждому направлению A–D: найдено / прочитано / прошло / добавлено-обновлено — числом.
2. Все новые и обновлённые строки: гейты в 0–3, формула сходится, `areas_multi` в каноне, `read_note` русский, `url` резолвится, `scored_from='precise'`.
3. Дедуп: 0 задвоений url/нормализованного title. `count(*)` вырос ровно на число INSERT; кроме 16 ICM-строк ни одна старая не изменена (сверка с `git show` pre-zahod7).
4. **Главный вердикт:** сколько проходящих обзоров с датой 2026-06/2026-07 стало после захода (было ~0 читабельных); если свежих объективно нет — честно, с числами, планку не подкручивать.
5. ICM: сколько из 16 пленарок получили материал и полноценный скор.
6. `probe_mathnet.py` без флагов — зелёный, вердикт соответствует режиму.
7. Верификатор (§4) зелёный. **Время прогона + токены в отчёте** — числа из счётчика/транскрипта сессии (как ЗАХОД-5: суммарный output и cache read; «счётчик не публикуется» — не ответ, гейт не закрыт).

## 4. ВЕРИФИКАТОР (свежий субагент, ДРУГОЙ метод)
Отдельный субагент, не автор правок: (a) прямой SQL — `count(*)` = 858 + INSERT; diff всех старых строк против `git show` pre-zahod7 → изменены только 16 ICM; (b) у всех новых/обновлённых g в 0–3, `score_total` по формуле, канон area, русский `read_note`; (c) 6 случайных новых/обновлённых — открыть `url`, подтвердить реальность материала и правдоподобие скора; (d) `probe_mathnet.py` без флагов прогнать, сверить печатный вердикт с `lever_used` в выходном JSON. Список расхождений ПУСТ.

## 5. ОТЧЁТ → секция `## ОТЧЁТ`
Что сделал + ЗАЧЕМ / числа по A–D / **главный вердикт о свежих обзорах** / ICM: кто получил материал / что НЕ трогал / вопросы / результат верификатора / **время прогона + потраченные токены (способ: счётчик сессии)**. `README`/`report.*`/визуализации НЕ пересобирать — аналитик.

---
## ПЛАН — (заполняет исполнитель)

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)
