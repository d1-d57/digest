# Канал исполнителя — rucola-probe (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — продукт захода ДИАГНОЗ, а не починка: решение «что дальше» рождается по ходу прогона.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — продукт захода диагноз, а не починка: решение «что дальше» рождается по ходу прогона.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/matemdigest-map.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/matemdigest-map/zhurnal/2026-08-15_vypusk-04/kod_rucola-probe.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: git branch --show-current, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-16, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 $MAT/_generator/tools/schet_nezakrytogo.py zhurnal/2026-08-15_vypusk-04`
Область: «zhurnal/2026-08-15_vypusk-04» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/adoring-vibrant-heisenberg/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. Проект — рассылка «Современная история идей» (matemdigest): выпуски по современной математике, у каждого шесть фаз конвейера. Прошлый этап: у фазы `stil` есть класс дефектов, который машиной не ловится, — **незаконная русская математическая идиома**. «Граница сдвинулась» можно, «граница ушла» нельзя; «квантование меняет алгебру» можно, «машинка дала ответ» нельзя. Владелец различает это на слух, механического носителя у различения нет. Разведка 16.08 закрыла синтаксический путь (гипотеза «незаконно = объект-подлежащее при переходном глаголе» опровергнута на парах вердиктов) и нашла кандидата — классификаторы приемлемости, обученные на RuCoLA.

ЦЕЛЬ: **назвать число — сколько пар «незаконно / законно» классификатор разделяет правильно, и вывести порог замером на принятом материале.** Пар в материале 23; число не бери из этой фразы, а сними командой из корня репо: `python3 -c "import json;print(sum('id' in json.loads(l) for l in open('vypuski/pary_idiom.jsonl',encoding='utf-8')))"` (при сборке 2026-08-16 печатала `23`).

🔴 Отрицательный результат — полноценный успех: «модель не разделяет» закрывает целое направление и экономит следующую сессию. **Браком считается только выдуманный вердикт** — число, не посчитанное кодом, или порог, взятый из статьи вместо замера. Гипотезу аналитика («RuCoLA должна взять этот класс») **не подыгрывай**: аналитик — заинтересованное лицо.

Приёмка — по ОТЧЁТУ, без построчной сверки. Стоп до цели: ты получишь ЧИСЛО и порог, но НЕ гейт — встраивать классификатор в `check_stil.py` этот заход не должен, это решение владельца после результата.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-16) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. Гоним готовый RuCoLA-классификатор сразу на парах, корпус математического русского сперва не ищем
2. Порог выводим замером на принятых выпусках 02 и 03, из статьи не берём
3. Не разделит — пробуем вторую модель из трёх найденных
4. Пар 23, а не 27: число 27 по документам не восстанавливается, строим на фактическом наборе
5. Прогон на машине владельца в Claude Code, канал app
6. ГИТ-КОНТУР ОБЯЗАТЕЛЕН и идёт первым ходом: субагент забирает накопленный хвост в git и вывозит на origin, свою зону заход коммитит сам последним ходом. Отмена предыдущего решения 'коммитов заход не делает' — владелец 2026-08-16: невлитое и незакоммиченное копится и потом стоит куче сил

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `rabota` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `vypuski/probe_rucola.py` `zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt` `zhurnal/2026-08-15_vypusk-04/kod_rucola-probe.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 $MAT/_generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 🔴 ПОПРАВКА СРЕДЫ — читать ДО §0, иначе команды ниже не найдут инструментов

Шаблон рождён в монорепо `materials`; работаешь ты в `matemdigest-map`, и **инструментов фабрики здесь нет физически**. Проверено командой при сборке 2026-08-16: `ls _generator/tools/` печатает один файл `KANON-KOREN`, а `git_zona.py`, `register_doc.py`, `priyomka.py`, `check_termin.py`, `schet_nezakrytogo.py`, `check_tool_contract.py` и `_studio/docs/KARTA.md` в этом репозитории отсутствуют. Поправка ровно одна, и она НЕ отменяет ни одного пункта ниже:

**Инструменты живут в соседнем репозитории — зови их оттуда.** Объяви переменную самым первым ходом, до гит-контура:

```
MAT=/Users/ivanyakovlev/Documents/GitHub/materials
export GIT_ZONA_REPO=/Users/ivanyakovlev/Documents/GitHub/matemdigest-map
```

`GIT_ZONA_REPO` обязателен: без него `git_zona.py` будет судить `materials`, а не тот репозиторий, в котором ты работаешь, — и «зона чиста» окажется правдой про чужое дерево. Ниже по тексту каждая команда вида `python3 _generator/tools/X.py` читается как `python3 $MAT/_generator/tools/X.py`.

🔴 **В `.git` лежит мёртвый лок — снять ДО гит-контура, иначе репозиторий не примет ни одной записи.** Замер аналитика 2026-08-16: `python3 $MAT/_generator/tools/git_zona.py doctor` печатал `🔴 ЛОКИ в .git` — `.git/index.lock`, возраст 61 минута. Порядок: `doctor` (посмотреть, что там сейчас) → лок свежий, секунды-минуты — рядом идёт живой коммит, ЖДАТЬ; лок старый — `python3 $MAT/_generator/tools/git_zona.py clean`. Само число возраста не переписывай из этой строки: оно устареет, снимай `doctor`.

⚠ **Регистрации заход не получил, и это не твоя недоделка:** `register_doc.py` при сборке отказал — дверь судит только `_studio/`, `obzory/`, `_illustracii/`, `_meta/`, а заход лежит в `zhurnal/` проектного репозитория, где своей картотеки нет. Пункт гигиены **Г5** для файла-захода поэтому неприменим; для СВОИХ новых `.md` (если заведёшь) он в силе.

🔴 **Если по ходу окажется, что поправка неверна — это находка для `## УРОКИ ФАБРИКЕ` с ценой, а не повод чинить чужой репозиторий.**

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 $MAT/_generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone vypuski/probe_rucola.py --zone zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt --kommitit 'ХВОСТ, накопленный до тебя в рабочем дереве matemdigest-map: при сборке 2026-08-16 '"'"'git --no-optional-locks status --porcelain | wc -l'"'"' печатал 22 — снять число заново, забрать в git и вывезти на origin ветку rabota' --kommitit 'СВОЮ ЗОНУ последним ходом: vypuski/probe_rucola.py и zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt — форма add + commit с теми же путями после --' --zakryt 'ничего не гасить: worktree не заводился, ветка rabota живая и остаётся рабочей' --zakryt 'ветку rabota в main НЕ вливать: main здесь витрина сайта, влитие туда есть публикация и решение владельца, а не гигиена — при сборке '"'"'git branch --no-merged main'"'"' показывал только саму rabota, веток zahod/* невлитых ноль'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-08-16, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.



- Проверь ветку: `git branch --show-current` — обязано быть `rabota`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `vypuski/probe_rucola.py` `zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt` `zhurnal/2026-08-15_vypusk-04/kod_rucola-probe.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- 🔴 **Проверь сеть ОДНОЙ командой до всякой работы** (после гит-контура §0.1, до задачи §2):
  ```
  curl -sI https://huggingface.co/d0rj/RuModernBERT-small-rucola/resolve/main/model.safetensors | head -3
  ```
  Ждём 302 и `location:` на CDN. Получил 403 или пустоту — **ты в ограниченной сети, содержательная часть захода неисполнима: гит-контур доведи до конца, а дальше останавливайся и пиши в `## ВОПРОСЫ`**, обходов не изобретай. Ровно на этом сгорела половина прошлой сессии: у аналитика в песочнице закрыты все CDN Hugging Face, и веса не качаются ничем.
- Прочитать ТОЛЬКО эти три файла, проект не изучай:
  1. `vypuski/pary_idiom.jsonl` — материал проверки: 23 пары; первая строка служебная (ключ `_`), в ней сказано, как пары собраны и что значит поле `zamena_ot`;
  2. `vypuski/check_stil.py` — функция `bloki(src)`, её переиспользуешь для разбора выпусков; заново не пиши;
  3. `docs/slovar-korpusa.md`, раздел «🔴 Обороты, признанные незаконными» — откуда пары взяты. Нужен, чтобы понимать материал, а не чтобы его пересобирать.

  `docs/DOLGI.md`, хэндоффы и дневник читать НЕ надо: результат в них впишет аналитик на приёмке.
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 $MAT/_generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА

Написать `vypuski/probe_rucola.py` и прогнать его. Сырой stdout целиком положить в `zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt`.

**Шаг 1 · пары.** Скрипт читает `vypuski/pary_idiom.jsonl` (строку с ключом `_` пропускает) и печатает `пар в файле: N`. **N ≠ 23 — печатает красное и останавливается:** файл разъехался с заходом.

**Шаг 2 · модель.** Ставит `torch` (CPU) и `transformers`, качает классификатор, считает для каждого предложения вероятность НЕприемлемости. Порядок моделей — сверху вниз; **переходи к следующей, если разделено меньше 16 пар из 23** (порог остановки, обоснование в шаге 3). Разделено 16 и больше — останавливайся на этой модели и иди к шагу 4.

| № | модель | чем интересна | веса |
|---|---|---|---|
| 1 | `p1746-lingua/ruRoberta-large-rucola-science` | дообучена на НАУЧНЫХ текстах поверх rucola — ближе всего к нашему жанру | 1421 МБ |
| 2 | `RussianNLP/ruRoBERTa-large-rucola` | эталонная от авторов корпуса | 1422 МБ |
| 3 | `d0rj/RuModernBERT-small-rucola` | маленькая, CPU; требует `transformers>=4.48` | 138 МБ |

*Размеры сняты с API Hugging Face 2026-08-16 командой `curl -s "https://huggingface.co/api/models/<имя>?blobs=true"`, не из головы.*

🔴 **Какая метка означает «неприемлемо», проверяй по `config.json → id2label`, а не по номеру класса:** у разных авторов порядок разный, и перепутанный знак даст «модель разделяет всё наоборот» вместо честного результата.

**Шаг 3 · разделение пар.** Для каждой пары: `p_nezak` для поля `nezakonno`, `p_zak` для `zakonno`. Пара разделена правильно, если `p_nezak > p_zak` строго. Печатает:

- таблицу всех пар: `id · klass · zamena_ot · p_nezak · p_zak · разделена да/нет`;
- строку **`разделено X из N`** — X считает код, не ты;
- **распределение счётчиком** (`collections.Counter`): сколько разделено из скольких в каждом классе `bessporno` / `pogranichnoe` / `iz-D27` и отдельно по `zamena_ot` (`vladelec` / `analitik`). Клетка с нулём — либо мёртвая клетка, либо непокрытый случай, и видно это только так;
- на 23 парах монетка даёт около 11–12 разделённых. **Число ниже 16 читается как «не разделяет», и так его и надо назвать** — не как «частичный успех».

**Шаг 4 · порог замером.** Порог из статьи или из карточки модели НЕ брать — прямой запрет владельца, повторяющий урок прошлой сессии: замер идёт до канона, а не после.

- Материал: проза СТРОГИХ блоков принятых выпусков 02 и 03 — `vypuski/2026-08-01-vypusk-02/vypusk.razmechen.md` и `vypuski/2026-08-08-vypusk-03/vypusk.razmechen.md`; разбор — `bloki()` из `check_stil.py`; строгие типы — `utverzhdenie`, `opredelenie`, `vyvod`.
- **Охват печатать числом, посчитанным кодом ДО работы:** `блоков M, строгих S, предложений P`. **Метод разбиения на предложения задан, а не на твой выбор:** `re.split(r'(?<=[.!?])\s+', proza)`, считаются куски длиннее двух слов; другой метод (spaCy, например) даст другое P, и число перестанет сверяться с замером аналитика. *Замер аналитика ЭТИМ ЖЕ методом на живых файлах 2026-08-16: 39 блоков, 32 строгих, 140 предложений. Числа не сошлись при том же методе — файлы изменились: остановись и напиши в `## ВОПРОСЫ`.*
- Эти предложения владелец ПРИНЯЛ — значит ложных срабатываний на них быть не должно. Возьми порог, при котором их ноль (или почти ноль — назови сколько и какие именно предложения).
- Напечатай: **`порог T выведен замером: ложных срабатываний Y из P`**, и рядом — сколько пар из N разделяется уже ЭТИМ порогом (незаконное выше T, законное ниже). Требование сильнее, чем в шаге 3, и число будет меньше — так и надо.

**Шаг 5 · честный охват.** Скрипт печатает блок **«ЧЕГО ЭТОТ ПРОГОН НЕ ПРОВЕРЯЕТ»**: как минимум — что пары короткие и вырваны из абзаца; что у 12 пар из 23 законный вариант реконструирован аналитиком и вердиктом владельца НЕ подтверждён (поле `zamena_ot: analitik`, число снимается тем же однострочником, что и в ЦЕЛИ); что порог выведен на 140 предложениях двух выпусков, а не на большом корпусе. Зелёный вывод с необъявленной слепой зоной опаснее красного: ему верят.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ):** заход сдан, когда выполнено ВСЁ:

1. `vypuski/probe_rucola.py` существует и отрабатывает одной командой без ручных шагов.
2. Сырой stdout целиком лежит в `zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt`.
3. В выводе есть строка `разделено X из N`, где N напечатано из файла пар, а X посчитан кодом.
4. В выводе есть распределение по `klass` и по `zamena_ot` — счётчиком, не прозой.
5. В выводе есть строка про порог с числом ложных срабатываний и охватом `Y из P`, где P посчитано кодом по выпускам 02 и 03. **Порог, взятый из карточки модели или из статьи, — провал критерия**, даже если всё остальное сделано.
6. В выводе есть блок «ЧЕГО ЭТОТ ПРОГОН НЕ ПРОВЕРЯЕТ».
7. В `## ОТЧЁТ` названы: какая модель прогнана и почему остановился на ней, число разделённых пар, порог, и ОДНА фраза вердикта — «инструмент найден» или «не берётся».
8. **ЖИВОЙ ПРОГОН на реальном объекте репозитория, а не на фикстуре:** тот же скрипт прогнан по прозе принятого выпуска 03 целиком и напечатал, сколько предложений он пометил бы подозрением при выведенном пороге — команда `python3 vypuski/probe_rucola.py --vypusk vypuski/2026-08-08-vypusk-03` из корня репо, вывод дословно в `## ОТЧЁТ`. Выпуск принят владельцем, поэтому осмысленный результат здесь — ноль или почти ноль; много красного означает, что порог негоден, и это тоже честный ответ.
9. **Гит-контур §0.1 исполнен и его шесть строк вставлены в `## ОТЧЁТ` дословно**, своя зона закоммичена по §4, хэш стоит в строке `**КОММИТ:**`.

**Провал по существу — законный исход.** Ни одна из трёх моделей не разделяет пары — критерий считается выполненным при условии, что все три прогнаны и числа напечатаны: продукт захода диагноз. Критерий проваливается ровно в двух случаях, и оба проверяются грепом по `probe-rucola-vyvod.txt`: нет строки `разделено X из` (число не посчитано машиной) или нет строки `порог T выведен замером` (порог взят из статьи). Команда приёмки: `grep -c 'разделено .* из\|выведен замером' zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt` → обязано быть 2 и больше.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
Операция не лоссовая — ничего не двигается и не удаляется, но верификатор всё равно ОБЯЗАТЕЛЕН: проверяется НЕ сохранность файлов, а честность числа, и автор прогона — последний, кто увидит в нём ошибку знака.

Запусти **ОДИН свежий субагент**, который НЕ видит твоего кода. Дай ему ровно два файла: `vypuski/pary_idiom.jsonl` и `zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt`. Задание:

- сверить построчно, что каждая пара из файла присутствует в таблице вывода — **сплошная выборка не менее трети, назвать какая**; вердикт несёт охват внутри себя («сверено 8 пар из 23»);
- пересчитать `разделено X из N` по таблице вывода своими руками и сказать, сошлось ли;
- назвать, не перепутан ли знак вероятности: если у ЗАКОННЫХ вариантов вероятность неприемлемости систематически выше — это признак перепутанной метки в коде, а не открытие про язык.

Финальной строкой субагент обязан написать «выдано N позиций из M найденных» — без неё считай ответ усечённым и перезапроси.

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- vypuski/probe_rucola.py zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt zhurnal/2026-08-15_vypusk-04/kod_rucola-probe.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- vypuski/probe_rucola.py zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt zhurnal/2026-08-15_vypusk-04/kod_rucola-probe.md   # отсекает всё чужое
python3 $MAT/_generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 $MAT/_generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `vypuski/probe_rucola.py` `zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt` `zhurnal/2026-08-15_vypusk-04/kod_rucola-probe.md`

- **Г1. Зона доехала в git.** `python3 $MAT/_generator/tools/git_zona.py check --zone vypuski/probe_rucola.py` → ✅; `python3 $MAT/_generator/tools/git_zona.py check --zone zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt` → ✅; `python3 $MAT/_generator/tools/git_zona.py check --zone zhurnal/2026-08-15_vypusk-04/kod_rucola-probe.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `matemdigest-map` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged rabota` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 $MAT/_generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

### Файл-заход пересобран ПОВЕРХ работающего исполнителя, дважды, и исполнителю об этом не сказано

Во время прогона `kod_rucola-probe.md` был перезаписан дважды. Первая пересборка вернула файл к шаблону: `## 2. ЗАДАЧА` стала `<конкретно, по шагам>`, `КОНТЕКСТ` — `<проект в 1–2 фразы>`, список файлов для чтения — `<названные файлы-якоря>`, раздел `ПОПРАВКА СРЕДЫ` исчез целиком, секция `## ПЛАН` с уже записанным планом обнулилась. Вторая пересборка содержание восстановила и принесла новое решение владельца (пункт 6 интервью, критерий 9). Заход самоценен по устройству — «читай только его» — и ровно поэтому пересборка отнимает у исполнителя единственный источник: несколько минут файл физически не содержал задачи. Своими средствами исполнитель подмену не видит: у файла нет ни номера редакции, ни отметки времени сборки, ни лока; я узнал случайно, от среды.

ЦЕНА: (1) план переписан заново целиком — он был записан ДО действий, как велит §0, и уничтожен; (2) дороже — субагент гит-контура, у которого права на запись в `.git` и на `push`, был запущен по редакции БЕЗ поправки среды и потому получил её неполной: без `export GIT_ZONA_REPO=…`. Без этой переменной `git_zona.py` судит и правит соседний репозиторий `materials`, а не рабочий, и по числам подмена не видна — в `materials` в этот момент было ровно 22 грязных пути, столько же, сколько в `matemdigest-map`. Ошибку пришлось догонять письмом вслед уже запущенному субагенту; он переснял `doctor`/`zayavki`/`clean` с правильной переменной и отдельно доказал по reflog, что в `materials` не наследил. Обошлось, но проверка стоила лишнего круга, а класс бага в этом репозитории уже оплачен — коммит `5f8dfe4` «кросс-репозиторный баг log_incident по раскладке materials». Пересборка захода, у которого секция `## ПЛАН` уже НЕ пуста, — это пересборка под работающим исполнителем: она обязана либо отказывать, либо оставлять в файле строку «пересобран HH:MM, редакция N».

### Критерий готовности не ловит вывод, сделанный ПРЕДЫДУЩЕЙ версией скрипта

Критерий требует, чтобы существовал скрипт и чтобы «сырой stdout целиком» лежал в файле, а проверяется всё это грепом по двум строкам. Ни один пункт не связывает файл вывода с той версией скрипта, которая лежит рядом в коммите. Я поймал это на себе: прогон стартовал, я правил скрипт по ходу (заход требовал снимать число `12 пар` кодом, а не текстом), и получившийся `probe-rucola-vyvod.txt` был выводом СТАРОЙ версии — в нём это число стояло прописью. Греп приёмки на таком файле зелёный: обе искомые строки на месте.

ЦЕНА: два полных перепрогона (три модели, четыре загрузки весов по 1,4 ГБ, инференс на CPU) — иначе в коммит уехали бы скрипт и вывод, не соответствующие друг другу, причём расходящиеся ровно в том месте, которое заход называет БРАКОМ: «число, не посчитанное кодом». Поймано глазами, ни одним гейтом не видно. Дешёвая починка: скрипт печатает первой строкой хэш собственного файла, а приёмка сверяет его с `git hash-object` — тогда устаревший вывод краснеет сам.

## ПЛАН — (заполняет исполнитель)

> ⚠ Этот план пишется ВТОРОЙ РАЗ. Первый был записан сюда до всяких действий, как велит §0, и
> затёрт пересборкой файла `bootstrap_zahod.py`, случившейся во время прогона (разбор и цена —
> `## УРОКИ ФАБРИКЕ`). Содержание не изменилось, кроме пункта про гит-контур, который пересборка
> развернула на противоположный; он переписан по НОВОЙ редакции.

**Снято до плана (read-only, §0):** ветка `rabota` ✅; сеть — `302` + `location:` на `us.aws.cdn.hf.co` ✅ (заход исполним); пар в файле `23` ✅; невлитых `zahod/*` — `0` ✅; счёт незакрытого — инцидентов 0, уроков 0, очереди 0, долгов ЖИВ 39 (глобальные); заявок открытых 0; хвост `status --porcelain` — 22 пути.

**Среда, замерена командой, а не предположена:** системный `python3` — 3.9.6 без `torch`; свободно 104 ГБ; кэша HF нет (веса качаются с нуля, ~1.4 ГБ на большую модель); рядом лежат `~/.local/bin/python3.12` и `python3.13`. `_generator/tools/` в ЭТОМ репозитории содержит один файл `KANON-KOREN` — инструменты фабрики живут в `/Users/ivanyakovlev/Documents/GitHub/materials`, и все команды вида `python3 _generator/tools/X.py` исполнимы только через `$MAT`.

**Шаги:**

1. **Гит-контур первым** — один субагент по заданию, напечатанному `bootstrap_zahod.py --zadanie-subagentu`, с поправкой на `$MAT` и с явными запретами (никакого `checkout`/`reset`/`push --force`, в `main` не вливать, зону захода не трогать).
2. **Самобутстрап вместо ручной установки.** `probe_rucola.py` при запуске проверяет `torch`/`transformers` в текущем интерпретаторе; нет — заводит venv в `~/.cache/matemdigest-rucola/venv` на `python3.12`, ставит `torch` (CPU) + `transformers`, и **перезапускает сам себя** внутри venv. Так критерий «одной командой без ручных шагов» выполняется от системного `python3`, а системный питон и репозиторий остаются нетронутыми: venv лежит ВНЕ репо, новых файлов в зоне не появляется.
3. **Шаг 1 захода** — чтение пар, `пар в файле: N`, красное и стоп при `N ≠ 23`.
4. **Шаг 2** — модели сверху вниз, метка «неприемлемо» берётся из `config.json → id2label` (не по номеру класса) и вдобавок **проверяется замером** на контрольной паре «ломаное/целое»: это прямая защита от перепутанного знака, названного заходом. Переход к следующей модели при `разделено < 16`.
5. **Шаг 3** — таблица пар, `разделено X из N`, `Counter` по `klass` и по `zamena_ot`.
6. **Шаг 4** — порог замером на строгих блоках 02+03 через `bloki()` из `check_stil.py` (переиспользую импортом, не переписываю), разбиение предложений ровно заданной регуляркой; сверка охвата с замером аналитика `39 / 32 / 140`.
7. **Шаг 5** — блок «ЧЕГО ЭТОТ ПРОГОН НЕ ПРОВЕРЯЕТ».
8. **Живой прогон** `--vypusk vypuski/2026-08-08-vypusk-03`.
9. **Верификатор** — один свежий субагент на двух файлах.
10. **Коммит своей зоны** последним ходом, `add` + `commit` с теми же путями после `--`.

**ОСПАРИВАЮ ОДНУ ПРЕДПОСЫЛКУ КРИТЕРИЯ — живой прогон частично тавтологичен.** Порог выводится на строгих блоках выпусков 02 и 03; живой прогон идёт по выпуску 03 — то есть по материалу, НА КОТОРОМ порог и калиброван. «Ноль подозрений» там гарантирован построением и потому ничего не доказывает. Поправка, которую внёс, не меняя буквы критерия: прогон `--vypusk` печатает **два числа отдельно** — сколько подозрений на предложениях, участвовавших в калибровке, и сколько на прозе выпуска, в калибровку НЕ входившей (нестрогие блоки). Второе число — единственное, что здесь несёт информацию, и в отчёте названо именно оно. Команда и её дословный вывод в отчёте — ровно те, что заказаны.

**Развилка, названная заранее:** если модель 1 (1.4 ГБ, `ruRoberta-large`) на CPU окажется медленной — это не повод менять модель; 46 предложений пар и 140 предложений выпусков — материал маленький. Менять модель буду ТОЛЬКО по числу `< 16`, как велит шаг 2.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. `vypuski/check_stil.py` не импортируется системным `python3` этого репозитория. `vypuski/proverki.py:124` объявляет `def mark(d: Path, imya: str, sled: str, verdikt: str | None)`, а запись `str | None` в аннотации ВЫЧИСЛЯЕТСЯ при импорте и требует Python 3.10+; на машине владельца `python3` — это 3.9.6. Команда падает с `TypeError: unsupported operand type(s) for |: 'type' and 'NoneType'` ещё до первой строки работы. То есть гейт фазы `stil` не запускается той самой командой, которая написана в его собственной докстринге (`python3 check_stil.py <папка выпуска>`). Обходится любым `python3.10+` — у владельца лежат `~/.local/bin/python3.12` и `python3.13`, мой прогон работал на 3.12 и `bloki()` оттуда импортировал без единой правки. Файл вне моей зоны, не трогал.
   ДОМ: vypuski/proverki.py
   ДОСТАВЛЕНО: нет

2. Пересборка захода поверх работающего исполнителя — разбор и цена в `## УРОКИ ФАБРИКЕ`, строка первая.
   ДОМ: zhurnal/2026-08-15_vypusk-04/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

3. Критерий готовности не связывает файл вывода с версией скрипта — разбор и цена в `## УРОКИ ФАБРИКЕ`, строка вторая.
   ДОМ: zhurnal/2026-08-15_vypusk-04/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

4. `_studio/zhurnal/_INFRA-git/INCIDENTY.md` отрастает в этом репозитории третий раз подряд: инструмент `log_incident` пишет автолог по раскладке монорепо `materials`, и в `matemdigest-map` файл рождается не на своём месте. Прошлые заходы сносили его руками (коммиты `a5e89c3`, `5f8dfe4`) — то есть чинили след, а не причину. Нужно решение: либо `log_incident` учится раскладке проектного репозитория, либо файлу заводится законное место здесь.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. ВОПРОС ПО СУЩЕСТВУ РЕЗУЛЬТАТА, а не по фабрике. Прогон закрыл путь «готовый RuCoLA-классификатор», но попутно назвал, ПОЧЕМУ он закрыт, и это сужает следующий шаг. Модель меряет грамматическую приемлемость; незаконная идиома грамматически безупречна, поэтому шкалы не совпали. При этом та же модель дала `0.9933` предложению «Читается легче обзоров», которое владелец ПРИНЯЛ, — то есть она реагирует на короткие эллиптичные фразы, а не на сочетаемость. Отсюда содержательная развилка для владельца: следующий кандидат — не классификатор приемлемости, а мера СОЧЕТАЕМОСТИ пары (объект, глагол) по корпусу русской математической прозы, ровно как сказано в докстринге `check_stil.py` («различие лежит в СОЧЕТАЕМОСТИ… сверять надо по корпусу либо по языковой модели»). Считать ли это направление следующим заходом — решение владельца, я его не принимаю.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*
```
git --no-optional-locks branch --no-merged <основная>     # невлитые
git --no-optional-locks status --porcelain | wc -l        # не закоммичено
git --no-optional-locks log --oneline @{u}.. | wc -l      # не вывезено
python3 $MAT/_generator/tools/git_zona.py zayavki              # открытые заявки
```
Снято исполнителем ПЕРВЫМ ходом, до всякой работы (субагент к этому моменту ещё не был запущен):

```
$ git --no-optional-locks branch --no-merged
  main
$ git --no-optional-locks branch --no-merged | grep -c 'zahod/'
0
$ git --no-optional-locks status --porcelain | wc -l
22
$ git --no-optional-locks log --oneline @{u}.. | wc -l
0
$ python3 $MAT/_generator/tools/git_zona.py zayavki
✅ заявок нет
Охват: заявок открыто 0, переадресовано 0, постоянных исключений 0, сторож краснеет на 0
```

**ЧТО СДЕЛАНО** *(с хэшами)* — шесть строк субагента гит-контура, вставлены ДОСЛОВНО:

1. заявок было 0, закрыто 0, застряло 0 (`git_zona.py zayavki` с `GIT_ZONA_REPO=…/matemdigest-map` → «заявок нет», охват 0/0/0; id нет ни одного) — но в рабочем дереве лежал бумажный аналог `zhurnal/2026-08-15_vypusk-04/ZAYAVKA-NA-KOMMIT.md` (заявка сессии 2026-08-16, «коммит из песочницы Cowork не проходит»), она и была исполнена коммитами ниже.
2. влито в основную 0 веток из 0 названных; основная ветка снята командой — `main` (`git branch --no-merged` печатает только `main`, веток `zahod/*` ноль, подтверждено заново); `rabota` в `main` не вливалась по прямому запрету, `main` == `origin/main` (5f3f754), не тронута.
3. хвост Cowork ЗАБРАН четырьмя коммитами (`git log --oneline 2710ecd..HEAD`): 926142f — `zhurnal/2026-08-15_vypusk-04/ZAYAVKA-NA-KOMMIT.md` (индекс/автолог, `git_zona --zone`); a172770 — `docs/PROTOKOL-VYPUSKA.md`, `docs/DOLGI.md`, `docs/kanony.md`, `docs/slovar-korpusa.md`, `docs/fazy/{razvedka,raskladka,karta,tekst,ponyatnost,stil}.md`, `docs/verifikatory/{karta,ponyatnost,stil}.md` (`git_zona --zone docs`); 57722b0 — `vypuski/{proverki,check_raskladka,check_vypusk,istochniki,check_ponyatiya,check_stil}.py`, `vypuski/pary_idiom.jsonl`, `vypuski/2026-08-15-vypusk-04/{.proverki.json,PONYATIYA.md}`; 91036bc — `zhurnal/2026-08-15_vypusk-04/{SESSIYA.md,HANDOFF-2026-08-18.md}`; последние два — ГОЛЫМ git в форме `add -- <пути>` + `commit -m … -- <те же пути>`, потому что `--zone vypuski` и `--zone zhurnal/2026-08-15_vypusk-04` затянули бы запретную зону захода; `vyvezti --yes` → `rabota` push прошёл, `origin/rabota` 2710ecd → 91036bc, впереди/позади 0/0; ОСТАЛОСЬ вне git 4 пути (было 24, `git status --porcelain`): `_studio/` (внутри ровно два файла — `_studio/.commit-plan`, уже в `.gitignore`, и `_studio/zhurnal/_INFRA-git/INCIDENTY.md` — stray кросс-репозиторного бага `log_incident`, который прошлые заходы коммитами a5e89c3 и 5f8dfe4 уже дважды сносили руками, поэтому не коммичу и не сношу), плюс три пути зоны захода `vypuski/probe_rucola.py`, `zhurnal/2026-08-15_vypusk-04/kod_rucola-probe.md`, `zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt`; `KARTA.md`/`VERDIKTY.md`/`UROKI-FABRIKE.md` в грязных не значились (первых двух в репозитории нет вовсе), `--no-verify` не применялся — хуков в `.git/hooks` нет и `core.hooksPath` не задан.
4. погашено НИЧЕГО (worktree не заводился, `rabota` живая и остаётся рабочей, `main` не трогалась) — rc всех выданных команд: `zayavki` rc=0, `doctor` rc=0, `clean` rc=0, `commit --zone` ×2 rc=0, голый `add`+`commit` ×2 rc=0 (первая попытка упала rc=128 на zsh-разбиении переменной в pathspec, ничего не изменив, повторена массивом), `vyvezti --yes` rc=0.
5. необратимое: снят мёртвый `.git/index.lock` (0 байт, создан 17:41, возраст 63 мин, живых git-процессов 0, дескриптор держала песочница `com.apple.Virtualization.VirtualMachine` только на чтение) командой `git_zona.py clean` · в `/Users/ivanyakovlev/Documents/GitHub/matemdigest-map/.git/` · не восстанавливается и не требует восстановления — файл пустой и служебный, без него пункты 0 и 3 были технически невозможны; называю отдельно, так как запрет «не чини чужое состояние» это касается. Проверка на след в чужом репозитории по поправке координатора: в `materials` МОИХ коммитов НЕТ — последний там 024d418 от 15:43:56 на ветке `arka/mat-kostyak`, моя сессия началась в 18:43, reflog чист; ранние вызовы `doctor`/`zayavki`/`clean`, выданные до поправки без `GIT_ZONA_REPO`, работали по CWD и относились к `matemdigest-map` (переснято с переменной — вывод посимвольно тот же: ветка `rabota`, HEAD 2710ecd), локов в `materials` не было и не тронуто.

выдано 15 позиций из 15 найденных

**ПРОВЕРЕНО ИСПОЛНИТЕЛЕМ НЕЗАВИСИМО** (галочка — не источник истины; те же команды после работы субагента):

```
$ git --no-optional-locks log --oneline -5
91036bc zhurnal выпуска 4: дневник сессии 3 + хэндофф на 18.08
57722b0 проверки выпуска: REESTR на именованные фазы + два новых верификатора
a172770 протокол выпуска: шесть именованных фаз вместо двух
926142f автолог/индекс: заявка на коммит сессии 2026-08-16 — Cowork не пишет в .git из песочницы
2710ecd мусор: удалить .DS_Store/.fuse_hidden*/materials.db-shm/-wal (23 пути)
$ git --no-optional-locks log --oneline @{u}.. | wc -l
0
$ git --no-optional-locks rev-parse main origin/main
5f3f75474f8c346967221dbc1ce4bfffc68c93db
5f3f75474f8c346967221dbc1ce4bfffc68c93db
$ git --no-optional-locks status --porcelain
?? _studio/
?? vypuski/probe_rucola.py
?? zhurnal/2026-08-15_vypusk-04/kod_rucola-probe.md
?? zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt
```

Сходится со сказанным субагентом: четыре коммита на месте, невывезенного ноль, `main` не сдвинут, вне git — только `_studio/` и три пути моей зоны.

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет`

Осталось незакрытым ровно одно, поимённо:
- `_studio/zhurnal/_INFRA-git/INCIDENTY.md` — stray-файл кросс-репозиторного бага `log_incident`: инструмент пишет автолог по раскладке `materials`, и в `matemdigest-map` файл рождается не на своём месте. Непроходимо правами субагента и моими: это не мусор (в нём живые строки автолога) и не содержательная работа зоны, а дефект чужого инструмента; прошлые заходы дважды сносили его руками (коммиты a5e89c3 и 5f8dfe4), и он отрос снова. Нужно решение владельца — чинить `log_incident` или завести файлу законное место в этом репозитории. Второй путь, `_studio/.commit-plan`, долгом не является: он в `.gitignore`.
*(`нет` законно — но ТОЛЬКО со списком поимённо: что осталось и почему это непроходимо ТВОИМИ
правами (чужая живая рабочая папка, нужно решение владельца, конфликт, обеих сторон которого
не понимаешь). «Сложно» и «не моя тема» причинами не являются. `нет` без списка = красный.)*

## ОТЧЁТ — (заполняет исполнитель)

### ВЕРДИКТ ОДНОЙ ФРАЗОЙ

**Не берётся.** Готовый RuCoLA-классификатор незаконную математическую идиому не ловит: ни одна из трёх моделей не дошла до порога 16 из 23, а порог, выведенный замером, разделяет 0 пар из 23.

### ЧИСЛА (все посчитаны кодом, ни одно не взято из захода и не оценено на глаз)

| модель | разделено из 23 |
|---|---|
| `p1746-lingua/ruRoberta-large-rucola-science` | 11 |
| **`RussianNLP/ruRoBERTa-large-rucola`** | **14** ← лучшая |
| `d0rj/RuModernBERT-small-rucola` | 10 |

- **Почему остановился на второй модели:** не потому, что она прошла, — не прошла ни одна. Правило шага 2 гнало скрипт сверху вниз, все три дали меньше 16, и порог по шагу 4 выведен на ЛУЧШЕЙ из прогнанных: иначе неизвестно, дело в пороге или в модели.
- **`14 из 23` — это не «почти»:** вероятность получить 14 и больше случайно, честной монеткой на 23 парах, `p = 0.2024`. От случайности не отличимо. Число считает код (`math.comb`), а не я.
- **Порог:** `0.993342 выведен замером: ложных срабатываний 0 из 140`. Порог упирается в предложение, которое владелец ПРИНЯЛ, — «Читается легче обзоров» (блок 1.3 выпуска 02) получило `p_neprijem = 0.993342`. Порогом с нулём ложных срабатываний **разделяется 0 пар из 23**. Ослабление до 5 ложных срабатываний даёт 1 пару из 23. То есть отсечка не подбирается: шкала меряет не то.
- **Охват калибровки:** `блоков 39, строгих 32, предложений 140` — сошлось с замером аналитика 2026-08-16 посимвольно, файлы не разъехались.
- **Распределение (лучшая модель):** по классу — `bessporno 5/8`, `pogranichnoe 6/12`, `iz-D27 3/3`; по источнику замены — `vladelec 7/11`, `analitik 7/12`. Мёртвых клеток нет. Клетка `iz-D27 3/3` заманчиво выглядит успехом — это три пары, на трёх парах отличить умение от везения нельзя.

### ЖИВОЙ ПРОГОН (критерий 8) — вывод дословно

Команда: `python3 vypuski/probe_rucola.py --vypusk vypuski/2026-08-08-vypusk-03` из корня репо.

```
ЖИВОЙ ПРОГОН · vypuski/2026-08-08-vypusk-03/vypusk.razmechen.md · порог T = 0.993342
==============================================================================
блоков 17, строгих 14, предложений 66 (из них 60 участвовали в калибровке порога, 6 — нет)
помечено подозрением при пороге 0.993342: 0 из 66
  из них на КАЛИБРОВОЧНЫХ предложениях: 0 (ноль здесь гарантирован построением порога и потому ничего не доказывает)
  из них на прозе ВНЕ калибровки: 0 из 6 — вот это число несёт информацию
    (ни одного — выпуск принят владельцем, это осмысленный результат)

максимум p_neprijem по выпуску: 0.9788 · Л2 · Софичность — из самых слабых способов сказать, что бесконечную группу можно приб
```

🔴 **Читать этот ноль как успех нельзя, и я предупредил об этом в `## ПЛАН` до работы.** Порог калиброван на строгих блоках выпусков 02 и 03, а живой прогон идёт по выпуску 03 — 60 из 66 предложений те же самые, и ноль на них гарантирован построением. Информацию несут только 6 предложений вне калибровки, и там тоже ноль. Шесть — слишком мало, чтобы что-то доказывать; честная формулировка: ложных срабатываний не обнаружено, проверено 6 предложений из 6 непокрытых.

### ЧТО СДЕЛАНО И ЗАЧЕМ

Написан `vypuski/probe_rucola.py` — он ставит `torch` (CPU) и `transformers` сам, в venv вне репозитория, и перезапускается в нём, поэтому критерий «одной командой без ручных шагов» выполняется от системного `python3` (3.9.6, где `torch` нет и быть не может). Скрипт переиспользует `bloki()` из `check_stil.py` импортом, заново её не пишет. Три вещи усилены против буквы задания, все три — в сторону проверяемости:

1. **Знак метки проверяется ЗАМЕРОМ, а не только по `id2label`.** Две из трёх моделей отдают безымянные `LABEL_0`/`LABEL_1`, где `id2label` не отвечает на вопрос вовсе; для них взято соглашение корпуса RuCoLA, и оно тут же проверено контрольной парой «Мальчик читают книга на столом» против «Мальчик читает книгу за столом». Все три модели дали ломаному предложению вероятность выше — знак верный, «модель разделяет всё наоборот» исключено замером, а не рассуждением.
2. **Вероятности печатаются научной записью** (`2.6136e-03`), а не с четырьмя знаками. У первой модели обе вероятности пары меньше `1e-4`, и при округлении столбцы становятся неразличимы — верификатор, которому §3 велит ПЕРЕСЧИТАТЬ `разделено X из N` по таблице, физически не смог бы этого сделать.
3. **Вероятность случайного результата считается кодом**, чтобы «14 из 23» не выглядело успехом на глаз.

### КАК ПРОВЕРЯЛ

- Приёмочный греп захода: `grep -c 'разделено .* из\|выведен замером' …/probe-rucola-vyvod.txt` → **31** (требуется ≥2).
- Код возврата снят ПЕРВЫМ, до чтения содержимого: `EXIT_OSNOVNOY=0`, `EXIT_ZHIVOY=0`.
- Среда проверена до всего остального: `curl -sI` на веса → `302` + `location:` на `us.aws.cdn.hf.co`. Это ровно то место, где сгорела прошлая сессия; здесь сеть открыта.
- Числа воспроизведены: полный прогон делался дважды (см. `## УРОКИ ФАБРИКЕ`), оба раза дал `11 / 14 / 10` и порог `0.993342`. Модели детерминированы, расхождений нет.
- Охват `39/32/140` сверен с замером аналитика — сошёлся, поэтому в `## ВОПРОСЫ` останавливаться не потребовалось.

### РЕЗУЛЬТАТ ВЕРИФИКАТОРА (свежий субагент, моего кода не видел, получил ровно два файла)

1. Сверка построчно: сплошная выборка **подряд B1…P4, 12 пар** (треть от 23 — это 8, взято с запасом), затем пройден и остаток; сверялись `klass` и `zamena_ot` с jsonl. **Сверено 23 пары из 23, расхождений 0.**
2. Пересчёт руками: «да» — 14 строк, «НЕТ» — 9, сумма 23. Напечатано `разделено 14 из 23` — **сошлось**. Пометки проверены как числа на 8 строках, все верны.
3. Знак **не перепутан**, три основания: перевес 14 против 9 в ожидаемую сторону; все крупные разрывы идут в правильном направлении (B8 ×245, P10 ×143), обратных такого масштаба нет; контрольный замер в самом выводе (0.9975 против 0.0025). Вывод верификатора дословно: «малое разделение — свойство результата, а не ошибка знака».
4. Финальная строка охвата на месте: «выдано 23 позиций из 23 найденных».

### ЧТО НЕ ТРОГАЛ

Всё вне зоны. В частности: не встраивал классификатор в `check_stil.py` — заход прямо запрещает, это решение владельца после результата; не чинил `vypuski/proverki.py`, хотя он ломает импорт под системным питоном (в `## ВОПРОСЫ` пункт 1); не трогал `_studio/zhurnal/_INFRA-git/INCIDENTY.md` и не сносил его, как делали прошлые заходы (пункт 4); не вливал `rabota` в `main`; не менял ветку.

### НЕОБРАТИМОЕ

- Снят мёртвый `.git/index.lock` (0 байт, возраст 63 мин, живых git-процессов 0) · `/Users/ivanyakovlev/Documents/GitHub/matemdigest-map/.git/` · восстановления не требует, файл пустой и служебный. Сделано субагентом гит-контура, без него влитие и коммиты были технически невозможны.
- Четыре коммита хвоста и `push` на `origin/rabota` (`2710ecd` → `91036bc`) · восстанавливается `git reset --hard 2710ecd` плюс `push --force`, если владелец решит откатить.
- Файл `zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt` перезаписывался трижды (два первых прогона, третий — итоговый) · восстанавливается только перепрогоном, промежуточных версий не сохранял: они и были дефектными, ради чего перегонялось.
- Заведён venv `~/.cache/matemdigest-rucola/venv` (~850 МБ) и кэш весов `~/.cache/huggingface` (~2,8 ГБ) — вне репозитория; удаляются `rm -rf`, скрипт заведёт заново.
- Больше необратимого нет.

### ПОВТОРЯЕМОСТЬ НАХОДОК

- **Повторится на СЛЕДУЮЩЕМ заходе, класс НЕМЕДЛЕННОЕ — оба урока фабрике.** Пересборка захода под работающим исполнителем повторится ровно тогда, когда аналитик снова дособерёт заход по ходу прогона, а это здесь норма, а не случай. Разрыв «вывод от старой версии скрипта» повторится на любом заходе вида «скрипт + его stdout», то есть на всех пробных: греп приёмки его не видит по устройству.
- **Не повторится, законно уходит пунктом очереди:** `proverki.py` под 3.9 (чинится одной строкой и один раз), stray `INCIDENTY.md` (дефект чужого инструмента, повторяется, но не в моей единице работы).
- **Находка сделана ПРОБНЫМ прогоном и потому чинится ДО следующего:** оба урока — про механику фабрики, и оба дешевле починить сейчас, чем оплачивать ещё раз.

### ВРЕМЯ ПРОГОНА И ТОКЕНЫ

Неприменимо: канал `app`, следа с числами не оставляет.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/matemdigest-map/zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt` — любой текстовый редактор; главные числа в конце файла, раздел «СВОДКА». Второй файл, который владелец может захотеть открыть: `/Users/ivanyakovlev/Documents/GitHub/matemdigest-map/vypuski/probe_rucola.py`.

### ГИГИЕНА ПЕРЕД СДАЧЕЙ (§4.1) — вывод команд, не пересказ

- **Г1. Зона доехала в git** — все три команды зелёные:
  `✅ зона vypuski/probe_rucola.py: работа доехала в git, вне git ничего нет.` (rc=0)
  `✅ зона zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt: …` (rc=0)
  `✅ зона zhurnal/2026-08-15_vypusk-04/kod_rucola-probe.md: …` (rc=0)
  Все три сняты с `export GIT_ZONA_REPO=…/matemdigest-map`, и инструмент это подтвердил строкой `🔎 Репозиторий: …/matemdigest-map · ветка: rabota` — без переменной он судил бы `materials`.
- **Г2. Второй репозиторий** — неприменимо: все три пути зоны лежат внутри `matemdigest-map`, за его пределы зона не расширялась. Инструменты вызывались из `materials`, но НЕ правились: `git --no-optional-locks -C …/materials status --porcelain | wc -l` → `22`, столько же, сколько было на входе.
- **Г3. Невлитых веток не прибавилось** — `git --no-optional-locks branch --no-merged rabota` → `main`, число `1`. На входе было ровно то же (`main`, веток `zahod/*` — 0). Не выросло.
- **Г4. Новый инструмент имеет живую точку вызова** — ни одного нового `.py` в `_generator/**` не заведено (`git show --name-only e23c63e | grep -c '_generator/'` → `0`). Единственный новый `.py` — `vypuski/probe_rucola.py`, и его точка вызова живая: он прогнан обеими своими командами, обе с rc=0, вывод в артефакте.
- **Г5. Новый `.md` зарегистрирован** — неприменимо, и это сказано самим заходом: `register_doc.py` судит только `_studio/`, `obzory/`, `_illustracii/`, `_meta/`, а файл-заход лежит в `zhurnal/` проектного репозитория, где картотеки нет. Своих новых `.md`, кроме самого файла-захода, я не заводил.
- **Г6. В коммите нет чужих путей** — `git --no-optional-locks show --stat`: ровно три файла, все мои (`vypuski/probe_rucola.py`, `zhurnal/2026-08-15_vypusk-04/kod_rucola-probe.md`, `zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt`), 1231 вставка, ни одного чужого пути.

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/matemdigest-map/zhurnal/2026-08-15_vypusk-04/probe-rucola-vyvod.txt` — любой текстовый редактор; главные числа в конце файла, раздел «СВОДКА».

**КОММИТ:** `e23c63e` — «проба RuCoLA: классификатор приемлемости незаконную идиому не берёт — 11/14/10 из 23, порог 0.993342 разделяет 0 пар» · `git_zona.py check --zone …` по всем трём путям зоны → ✅ ✅ ✅
*(строка отчёта с этим хэшем дописана следующим коммитом — сам хэш не может лежать в файле, который им же коммитится)*

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 $MAT/_generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `<принято | доработка | отклонено>` — `<почему именно так, одной фразой: что проверено и чем>`

**ВЕТКА РАБОТЫ:** `rabota`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 $MAT/_generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/matemdigest-map/_studio/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 $MAT/_generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 $MAT/_generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

- `<id заявки>` — `<род>` — `<суть одной строкой: влитие / коммит / вывоз / деплой / гашение>`

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*
