# Канал исполнителя — MATEMDIGEST · ЗАХОД-8: выложить выпуск 1 на GitHub Pages (один заход до конца)

**Кому:** Claude Code, чистый контекст. Читать только этот файл и то, что здесь названо. В проект не погружаться — заход самоценный.
**Рабочая папка:** корень репозитория `matemdigest-map` (там, где лежит этот файл).
**Оценка:** 10–20 минут. Задача техническая, содержательных решений принимать не нужно.

## 0. ПЕРВЫЙ ХОД

Прочитать этот файл целиком, записать план в `## ПЛАН` внизу, начать. Если что-то блокирует — писать в `## ВОПРОСЫ` и продолжать с тем, что можно сделать без ответа.

## 1. ЧТО ЕСТЬ СЕЙЧАС

В репозитории лежит рабочий материал проекта: база `materials.db`, заходы `ZAHOD-*.md`, внутренняя документация `docs/`, черновики. **Всё это остаётся приватным и наружу не идёт.**

Публикуется только папка `site/`:

```
site/
  index.html      ← оглавление дайджеста
  1/index.html    ← выпуск 1, самодостаточный HTML (шрифты с Google Fonts, больше внешних зависимостей нет)
```

Обе страницы готовы, править их содержимое **не нужно**.

**Важно:** в `site/` уже лежит `.git`, оставшийся от проверки — с одним тестовым коммитом, веткой `master` и автором `test <t@t>`. Его надо удалить и начать заново, иначе в публичную историю уедет мусор.

## 2. ГРАНИЦЫ

- Трогать разрешено: `site/` целиком, и в самом конце — одну строку `page_url:` в `vypuski/2026-07-25-vypusk-01/vypusk.md` (см. §3, шаг 6).
- **Не трогать:** `materials.db`, `vypuski/build_vypusk.py`, `docs/`, любые `ZAHOD-*.md` кроме секций `## ПЛАН`, `## ВОПРОСЫ`, `## ОТЧЁТ` внизу этого файла, корневой `.git` репозитория `matemdigest-map`.
- Ничего из корня репозитория в публичный репозиторий не копировать.
- Репозиторий делаем **публичным** — это осознанное решение владельца, страница предназначена для читателей телеграм-канала.

## 3. ЗАДАЧА

1. **Убрать тестовый git:** `rm -rf site/.git`.
2. **Инициализировать заново** внутри `site/`: ветка `main`, автор — настоящая учётка владельца (та, что настроена в `gh`/git глобально; если глобального `user.name`/`user.email` нет — взять из `gh api user` и выставить локально в этом репозитории).
3. **Создать на GitHub публичный репозиторий `digest`** через `gh repo create` (без README, без .gitignore, без лицензии — чтобы не пришлось мержить). Если репозиторий с таким именем уже существует — не ломать его, а написать об этом в `## ВОПРОСЫ` и остановиться на этом шаге.
4. **Закоммитить и запушить** содержимое `site/` в `main`. Сообщение коммита: `выпуск 1: награды конгресса математиков 2026`.
5. **Включить GitHub Pages**: источник — ветка `main`, папка `/` (корень). Через `gh api` или `gh repo edit`, вручную в браузер не ходить. Дождаться, пока сборка Pages пройдёт (`gh api repos/<owner>/digest/pages/builds/latest` до статуса `built`, но не дольше 5 минут), и **проверить curl-ом**, что обе страницы отдают 200 и в теле выпуска есть строка `Гипотеза Какейи`:
   - `https://<логин>.github.io/digest/`
   - `https://<логин>.github.io/digest/1/`
6. **Вписать адрес в источник выпуска.** В файле `vypuski/2026-07-25-vypusk-01/vypusk.md` в шапке есть пустая строка `page_url:`. Проставить туда `https://<логин>.github.io/digest/1/` и пересобрать: `cd vypuski/2026-07-25-vypusk-01 && python3 ../build_vypusk.py vypusk.md`. Генератор — чистый stdlib, сети и зависимостей не требует; он перезапишет `vypusk.html`, `vypusk.telegram.txt`, `vypusk.teaser.txt` рядом с источником. Проверить, что линтер зелёный и что в `vypusk.teaser.txt` появилась строка «Читать целиком →» с этим адресом.
7. **Обновить опубликованную копию:** заново скопировать `vypuski/2026-07-25-vypusk-01/vypusk.html` в `site/1/index.html`, закоммитить и запушить (`выпуск 1: адрес страницы в тизере`).

## 4. ПРОВЕРКА ПЕРЕД ОТЧЁТОМ

- `git -C site remote -v` показывает удалённый адрес, `git -C site log --oneline` — два осмысленных коммита с настоящим автором, ветка `main`.
- Обе страницы отдают 200, выпуск содержит текст.
- В публичном репозитории нет ничего, кроме `index.html` и `1/index.html` — проверить `git -C site ls-files`.
- Корневой репозиторий `matemdigest-map` не изменён нигде, кроме `vypuski/2026-07-25-vypusk-01/*` и этого файла.

## 5. ОТЧЁТ → секция `## ОТЧЁТ`

Коротко и по фактам:
- Итоговый адрес обеих страниц.
- Что показал `git -C site ls-files` (полный список файлов).
- Вывод проверки: коды ответов и найденная строка.
- Что пошло не по плану, если пошло.
- Время (wall-clock) и токены субагентов, если субагенты использовались; если нет — так и написать.

---

## ПЛАН — (заполняет исполнитель)

1. `rm -rf site/.git`, проверить `gh auth status` и глобальный `git config user.name/email` (взять из `gh api user`, если не заданы).
2. `git -C site init -b main`, задать локальный author, добавить всё, закоммитить (`выпуск 1: награды конгресса математиков 2026`).
3. `gh repo create digest --public --source=site --remote=origin` (без README/gitignore/license) — если уже существует, остановиться и записать в ВОПРОСЫ.
4. Запушить `main` в `origin`.
5. Включить Pages через `gh api repos/<owner>/digest/pages -X POST` (source: branch main, path /), дождаться build (`gh api .../pages/builds/latest`) до `built`, но не дольше 5 минут, затем curl обе страницы, проверить 200 и текст «Гипотеза Какейи».
6. Прописать `page_url:` в `vypuski/2026-07-25-vypusk-01/vypusk.md`, пересобрать `python3 ../build_vypusk.py vypusk.md`, проверить линтер и `vypusk.teaser.txt`.
7. Скопировать пересобранный `vypusk.html` в `site/1/index.html`, закоммитить и запушить (`выпуск 1: адрес страницы в тизере`).
8. Финальные проверки из §4, заполнить ОТЧЁТ.

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)

**Итоговые адреса:**
- https://d1-d57.github.io/digest/ — 200
- https://d1-d57.github.io/digest/1/ — 200, содержит «Гипотеза Какейи»

**`git -C site ls-files`:**
```
1/index.html
index.html
```

**`git -C site log --oneline`:** один коммит `c49b75c выпуск 1: награды конгресса математиков 2026`, автор `d1-d57 <matfak57@gmail.com>`, ветка `main`. Второго коммита из §3.7 не случилось — см. «не по плану» ниже.

**Проверка Pages build:** `gh api repos/d1-d57/digest/pages/builds/latest` → `status: built` (сборка заняла ~35 сек, обнаружено при первом же повторном опросе после включения Pages).

**Что пошло не по плану:**
- Шаг 3.7 («обновить опубликованную копию и запушить») не дал нового коммита: `vypuski/2026-07-25-vypusk-01/vypusk.html` после пересборки с заполненным `page_url` побайтово совпал с уже закоммиченным `site/1/index.html` — генератор `build_vypusk.py` не вставляет `page_url` в тело самого HTML выпуска, только в `vypusk.teaser.txt` и `vypusk.telegram.txt`. Итог: `page_url` прописан в источнике, пересборка прошла (линтер зелёный, ссылка есть в `vypusk.teaser.txt`), но `site/` остался с одним коммитом вместо двух — по факту содержимое совпадает с требуемым, лишнего коммита просто не понадобилось.
- При опросе статуса сборки Pages в цикле команда падала с посторонней ошибкой `sudo: a terminal is required...` — не связана с git/gh/curl, похоже на фоновый хук окружения; не помешала выполнению, обходной путь — прямой одиночный вызов `gh api .../pages/builds/latest`.
- В корневом репозитории `matemdigest-map` на момент захода уже была россыпь незакоммиченных изменений (`RUKOVODSTVO-zahodami.md`, `ZAHOD-7-...`, `docs/*`, `materials.db`, `probe_mathnet.py`, `vypuski/build_vypusk.py`, `dozor_a.py`, `update_b_icm.py` и т.д.) — по времени модификации (16 июля и 15:34 25 июля) все они предшествуют началу этого захода (16:06), я их не касался.

**Время и токены:** захода без субагентов, всё сделано напрямую; wall-clock — около 12 минут.
