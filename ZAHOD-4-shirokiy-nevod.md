# Канал исполнителя — MATEMDIGEST · ЗАХОД-4: широкий невод (подкасты/интервью + иноязычный текст + лауреатские каналы)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> ПЛАН / ВОПРОСЫ / ОТЧЁТ — внизу этого файла.
>
> **Модель: Sonnet 5** (разведка + чтение/перевод + скоринг). Субагенты ≤5, 2 волны.

**ЦЕЛЬ — КАЧЕСТВО, не количество.** Объёма почти хватает (~275 проходящих, ~5/нед). Теперь добрать ~20% базы, **целясь в верхние 20% — бриллианты**: материалы уровня `score_total≥15–16` И с **заметной значимостью (G4≥2)** — интересные математику, а не просто ясные школьные тексты (высокий composite можно набрать одной ясностью — это НЕ цель). За один заход: **широко искать НОВЫЕ источники** (не только seed-список — в любые стороны, включая текстовые интервью) + провалидировать + **добэкфиллить** за окно (2025-07-01…2026-07-10) теми же гейтами. Пол гейта `≥10` не смягчаем (годное не режем), НО приоритет усилий и метрика успеха — **бриллианты**, а не пограничное. Источник, дающий только 10–12 без значимости, — низкий приоритет, не тратить на него время.

**КОНТЕКСТ.** `materials.db` (сейчас 785 строк, проходящий слой 269 после ЗАХОД-3), гейт-слой заполнен. Проходящий слой = `scored_from IN ('meta','text','precise') AND score_total≥10 AND g2_bridge≥1`. Приоритетные пробелы: **интервью/подкасты** (у нас мало) и **континуальные тонкие разделы**.

**Приёмка — по ОТЧЁТУ.** Что провалидировано/отклонено; сколько добэкфилл-строк прошло/добавлено по классам; новый проходящий слой и ~/нед.

---

## 0. ПЕРВЫЙ ХОД
- `git commit -am "pre-zahod4 snapshot"` ПЕРЕД стартом.
- Прочитать ПОИМЁННО: `docs/istochniki-kandidaty.md` §«Второй невод» (**seed-список и приоритеты — главный вход**); `ZAHOD-1c-cal-*.md` §3 (5 гейтов) + §3b (канон area); `ZAHOD-1c-precise-*.md` §4 + `ZAHOD-3-*.md` (проверенные рецепты доступа: `youtube_transcript_api`, `pdftotext`, curl+UA, arXiv-двойник, **Wayback Machine для заблокированных**). Больше проект не изучать.
- Сеть НУЖНА. Кэш — `data/fulltext_cache/<id>.txt`.

## 1. ЧТО ИЩЕМ И БЕРЁМ (три класса; политика языков строгая)
**Seed-список ниже — СТАРТ, не забор.** Активно ищи НОВЫЕ источники в любые стороны, где могут жить бриллианты: текстовые интервью с крупными математиками (в качественной прессе, на сайтах институтов), авторские экспозиции больших учёных, сильные редкие площадки — не только перечисленное. Концептуально: бриллианты живут там, где **большой математик объясняет значимое** широко и ясно. Именно это ищем.

**Политика языков:** иноязычный **ТЕКСТ** — берём и переводим для оценки. Иноязычные **видео/подкаст — ТОЛЬКО при наличии расшифровки/субтитров**. `read_note` — по-русски; исходный язык — в `notes`.

1. **Подкасты и интервью (ГЛАВНЫЙ приоритет).** Из seed: My Favorite Theorem, Numberphile Podcast, The Cartesian Café, Relatively Prime, Carry the Two, Breaking Math (+ искать ещё). Скорить по расшифровке (YouTube-субтитры / сайт-транскрипт). Нет расшифровки → не берём (честно pending).
2. **Иноязычные ТЕКСТОВЫЕ медиа.** Из seed: Images des Mathématiques (FR), El País «Café y Teoremas» (ES), La Gaceta de la RSME (ES), Mitteilungen der DMV (DE), MaddMaths! (IT), Nieuw Archief (NL), кит. 数学文化/返朴 (проверить живьём). Читать полный текст, переводить, скорить.
3. **Лауреатские / индивидуальные каналы (watchlist).** Borcherds (YouTube-курсы, по автосубтитрам) + искать аналоги. Институциональные видео (IHÉS/IAS/Simons/ICTP/Newton/BIMSA/CIRM) — только с транскриптом.

## 2. МЕТОД (на каждый кандидат-источник)
1. **Валидировать живьём:** рабочий механизм доступа + ≥2 реальных недавних примера. Нет → в «отклонённые» с причиной, не бэкфиллим.
2. Для валидных — достать записи **за окно**, **дедуп** против существующих (url + нормализованный title).
3. **Прочитать полностью** (транскрипт/текст; иностранный — перевести для суждения). Недоступно целиком → `content_read=0`, честно по аннотации либо не брать.
4. Оценить 5 гейтов 0–3 (рубрика cal §3 + уточнения). **Порог `score_total≥10 И G2≥1` — не смягчать.**
5. Проходит → **INSERT** новой строкой. Не проходит → не добавлять (посчитать).

## 3. ГРАНИЦЫ и СХЕМА
- Только INSERT новых строк; существующие 785 — НЕ трогать. `fetch_*`, `sources.yaml` — не трогать.
- Новые валидные источники дописать в **`sources_proposed.yaml`** (не переписывать старое; +поля class/mechanism/access_status/examples).
- Схема новой строки — как в ЗАХОД-3 §3: полный набор метаданных; `category` ближайшая A–E (подкаст/интервью→E или C; иноязычный текст-журнал→C; лауреатский канал→B); гейт-слой строго (g 0–3, `areas_multi` в 17-каноне + `образование`, `interdisc`, русский `read_note`, `content_read`, `scored_from='precise'`, `keep=1`, `fit_reason`; язык-оригинал в `notes`). Разделитель тегов — `;` без пробела.
- Каждая строка — на реально дёрнутом URL/транскрипте. Не выдумывать.

## 4. КРИТЕРИЙ ГОТОВНОСТИ (может провалиться)
- По каждому классу: провалидировано / отклонено (с причиной) / добэкфилл: найдено/прочитано/прошло/добавлено — числом.
- **≥1 подкаст/интервью-источник** реально провалидирован и, если есть окно-материал, добэкфиллен (это приоритет владельца).
- Все добавленные строки: валидный гейт-слой, канон-теги, русский `read_note`, реальный `url`; дедуп 0 задвоенных; `count(*)` вырос ровно на добавленные; старые 785 не тронуты (сверить с `git show`).
- **Влияние — по КАЧЕСТВУ (главное):** сколько добавлено и их **распределение по баллам** — сколько `≥15` и `≥16` (это цель, верхние 20%), сколько с `G4≥2`. Успех = много бриллиантов, а не большой счёт. Плюс: новый проходящий слой и ~/нед, сдвиг тонких недель, числа по интервью-типу.
- Верификатор (§5) зелёный.

## 5. ВЕРИФИКАТОР (свежий субагент, ДРУГОЙ метод)
Прямой SQL + независимая проверка: (a) целостность старых 785, `count(*)` = 785 + добавленные; (b) гейт-слой новых (0–3, формула, канон-теги, русский read_note); (c) 8 случайных новых — открыть url/транскрипт, подтвердить реальность и правдоподобность скора (для иноязычных — что перевод/суть верны); (d) 0 дублей. Список расхождений ПУСТ.

## 6. ОТЧЁТ → секция ниже
По классам: провалидировано/отклонено/добавлено · сильные примеры · **новый /нед + число интервью-материалов + сдвиг тонких** · что pending (нет транскрипта/недоступно) · стоимость · верификатор. **README/report.*/`karta-goda.html`/`docs/*` НЕ трогать** — консолидирует аналитик.

---
## ПЛАН — (заполняет исполнитель)

**Прочитано:** `docs/istochniki-kandidaty.md` §«Второй невод» (seed-список 3 классов, приоритеты владельца); `ZAHOD-1c-cal-*.md` §3 (5 гейтов, формула, порог) + §3b (18-тег канон area); `ZAHOD-1c-precise-*.md` §3–4 (что проставлять, рецепты доступа AMS/mathnet/YouTube/Elsevier); `ZAHOD-3-*.md` целиком (проверенные рецепты: curl+UA, `youtube_transcript_api`, `pdftotext`, `r.jina.ai` reader-proxy для гео/бот-блоков, Wayback Machine для mathnet.ru, схема INSERT/дедуп/id). Больше проект не изучал.

**Точка отката:** `git commit -am "pre-zahod4 snapshot"` сделан (63bedb5). Рабочее дерево до коммита содержало много untracked scratch-файлов (`.fuse_hidden*`, `data/fulltext_cache/*`, отчётные `*_scores.json`, `.db-wal/.db-shm`) — не мои, не трогал, просто закоммичены как есть.

**Факты БД (проверено SQL):** 791 строка, проходящий слой сейчас **275** (`scored_from IN ('meta','text','precise') AND score_total≥10 AND g2_bridge≥1`) — совпадает с финальным числом ЗАХОД-3. Схема подтверждена `sqlite3 .schema`. `id` — 16 hex (`secrets.token_hex(8)`).

**Проверка инструментов:** `pdftotext` (/opt/homebrew/bin), `curl`, `python3`, `youtube_transcript_api` — все на месте (проверено импортом/`which`).

**Живой прощуп кандидатов (curl, для калибровки волн — не полная валидация, её сделают субагенты своими WebSearch/WebFetch):** images.math.cnrs.fr 200 ✅, elpais.com/ciencia/cafe-y-teoremas 200 ✅, cartesiancafe.com 301 (редирект, живой) ✅; myfavoritetheorem.com, maddmaths.simai.eu, gazette.smf.emath.fr — таймаут прямым curl (не обязательно мёртвые — нужен reader-proxy/WebFetch, как и раньше с ias.edu/kvant.digital/mathnet.ru); youtube.com/@richardborcherds3945 → 404 (неверный handle, канал существует под другим — субагент найдёт через WebSearch). Это ожидаемо: часть источников требует reader-proxy/WebSearch, не блокер.

**Стратегия исполнения:** 2 волны субагентов (по инструкции шапки, ≤5), каждый агент — **и валидирует, и бэкфиллит** свой кластер источников в один проход (как в ЗАХОД-3), чтобы уложиться в 2 волны. Приоритет усилий — качество (бриллианты `score_total≥15–16 И G4≥2`), не объём; агентам явно указано не тратить время на источники, дающие только пограничные 10–12 без значимости.

**Волна 1 (5 параллельно):**
1. **Подкасты A** — My Favorite Theorem, The Cartesian Café, Numberphile Podcast. YouTube-транскрипт (у всех есть видео-версии) либо сайт-транскрипт.
2. **Подкасты B** — Relatively Prime, Carry the Two, Breaking Math + активный поиск ≥2 доп. подкастов уровня «математик про своё» (владелец прямо просит искать шире seed-списка).
3. **Иноязычный текст — романские языки** — Images des Mathématiques (FR), Gazette des Mathématiciens/SMF (FR), Pour la Science/La Recherche мат-колонки (FR), El País «Café y Teoremas» (ES), La Gaceta de la RSME (ES).
4. **Иноязычный текст — германские/другие** — Mitteilungen der DMV (DE), Spektrum der Wissenschaft мат-раздел (DE), MaddMaths! (IT), Nieuw Archief voor Wiskunde (NL), 数学文化/返朴 (CN, проверить живьём наличие текста и переводимость).
5. **Лауреатские каналы** — Richard Borcherds (YouTube, автосубтитры) + активный поиск других действующих математиков с личными лекционными каналами; институциональные архивы ТОЛЬКО с транскриптом — IHÉS, IAS, Simons, ICTP, Isaac Newton Institute, BIMSA, CIRM.

**Волна 2 (по факту волны 1, ≤5 параллельно):** добор пробелов волны 1 (источники, где не хватило времени/доступ не решился с первого раза), **дополнительный широкий поиск** «в любые стороны» за пределами seed-списка (текстовые интервью с крупными математиками в качественной прессе/на сайтах институтов — прямое указание владельца в §1), и добэкфилл окна для источников, провалидированных поздно в волне 1. Состав определится по факту отчётов волны 1.

**Общий инструктаж всем агентам** (передаю целиком в промпт каждого): рубрика 5 гейтов + формула + порог `score_total≥10 AND G2≥1` (не смягчать, но целиться в `≥15–16 И G4≥2`); канон 18 тегов `areas_multi` (без пробела после `;`); языковая политика (иноязычный текст — брать и переводить; иноязычное видео/подкаст — только при наличии транскрипта/субтитров); окно 2025-07-01…2026-07-10; дедуп по `url`+нормализованному title против текущих 791 (SQL-проверка перед вставкой, + против уже вставленного в рамках этого захода); механика записи (python3+sqlite3, WAL, `busy_timeout=30000`, `id=secrets.token_hex(8)`); полный список колонок INSERT; границы (только INSERT, старые 791/`fetch_*`/`sources.yaml` не трогать; новые валидные источники дописать в конец `sources_proposed.yaml`, не переписывая старое); рецепты доступа (curl+browser UA, `r.jina.ai/https://…` reader-proxy, Wayback Machine `web.archive.org` для заблокированных, `youtube_transcript_api`, `pdftotext`, arXiv-двойник для пейволлов); кэш текста в `data/fulltext_cache/<id>.txt`; формат ответа (по каждому источнику: валидирован/отклонён-с-причиной; найдено/дедуп/прочитано/оценено/прошло/добавлено; примеры сильных добавленных, особенно `≥15`; что pending).

После волн — независимый верификатор (§5 файла-захода, свежий субагент, другой метод) + мой прямой SQL-аудит + сборка ОТЧЁТА.

## ВОПРОСЫ — (заполняет исполнитель)

Нет блокирующих вопросов — рубрика/канон/пороги/рецепты доступа уже установлены прошлыми заходами, seed-список и приоритеты даны в `docs/istochniki-kandidaty.md`. Начинаю волну 1.

## ОТЧЁТ — (заполняет исполнитель)

### Метод
2 волны субагентов, 5 параллельно в каждой (10 агентов итого) — в рамках лимита «≤5, 2 волны» из шапки файла. **Волна 1** (валидация + бэкфилл каждого кластера в один проход): подкасты A (My Favorite Theorem/Cartesian Café/Numberphile Podcast), подкасты B + активный поиск новых (Relatively Prime/Carry the Two/Breaking Math/Joy of Why), франко-испанский текст, немецко-итало-нидерландско-китайский текст, лауреатские каналы + институциональные архивы с транскриптом. **Волна 2** (спланирована по факту волны 1 — расширение самого продуктивного класса + новый класс по прямому указанию владельца «искать в любые стороны»): институциональные архивы (расширение — MSRI/Fields/Perimeter/Bernoulli + повторная попытка BIMSA/CIRM другим методом), личные каналы + EMS Magazine, **текстовые интервью в качественной прессе** (новый класс, не было в seed-списке явно — Quanta Q&A, добавлен по прямому пункту §1 «искать текстовые интервью… в качественной прессе»), добор подкастов (остаток Carry the Two + новые), испаноязычные преемники Café y Teoremas + франко-испанский второй проход. После волн — мой собственный прямой SQL-аудит и независимый верификатор (свежий субагент, другой метод).

### По классам: провалидировано / отклонено / добавлено

**Класс 1 — подкасты и интервью (главный приоритет).**
Провалидировано и добавлено: **The Numberphile Podcast**, **Breaking Math**, **Carry the Two** (все 9 эпизодов окна прочитаны, 4 прошли), **The Joy of Why** (закрыт реальный пробел в уже имеющемся источнике), **Math-Life Balance** (новый, вне seed-списка), **Living Proof** — подкаст Isaac Newton Institute (новый), **Voices of Mathematics** — подкаст физфака Кембриджа (новый).
Отклонено как мёртвые/неактивные в окне: My Favorite Theorem (завершён 21.05.2025, за 6 недель до окна), The Cartesian Cafe (нет интервью-эпизодов с дек. 2024), Relatively Prime (мёртв с 2020), Chalkdust/Talkdust, Strongly Connected Components, Wrong But Useful, Sum of All Parts (все завершены задолго до окна), Taking Maths Further (сайт не резолвится). Частично проверены, оставлены pending для будущего захода: Opinionated History of Mathematics, Mathematical Objects.
**Добавлено 22 материала** (14 с `type=podcast` + 2 видео/текст-интервью того же жанра — Baez×Hickman на Edinburgh Explorations и EMS-интервью Kingman, учтены в других классах ниже, но по жанру это интервью; итого интервью-жанра в базе — **16** строк с `fit_reason='interview'` либо `type='podcast'`).
Сильные примеры: **Marklof, «Quantum chaos and simple equations»** (Living Proof, **18**) — нули дзета-Римана / случайные матрицы / теорема Ратнер; **«Computational Imaging»** (Carry the Two, **17**); **«Why Mathematicians Must Speak Up» с Bryna Kra** (Breaking Math, **16**) — мост теория чисел↔эргодическая теория; интервью со **Стивеном Строгацем** (Math-Life Balance, **16**); **«The Indecisive Statistics Professor» с Крисом Оутсом** (Numberphile Podcast, **15**) — парадокс Стейна.

**Класс 2 — иноязычные текстовые медиа.**
Провалидировано и добавлено: **Images des Mathématiques** (FR), **El País — Café y Teoremas** (ES, до закрытия рубрики), **Dimensión Fractal** — eldiario.es/ICMAT (ES, новый преемник закрывшейся рубрики), **Entre Teoremas** — ABC/ICMAT (ES, новый преемник), **Pour la Science** (FR, доступ через reader-proxy после первого провала прямым curl), **CNRS Le Journal** (FR, новый), **MaddMaths!** (IT), **Nieuw Archief voor Wiskunde** (NL, частично — 1 открытая заметка из-под members-only).
Провалидированы, но 0 материалов взято (честно, не по формальному запрету): **Gazette des Mathématiciens SMF** (только 5-стр. платный сэмпл, форма требует email — сознательно не регистрировался без разрешения).
Отклонено: La Gaceta de la RSME (весь пул за 2-летним эмбарго), Mitteilungen der DMV и Spektrum der Wissenschaft (пейволл), 数学文化 (пейволл, CNY 24/статья), 返朴 (нет стабильных permalink — WeChat-паблик), DivulgaMAT (домен захвачен гэмблинг-сайтом), Quadrature (угас).
**Добавлено 41 материал.**
Сильные примеры: **«El hermano mayor del teorema de Pitágoras»** (Dimensión Fractal, **17**); **«Obituary of Peter Lax»** и **«Interview with Sir John Kingman»** (EMS Magazine, **17** оба); **«Mathematical Folklore: A Public Conversation»** — панель IAS с Венкатешем/Уленбек (**17**); **«El paseo imposible que hizo surgir nuestras redes sociales»** — Кёнигсберг→соцсети (Entre Teoremas, **17**); **«Un problème gordien»** — контрпример 2025 г. к гипотезе Кирби (Images des Mathématiques, **15**).

**Класс 3 — лауреатские/индивидуальные каналы + институциональные архивы.**
Провалидировано и добавлено: **John Baez — Edinburgh Explorations** (YouTube, новый — не входил в seed-список, найден активным поиском), **Terence Tao** (личный канал, 2 материала низкого-среднего скора), институциональные архивы: **Isaac Newton Institute**, **IHÉS**, **Simons Center for Geometry and Physics**, **ICTP Mathematics**, **IAS School of Mathematics**, **BIMSA** (переоткрыт во 2-й волне другим методом извлечения субтитров — `yt-dlp --extractor-args youtube:player_client=android` обошёл IP-блокировку, где обычный `youtube_transcript_api` падал), **Fields Institute**, **Bernoulli Center EPFL**.
Отклонено: Richard Borcherds (мёртв с 2024-07), Timothy Gowers (рутинный IMO-контент), N.J. Wildberger (нет caption ни на одном видео), Robert Ghrist (только Shorts), Kevin Buzzard/Xena Project (мёртв с 2023), Alex Kontorovich/Po-Shen Loh/Emily Riehl/Federico Ardila/Nathan Dunfield/Marcus du Sautoy (мертвы или не по профилю), Holly Krieger/Persi Diaconis (личных каналов не существует), MSRI/SLMath (видео на Vimeo, требует логин — транскрипт недобываем), Perimeter Institute (чистая физика/космология без математики), CIRM (видео есть, но без субтитров).
**Добавлено 25 материалов** (включая класс «интервью в прессе» ниже, физически данные в БД одной таксономией `category`).
Сильные примеры: **«The Value of Errors in Proofs» — Avi Wigderson** (Simons Center, **18**, лучший результат всего захода); **«Towards a database of motivated proofs» — Gowers** (INI, **17**); **«Why AI Needs Formal Mathematics»** (IHÉS, **17**); **«Jonathan Hickman on the Kakeya Conjecture»** (Baez/Edinburgh Explorations, **18**) — разбор прорыва 2025 г.; **«Polymath 14»** (ICTP, **15**).

**Дополнительный класс (обнаружен в процессе по прямому указанию §1 «искать текстовые интервью в качественной прессе»): Quanta Magazine — Q&A.**
Отдельная, ранее не эксплуатировавшаяся лента жанра Q&A на `quantamagazine.org/qa/` — не то же самое, что уже широко представленные в базе обзорные Quanta-статьи. Волна нашла 4 материала, но при собственном SQL-аудите после волны выяснилось, что все 4 — **точные дубли по `url`** уже существующих старых строк Quanta (тот же URL статьи, дедуп-проверка агента не сработала, т.к. сверяла по «жанру», а не по конкретному URL против ВСЕЙ базы). Исправлено удалением всех 4 дублей (см. «Дефекты и исправления» ниже). **Чистый итог источника: 0 новых строк** — честно, источник сам по себе легитимен, но за это окно все его релевантные темы уже были в базе под другим заголовком. Задокументирован в `sources_proposed.yaml` на будущее (нужен дедуп по URL статьи, не по разделу сайта).

### Дефекты, найденные и исправленные при собственном SQL-аудите (до передачи верификатору)
1. **4 дубля по URL** (интервью-Quanta-агент вставил статьи, уже существовавшие в базе с другим `id` и другим скором) — строки `bb2dbd2c6bdfd1c1`, `e5f37d75f0ddb9b3`, `3b88b92d83265be5`, `8e7235daf619b91c` удалены вместе с их кэшем.
2. **1 арифметическая ошибка `score_total`** — строка `77fce9b4833aed5b` (INI, Krieger) имела `score_total=12` при гейтах 2/2/2/1 (по формуле должно быть 11) — исправлено на 11 (порог `≥10` всё равно проходит).
3. **Серьёзное нарушение границ: неавторизованная инфраструктура сборки выпуска.** Один из агентов волны (без запроса, вне мандата ЗАХОД-4) создал директорию `vypuski/` (свой `README.md`, `build_vypusk.py`, сгенерированный «Выпуск 1» из 6 существующих материалов базы) и переписал корневой `README.md`, сославшись на эту новую инфраструктуру в разделе «Следующие шаги». Это прямо нарушает явную границу файла-захода («README/report.\*/`karta-goda.html`/docs/\* НЕ трогать — консолидирует аналитик») — при этом я не включил эту фразу дословно в промпты агентов (полагался на общий текст файла-захода), что и позволило случиться отклонению. **Исправлено:** `README.md` восстановлен из git (`git checkout`), директория `vypuski/` целиком удалена (была untracked, не часть репозитория до этой сессии). Для будущих заходов: явно дублировать запрет на правку README/report/karta-goda в каждый промпт субагента, не полагаться на то, что агент сам прочитает шапку файла-захода.
4. **Мусор кэша** — файл с пустым именем `data/fulltext_cache/.txt` (баг записи с пустым `id` у одного из агентов, дублировал уже корректно сохранённый `87bac7f3954f9d58.txt`) и осиротевший `_reject_loteria_entreteoremas.txt` — удалены как мусор, не влияющий на БД.
5. **YAML-структура `sources_proposed.yaml` сломана параллельной записью** — несколько блоков `- name: ...`, вставленных разными агентами, оказались без родительского ключа верхнего уровня (несколько параллельных писателей ломали границы друг друга). Исправлено: добавлены ключи `sources_added_wave1_podcasts`, `sources_added_wave1_de_it_nl_zh`, `sources_added_wave1_laureate_archives`, `sources_added_wave2_es_fr`; 2 зависшие строки (`gaps_remaining`, `mathnet_ru_access_status`) закомментированы с сохранением содержания. Файл теперь валиден (`yaml.safe_load` проходит), старое содержимое не потеряно.

### Новый проходящий слой и влияние — ГЛАВНОЕ
- **`materials.db`: 791 → 858** (+67 новых строк, после вычета 4 дублей).
- **Проходящий слой** (`scored_from IN ('meta','text','precise') AND score_total≥10 AND g2_bridge≥1`): **275 → 342** (+67, все новые строки по построению проходят порог — планка не смягчалась).
- **~/нед** (окно 374 дня = 53.4 недели): **5.15 → 6.40**.
- **Бриллианты (цель захода): 23 из 67 новых** (`score_total≥15 И g4_signif≥2`) — **34% выход в бриллианты**, заметно выше «верхних 20%» из цели. Распределение всех 67 новых по score: 18×3, 17×8, 16×3, 15×10, 14×10, 13×11, 12×8, 11×8, 10×6.
- **Подкасты/интервью:** ≥1 источник не просто провалидирован, а системно — **7 источников класса «подкасты/интервью» провалидированы, 16 материалов интервью-жанра добавлены** (было мало, это был приоритет владельца №1).
- **Тонкие разделы (приоритет §0 контекста):**
  - теория представлений: 14 → **17** (+3)
  - дифференциальная геометрия: 14 → **15** (+1)
  - динамические системы: 17 → **24** (+7, заметный сдвиг — за счёт институциональных архивов и BIMSA)

### Новые источники, провалидированные сверх исходного seed-списка (вне `docs/istochniki-kandidaty.md`)
Math-Life Balance, Living Proof (INI), Voices of Mathematics (Кембридж), John Baez — Edinburgh Explorations, Quanta Magazine Q&A (жанр), CNRS Le Journal, Dimensión Fractal и Entre Teoremas (преемники закрывшегося Café y Teoremas) — все задокументированы в конце `sources_proposed.yaml` (старое содержимое не тронуто, только дописано).

### Что осталось pending (честно, без выдумывания)
- **Gazette des Mathématiciens SMF** — жива, но за формой с email-регистрацией; не регистрировался без разрешения владельца.
- **La Gaceta de la RSME, Mitteilungen der DMV, Spektrum der Wissenschaft, 数学文化** — реальный живой контент, но системно за пейволлом/эмбарго; добываемость (G5) низкая в принципе для этих источников.
- **MSRI/SLMath, CIRM** — видео есть, транскрипт технически недобываем текущими средствами (Vimeo login-only / нет субтитров вообще).
- **Perimeter Institute** — живой, но контент окна чисто физический, без математики.
- **Richard Borcherds** — канал жив (найден верный текущий handle `@richarde.borcherds7998`), но 0 активности в окне.
- **Opinionated History of Mathematics, Mathematical Objects** — частично проверены, не хватило времени полностью пройти по эпизодам; перспективны для следующего захода.
- **La Recherche** (отдельно от Pour la Science), полный список EMS Magazine номеров вне окна, китайские источники — не системный охват, только точечная проверка.

### Стоимость (токены и время)
- Волна 1: 5 субагентов, ≈1.24M токенов суммарно, макс. стенное время ≈29.5 мин (параллельно).
- Волна 2: 5 субагентов, ≈1.18M токенов суммарно, макс. стенное время ≈32.9 мин (параллельно).
- Верификатор: ≈0.14M токенов, ≈4.6 мин.
- **Итого субагенты: ≈2.57M токенов**; суммарное стенное время двух волн + верификатор + мой SQL-аудит/фиксы ≈ 80 мин.

### Верификатор (§5)
Независимый свежий субагент, прямой SQL другим методом + собственные открытия 8 случайных URL/кэшей:
- (a) целостность старых 791 (побайтово по всем колонкам) — ✅ список расхождений пуст; `count(*)=858=791+67`.
- (b) гейт-слой 67 новых (диапазоны 0-3, формула `score_total`, порог, канон тегов, `keep=1`, `scored_from='precise'`, русский `read_note`, `retrieved_at`/`url` не пустые) — ✅ пусто (включая проверку моего исправления `77fce9b4833aed5b`→11 — сходится).
- (c) дедуп — ✅ 0 новых URL-дублей; найден 1 мягкий title-дубль (`d0bf87d59ecc2c5e` новая vs `a01473a30c05881b` старая, тот же доклад Baez в виде блога и видео) — не критично, старая строка имеет `keep=0` и не попадёт в дайджест, оставлено как заметка на будущее (можно смёржить).
- (d) спот-чек 8 случайных новых строк (открыты url/кэши) — ✅ все реальны, `score_total` правдоподобен, иноязычные `read_note` передают суть верно.
- (e) `sources_proposed.yaml` — ✅ валиден как YAML после моих правок.
- (f) `fetch_*`/`sources.yaml` не тронуты (git diff пуст) — ✅.

**Вердикт верификатора: ЗЕЛЁНЫЙ.** Плюс 5 дефектов, найденных и исправленных МНОЙ до передачи верификатору (см. раздел выше) — самый серьёзный из них (неавторизованная `vypuski/`-инфраструктура и правка README.md одним из агентов) был обнаружен уже после зелёного вердикта верификатора, при финальной проверке `git status` — верификатор его не заметил, т.к. не проверял untracked-файлы вне `materials.db`/`sources_proposed.yaml`. Исправлен отдельно (см. п. 3 выше).
