# CLAUDE.md — вход для Claude Code в `matemdigest-map`

**`matemdigest-map` — карта жизнеспособности для еженедельной рассылки математических
материалов** («Quanta для математиков»): база кандидатов за скользящее окно (`materials.db`),
слои курирования, визуальная карта (`karta-goda.html`). Это НЕ бот и НЕ рассылка — только карта и
слой отбора; сборка рассыльщика — отдельный этап. Заходы — `ZAHOD-*.md` в корне, канон чтения —
`RUKOVODSTVO-zahodami.md`.

**Git — только через `../disciplina/_generator/tools/git_zona.py`** (у `matemdigest-map` нет
своей копии инструмента — он резолвит текущий репозиторий по `cwd`, а не по своему расположению,
так что работает и здесь), никакого голого `git` руками и никакого shell владельцу в чат.

Собираешься тронуть файлы или закоммитить — сначала подними `disciplina-git`.

---
*Полный порядок захода — `RUKOVODSTVO-zahodami.md`.*
