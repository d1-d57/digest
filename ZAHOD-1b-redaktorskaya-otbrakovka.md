# Канал исполнителя — MATEMDIGEST · редакторская отбраковка (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
> ПЛАН / ВОПРОСЫ / ОТЧЁТ — в секциях внизу ЭТОГО файла. Метрика — ЧЕСТНОСТЬ редакторского суждения. Часы — норма.
>
> _Эта версия уточняет черновик из `docs/handoff/zahody/` (супер­седит его). Изменения против черновика: `fit_reason` — закрытый словарь (не вольный текст); счёт годных с корректной укладкой грубых дат; объёмные якоря источников; area-fill вынесен из захода (в 1c)._
>
> **Рекомендация модели (в стартовое сообщение владельца):** Sonnet 5 — механика расписана, суждение стоит на детальном editor's bar + аудит-выборка + верификатор ловят промах. Если аудит-выборка покажет плавающий bar на спорных батчах — перезапустить именно их на Opus.

**КОНТЕКСТ.** В `~/Documents/GitHub/matemdigest-map/` лежит `materials.db` — **738 кандидатов** за 2025-07-01…2026-07-10 (окно строится в еженедельную Telegram-рассылку «Quanta для математиков» — курированные мосты-объяснения для широкого слоя математиков). Прошлый заход (ЗАХОД-1) по ошибке сделал только ГИГИЕНУ ДАННЫХ (снял масштхед, поправил заголовки) и не сделал главного. Здесь — только главное.

**ЗАДАЧА — редакторская отбраковка КОНТЕНТА, а не уборка.** Вопрос по каждой строке один: **пошёл бы этот материал в рассылку — мост / экспозиция / событие, легибельное широкому слою математиков (со 2–3 курса до профессионала)?** На выходе — вердикт на КАЖДОЙ из 738 строк + честное число выживших.

> **ЯВНО, чтобы не повторить промах:** чистота данных (дубли, масштхед, NULL) — **НЕ задача**. «БД чиста по гигиене» — это **НЕ ответ**. Задача — суждение о ПРИГОДНОСТИ КОНТЕНТА, и оно должно стоять на каждой из 738 строк.

> **ЗАЧЕМ закрытый словарь причин (forward-hook).** Следующий заход (1c) выведет из твоей разметки durable-правила-гейты (allow/deny по source/type/area + пороги). Это возможно, только если `fit_reason` — из ФИКСИРОВАННОГО набора тегов ниже, а не вольный текст. Один тег на строку; нюанс — в `notes`. Разнобой в причинах = 1c не соберётся.

**Приёмка — по ОТЧЁТУ.** **Флаг-стоп:** получишь оценённую базу + честный счёт + аудит-выборку. **Рассыльщик и правила-гейты НЕ строить** — это 1c и позже. Останавливаешься на разметке.

**АНТИ-ПОДКРУТКА (оспорь премису владельца — §1):** число годных — это ОТВЕТ, а не цель. Не тяни к 300 и не режь к 150. Editor's bar одинаков для всех строк; число выпадает само.

---

## 0. ПЕРВЫЙ ХОД
- Рабочая папка `~/Documents/GitHub/matemdigest-map/`. Точка отката: `git commit -am "pre-cull snapshot"` ПЕРЕД стартом (текущий HEAD = `1786496`, коммит ЗАХОД-1).
- Прочитать ПОИМЁННО (и только это): `materials.db` (все 738 строк: `title, authors, date, source_name, category, type, area, summary, breadth_score, significance_score, tags, notes`), `README.md` (§«Рубрика скоринга» — прообраз bar), `sources.yaml`.
- Сначала — **## ПЛАН**, потом действия.

## 1. ДИСЦИПЛИНА (Карпатов)
1. **Думать до правки.** Предпосылки/развилки — в ПЛАН. Оспорь премису «должно быть ~300»: докладывай фактическое число, каким бы ни вышло.
2. **Минимум.** Пайплайн (`fetch_*`, `build.py`, `score.py`) НЕ трогать — добавляешь слой суждения, не переписываешь скоринг.
3. **Хирургия.** Курирование = НОВЫЕ колонки; старые (`breadth_score`/`significance_score`/`summary`/…) не переписывать. Каждая правка трассируется к заданию.
4. **Цель — проверяемое условие.** См. §3 «КРИТЕРИЙ».
- **СУБАГЕНТЫ (аккуратный режим):** суди батчами, **≤5 параллельно**, батч ~100 строк (7–8 батчей на 738). Дешёвая механика (SQL, сбор) — оркестратору; суждение по bar — батч-субагентам. Рейт-лимит = отступить + доложить, НЕ слепой ретрай. Рой падений без результата = стоп + доклад. Назвать в ПЛАНЕ сколько батчей и на что.

## 2. ГРАНИЦЫ
- **Сырьё не удалять.** Вердикт — НОВЫЕ колонки: `keep` (0/1), `fitness` (0–3), `fit_reason` (один тег из §3). Отбраковка = `keep=0`, НЕ `DELETE`. `count(*)` обязан остаться 738.
- **area=None НЕ трогать в этом заходе.** Доразметка раздела (35% пустых) — работа 1c и только по keep-пулу. Здесь не заполнять, чтобы не смешивать риски.
- Перетегирование категории/типа (см. §3) = правка `category`/`type` со ВСТАВКОЙ старого значения в `notes` (`was: A/expository`), не молча.
- Не выдумывать: сомнение → `fitness` ниже, не выше.
- **51 «дубль по URL»** (ICM/Bourbaki/Takagi/CEB/книги/премии/некрологи на общих посадочных страницах) — законные РАЗНЫЕ материалы. Не схлопывать (это удаление сырья + занижение числа). Судить каждый по существу.

## 3. ЗАДАЧА
Проставить редакторский вердикт на **КАЖДОЙ из 738 строк** (не на подмножестве — считаем ОБЩЕЕ число годных). Судить по editor's bar; батчами через субагентов.

### Editor's bar — «пошло бы в рассылку для широкого слоя математиков?»
**IN (fitness 2–3, `keep=1`):**
- Научпоп высокого класса (Quanta, plus.maths, AMS Feature Column).
- Обзор/экспозиция, **легибельная за границей своего раздела** (Bulletin AMS; фичи и «What is…» в Notices; широкие обзоры Bourbaki/УМН уровня «поймёт не-специалист»).
- Крупные события/премии (Абель, Филдс, Breakthrough, экспозе Bourbaki, ICM — как «состав пленарных», а не 18 строк).
- Книги/биографии/нон-фикшн; юбилеи («учёному 80/100»); интервью (в т.ч. в неспециальных изданиях); крупные некрологи значимых фигур; заметная история математики.
- Заметные, доступные блоговые ЭКСПОЗИЦИИ и разборы прорывов (Рамсей у Кали; «Langlands Program 2.0» у Войта).

**OUT (fitness 0–1, `keep=0`):**
- Личное/нематематическое в блогах (бейсбол/Тейлор Свифт у Элленберга; климат/химия у Баэза).
- **Узкий спец-обзор, нечитаемый вне подполя** (Expositiones «Inverse limits of CM points…»; УМН «Three-dimensional Fano minifolds»; узкие Acta Numerica). **Экспозиция ≠ пригодность.**
- Админка/сервис: «Mathematics People», «Bookshelf», «Letters to the Editor», «AMS Employment Center», масштхед, backlog-заметки.
- Голые анонсы: «seminar starting», «nominations open», рутинные объявления конференций.
- Видео, слишком базовое для продвинутой аудитории (значительная часть Numberphile).

**Шкала `fitness`:** 3 — твёрдое ядро (пойдёт точно) · 2 — годится · 1 — слабое/на грани, по итогу OUT · 0 — явно вне. **Правило keep: `keep=1` ⇔ `fitness ≥ 2`** (зафиксируй в отчёте).

### `fit_reason` — ОДИН тег из закрытого набора (на КАЖДОЙ строке, вкл. дропы)
Причины OUT (`keep=0`): `personal` · `too-narrow` · `admin` · `bare-announcement` · `too-basic` · `dead`.
Причины IN (`keep=1`): `popsci` · `bridge` (экспозиция, легибельная вне раздела) · `blog-bridge` (блоговый разбор прорыва) · `event` (событие/премия/состав ICM/Bourbaki-экспозе) · `people` (юбилей/интервью/некролог/биография) · `book` · `history`.
Ровно один тег; если строка на стыке — бери доминирующую причину, `fitness` ставь на границе (1 или 2), а развилку опиши в `notes`. Новых тегов НЕ вводить (появилась нужда — в ВОПРОСЫ, не молча).

### Перетегирование
Некрологи / юбилеи / интервью, сидящие в журналах (Notices, УМН, Expositiones), → `category=E`, `type` соответствующий, `keep=1`, `fit_reason=people` (это ценная человеческая фактура, не балласт). Старое значение — в `notes`.

### Объёмные якоря (где концентрируется отбраковка — калибровка батчей)
Оценка аналитика по образцам: годного ≈ **270–315** (~5–6/нед в среднем), не 150. Где рубить сильнее всего:
- **Блоги D (278 строк)** → ждём ~55 годных. Тяжёлые: Ellenberg/Quomodocumque **104** (много бейсбола/личного → `personal`), Baez/Azimuth **72** (часть химия/климат → `personal`/`too-narrow`), Kalai **33** и Tao **31** (тут наоборот — часто `blog-bridge`, НЕ зарой их). Woit/Aaronson — разбор прорывов = `blog-bridge`, тех-посты = `too-narrow`.
- **Журнальные спец-обзоры (~113: Expositiones 67 · УМН 17 · EMS Surveys 17 · Acta Numerica 12)** → ждём ~24 годных: экспозиция ≠ легибельность, большинство `too-narrow`.
- **Notices of the AMS (127)** — смесь: фичи/«What is…» = `bridge`/`popsci`; но **24 пустые рубрики-оболочки** (Mathematics People/Bookshelf/Letters, помечены в ЗАХОД-1 как пограничные) = `admin`.
- **Твёрдый костяк (~4/нед сам по себе):** Quanta (73), фичи Notices, события/премии/Bourbaki (B, 43), книги/интервью (E). Это `popsci`/`bridge`/`event`/`people`/`book`.
Якоря — ориентир, НЕ квота. Editor's bar одинаков; если реальность разойдётся с оценкой — докладывай реальность.

### Курированный счёт (с корректной укладкой грубых дат)
Даты в базе неоднородны: **488 строк — полная дата (YYYY-MM-DD), 228 — только год-месяц, 22 — только год.** Поэтому недельный счёт ТОЛЬКО по полным датам ВРЁТ вниз: ~половина значимого ядра (события/премии/книги) несёт грубые даты и выпадает из недельных корзин. Считать так:
1. **Всего годных** (`keep=1`) + разбивка по `category` / `source_name` / `type` / месяцу.
2. **Недельный ритм на keep-пуле:** (a) по строкам с полной датой — ISO-неделя: mean/median/min/max, **сколько недель < 3 годных** (это прямой ответ на вопрос владельца «не меньше 3»); (b) по ВСЕМ годным — помесячно и месяц ÷ 4.33 как грубый недельный эквивалент. Доложить ОБА и назвать, какой недооценивает и почему.
3. **Разнообразие:** распределение `area` по keep-пулу; где `area` есть — сколько недель «моно» (один раздел) vs «≥3 раздела». Долю `area=None` в keep-пуле доложить отдельно (это вход для 1c; здесь не заполнять).

### КРИТЕРИЙ ГОТОВНОСТИ (может провалиться)
- **Все 738** строк имеют `keep` + `fitness` + `fit_reason`; **0 несудённых**; **0 тегов вне закрытого набора**.
- Сырьё цело: `count(*) = 738`, ничего не `DELETE`; `area` в этом заходе не менялась.
- Курированный счёт доложен с полной разбивкой И недельным ритмом (оба способа) **ДАЖЕ если мал/велик**; правило keep зафиксировано.
- `audit_sample.md` выдан: **50 строк, поровну keep/drop**, со всех уровней `fitness` и из РАЗНЫХ `fit_reason`-тегов (чтобы человек проверил суждение по срезам).
- Верификатор (§4) зелёный, список расхождений ПУСТ.

## 4. ВЕРИФИКАТОР (свежий субагент, ДРУГОЙ метод)
Отдельный субагент, прямыми SQL к БД (не по тексту отчёта): (a) `count(*) = 738`; (b) 0 строк без `fitness`; (c) 0 значений `fit_reason` вне закрытого набора §3; (d) курированный счёт прямым SQL == число в отчёте; (e) выборка **20** `keep=1` реально проходит editor's bar (не спец-обзор / не админка / не личное); (f) выборка **15** `keep=0` реально НЕ проходит — **особенно проверь блоги: не зарыт ли Тао/Кали/крупный разбор**. **Список расхождений обязан быть ПУСТ.**

## 5. ОТЧЁТ → секция ## ОТЧЁТ внизу
Как судил (bar, сколько батчей/субагентов, какая модель) / **годной счёт + полная разбивка + недельный ритм обоими способами + разнообразие** / где `audit_sample.md` / что перетегировал / что НЕ трогал / результат верификатора / **время прогона + потраченные токены**.
**Консолидацию в README НЕ делать** (это работа аналитика в Cowork — оставь в ОТЧЁТЕ, не трогай README/report.*). Вывод одним абзацем: держится ли ~5–6/нед и «не меньше 3» на годном слое, где просело сильнее всего.

---
## ПЛАН — (заполняет исполнитель)

**Предпосылки/развилки:**
- Оспариваю премису «~300»: bar одинаков для всех строк, число — что выпадет. Оценка аналитика (270–315) — ориентир для распределения батчей, не цель для подгонки.
- `materials.db` — 738 строк, 5 категорий (A=259, B=43, C=86, D=278, E=72), даты 488 full / 228 year-month / 22 year-only — числа сошлись с якорями в заходе, README §«Рубрика скоринга» и sources.yaml прочитаны как названные якоря.
- Точка отката зафиксирована: коммит `pre-cull snapshot` поверх `1786496`.
- Добавлены НОВЫЕ колонки `keep INTEGER`, `fitness INTEGER`, `fit_reason TEXT` (ALTER TABLE, без переписи старых). `journal_mode=WAL` включён — 5 параллельных субагентов будут писать в одну БД непересекающимися UPDATE по `id`; WAL + `busy_timeout` в каждой транзакции снимает риск `database is locked`.
- `area` в этом заходе не трогается никем из батчей (инструкция явно в промпте каждого субагента).

**8 батчей по source_name (дисциплинированы по смыслу, не только по размеру, чтобы субагент судил в одном режиме):**
1. A · Notices of the AMS (127) — смесь feature/«What is…»(bridge/popsci) и админ-оболочек (admin).
2. A · узкие спец-журналы: Expositiones(67)+Acta Numerica(12)+Japanese J.Math(2)+BLMS(1) = 82 — по умолчанию too-narrow, искать редкие широкие обзоры.
3. A(остаток)+B: УМН(17)+EMS Surveys(17)+Bulletin AMS(9)+Jahresbericht(7) + ICM2026(18)+Bourbaki(15)+Takagi(6)+JMM CEB(4) = 93 — широкие обзоры + события/премии/составы.
4. C: Quanta(73)+AMS Feature Column(10)+plus.maths(3) = 86 — попсай-ядро, ожидаемо высокий keep.
5. D · Ellenberg/Quomodocumque (104) — расчистка личного/бейсбола (`personal`) от `blog-bridge`.
6. D · Baez/Azimuth(72)+Gowers(3) = 75 — расчистка химии/климата (`personal`/`too-narrow`) от математических мостов.
7. D · Kalai(33)+Tao(31)+n-Category Café(15)+Aaronson(10)+Woit(10) = 99 — ЗАЩИЩЁННАЯ зона: не закапывать Тао/Кали, разбор прорывов = `blog-bridge`, тех-посты = `too-narrow`.
8. E: YouTube×6(43)+Joy of Why(6)+MAA Reviews(6)+премии/некрологи/юбилеи/книги(остаток) = 72 — люди/книги/события + видео (часть Numberphile — `too-basic`).

Сумма: 127+82+93+86+104+75+99+72 = 738 ✓.

**Механика:** оркестратор (я) — SQL-выборка на вход и сборка отчёта; суждение по bar — батч-субагентам. 2 волны по ≤5 параллельно (5 + 3), каждый субагент получает: полный текст editor's bar (§3 заход-файла), закрытый словарь `fit_reason`, список своих `id` через прямой SQL-запрос к `materials.db`, и пишет `UPDATE` сам через sqlite3 (одна транзакция, `PRAGMA busy_timeout=60000`) — так не теряется точность на 700+ построчных вердиктах при передаче через текст ответа. Перетегирование (`category`/`type`→E, `notes`: `was: …`) — только для некрологов/юбилеев/интервью, сидящих в журналах (относится к батчам 1, 2, 3).

После обеих волн: SQL-сверка (0 несудённых, 0 тегов вне словаря), курированный счёт с разбивкой и обоими недельными расчётами, `audit_sample.md` (50 строк), затем отдельный субагент-верификатор (§4, свежий контекст, прямой SQL).

## ВОПРОСЫ — (заполняет исполнитель)

## ОТЧЁТ — (заполняет исполнитель)

### Как судил
Editor's bar §3 заход-файла (попал/не попал в еженедельную рассылку-мост для широкого слоя математиков), закрытый словарь `fit_reason` (13 тегов: 6 OUT + 7 IN), правило `keep=1 ⇔ fitness≥2`. Модель — **Sonnet 5** (как рекомендовано в шапке; плавающего bar на аудит-выборке не обнаружено, перезапуск батчей на Opus не потребовался).

**8 батчей, 2 волны по ≤5 параллельно** (карта — в разделе ПЛАН):
- Волна 1 (5 субагентов): Notices AMS (127) · узкие спец-журналы A: Expositiones+Acta Numerica+Jap.JMath+BLMS (82) · широкие A+B: УМН/EMS Surveys/Bulletin AMS/Jahresbericht+ICM/Bourbaki/Takagi/JMM CEB (93) · C-попсай: Quanta/AMS FC/plus.maths (86) · D-Ellenberg (104).
- Волна 2 (3 субагента): D-Baez+Gowers (75) · защищённая зона D-Kalai/Tao/nCatCafe/Aaronson/Woit (99) · E-люди/книги/события/видео (72).
- Каждый субагент читал свой срез напрямую через `sqlite3 SELECT`, судил и сам писал `UPDATE` одной транзакцией (`PRAGMA busy_timeout=60000`, база в `journal_mode=WAL`) — вердикт не проходил через текст ответа, чтобы не терять точность на 700+ построчных решениях.

### Годной счёт

**Всего `keep=1`: 346 / 738 (46.9%).** Оспоренная владельцем премиса «~300» — ближе к верхней границе аналитик-оценки (270–315), но не к ней подогнан: bar был одинаков по всем 8 батчам, число выпало само; выше эталона в основном за счёт C-попсай-ядра (83/86, почти 97%) и защищённой зоны Kalai/Tao (см. ниже).

**По категории** (A/E разошлись с исходным списком source→category из-за перетегирования 22 строк):
| category | всего | keep=1 |
|---|---|---|
| A | 237 (было 259, −22 в E) | 67 |
| B | 43 | 20 |
| C | 86 | 83 |
| D | 278 | 101 |
| E | 94 (было 72, +22 из A) | 75 |

**По источнику** (топ и низ, полная таблица прогонялась верификатором): Quanta 72/73, Notices AMS 58/127, Kalai 27/33 (82%), Tao 23/31 (74%), 3Blue1Brown 15/15, Bourbaki 15/15 — на другом полюсе: Ellenberg 19/104 (18%), EMS Surveys 0/17, Acta Numerica 0/12, Takagi 0/6.

**По `fit_reason`:** IN — popsci 96, bridge 81, blog-bridge 80, people 35, event 20, history 17, book 17. OUT — too-narrow 177, personal 105, bare-announcement 53, admin 46, too-basic 11, dead 0.

**По `type` (keep=1):** blog 101, popsci 74, expository 64, video 27, obituary 17, survey 16, conf_talk 16, prize 11, review 5, interview 5, podcast 4, book 3, anniversary 3.

### Недельный ритм (оба способа)

**(a) По строкам с полной датой (265 из 346 keep-строк), ISO-неделя, окно 2025-07-01…2026-07-10 (54 полных ISO-недели):**
mean 4.91 · median 4.0 · min 0 · max 15 · **недель <3 годных: 9 из 54 (16.7%)**.

**(b) По ВСЕМ 346 годным помесячно ÷ 4.33** (учитывает грубые даты, которые метод (a) теряет):
| месяц | keep=1 | /нед |
|---|---|---|
| 2025-07 | 13 | 3.00 |
| 2025-08 | 19 | 4.39 |
| 2025-09 | 25 | 5.77 |
| 2025-10 | 17 | 3.93 |
| 2025-11 | 28 | 6.47 |
| 2025-12 | 28 | 6.47 |
| 2026-01 | 40 | 9.24 |
| 2026-02 | 26 | 6.00 |
| 2026-03 | 33 | 7.62 |
| 2026-04 | 23 | 5.31 |
| 2026-05 | 27 | 6.24 |
| 2026-06 | 52 | 12.01 |
| 2026-07 | 8 | 1.85 (окно оборвано на 10-м дне месяца — не показатель) |

mean(б) 6.02/нед · min 1.85 (частичный июль) · max 12.01. Плюс 7 keep-строк с датой только-год, вне помесячной разбивки (отдельный бакет, премии/книги).

**Какой метод недооценивает и почему:** метод (a) недооценивает — из 346 годных 81 строка (23%) имеет грубую дату (год-месяц или только год) и выпадает из недельных корзин; это как раз премии/книги/юбилеи/крупные события, то есть систематически теряется значимое ядро, а не случайный шум. Метод (b) грубее по разрешению (месяц ÷ 4.33 не ловит внутримесячную неравномерность — отсюда и артефакт «12/нед» в июне, вероятно из-за неравномерности исходного сбора данных, а не реального ритма публикаций), но не теряет строки. Честный ответ на вопрос владельца «не меньше 3»: **по методу (a) — нет, 9 из 54 недель (17%) ниже порога 3**; по методу (b) все месяцы кроме обрубленного июля ≥3/нед.

### Разнообразие

Распределение `area` на keep-пуле (738→346): топ — теория чисел 56, комбинаторика 29, матфизика 25, CS 15, геометрия 12, топология 11, история/философия 10, алгебра 10, логика 8, далее длинный хвост. **`area=None` в keep-пуле: 123/346 = 35.5%** (совпадает с общей долей пустого `area` по базе, названной в README/заходе; заполнение — работа 1c, здесь не трогалось).

На keep-строках с полной датой И заполненным `area` (233 из 346): **49 ISO-недель имели ≥1 такую строку** — из них 2 недели моно (один раздел), 15 недель с 2 разделами, **32 недели (65%) с ≥3 разделами**.

### Перетегирование

**22 строки** перетегированы `category A→E` (некрологи/юбилеи/интервью, сидящие в журналах): 19 в Notices of the AMS (13 obituary, 5 interview, 1 anniversary), 2 в УМН (1 anniversary — 80-летие Фоменко, 1 obituary — Кузнецов), 1 в Bulletin of the London Math. Society (некролог Bent Fuglede). Все получили `keep=1, fit_reason='people'`, старое `category/type` сохранено в `notes` через `| was: <old>`. Верификатор подтвердил формат на всех 22 строках, нарушений нет.

### Что НЕ трогал
`area` (0 изменений — сверено побайтово с `git show 37a8dfd:materials.db`, идентично); `breadth_score`, `significance_score`, `summary`, `title`, `authors`, `date`, `url`, `retrieved_at`; пайплайн (`fetch_*.py`, `build.py`, `score.py`) не переписывался; README/report.* не консолидировались (по инструкции — это 1c/аналитик); ничего не `DELETE`, `count(*)` осталась 738.

### Верификатор (§4, свежий субагент, независимый SQL)
**ЗЕЛЁНЫЙ, список расхождений пуст.** Проверено: (a) count=738 ✓; (b) 0 несудённых ✓; (c) 0 тегов вне словаря ✓; (c2) 0 нарушений правила keep⇔fitness ✓; (d) курированный счёт прямым SQL совпал (346, по категориям сошлось) ✓; (e) 20 случайных keep=1 — все правдоподобно легибельны, 2 мягких пограничных случая отмечены как вкусовые, не как ошибка; (f) 15 случайных keep=0 + отдельная явная проверка всех 14 keep=0-строк у Kalai/Tao — закопанного разбора прорыва не найдено (keyword-скан Ramsey/breakthrough/proved/disproved/solved по всей базе среди keep=0 вернул только легитимные узкие спец-обзоры); (h) 22 перетегированные строки — формат `was:` соблюдён на всех.

`audit_sample.md` — 50 строк (25 keep=1 / 25 keep=0), все 13 fit_reason-тегов представлены, лежит в корне репозитория `~/Documents/GitHub/matemdigest-map/audit_sample.md`.

### Время и токены
8 батч-субагентов + 1 верификатор = 9 запусков. Суммарно по субагентам: **≈737k токенов**, **≈35 минут суммарного subagent-времени**, но волны шли параллельно (≤5 одновременно) — реальное настенное время двух волн + верификации + сборки отчёта ≈ **20–25 минут**.

### Вывод
Годный слой держит заявленный темп **~5-6/нед в среднем** (метод (a) даёт медиану 4, метод (b) — среднее 6.0/нед; оба выше нижней границы ожидания), но **условие «не меньше 3» не универсально**: по точным датам 9 из 54 недель (17%) проседают ниже порога — это ожидаемый эффект грубых дат (премии/книги/юбилеи концентрируются без точного дня и не попадают в недельные корзины метода (a), хотя по факту материал есть). Просело сильнее всего там, где и предсказывал аналитик: **узкие спец-журналы** (EMS Surveys 0/17, Acta Numerica 0/12, Takagi 0/6, Expositiones 10/67) и **личный блог-балласт** (Ellenberg 19/104, Baez 10/72) — но защищённая зона **Kalai (82%) и Tao (74%)** не пострадала, как и требовалось. Итоговое число 346 выше диапазона аналитика (270–315) в основном за счёт того, что C-попсай оказался почти монолитно годным (83/86) и Kalai/Tao дали заметно больше блог-мостов, чем ожидалось.
