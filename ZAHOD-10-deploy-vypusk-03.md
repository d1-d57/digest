# Канал исполнителя — MATEMDIGEST · ЗАХОД-10: опубликовать выпуск 3

**Кому:** Claude Code, чистый контекст. Читать только этот файл и то, что здесь названо. В проект не погружаться — заход самоценный.
**Рабочая папка:** корень репозитория `matemdigest-map` (там, где лежит этот файл).
**Оценка:** 5–10 минут, задача техническая. Предыдущий такой заход — `ZAHOD-9-deploy-vypusk-02.md`, там же отчёт о том, как прошёл прошлый раз.

## 0. ПЕРВЫЙ ХОД

Прочитать файл целиком, записать план в `## ПЛАН` внизу, начать. Блокеры — в `## ВОПРОСЫ`, продолжать с тем, что можно без ответа.

## 1. ЧТО ЕСТЬ

Репозиторий `matemdigest-map` — рабочий и приватный. Наружу не идёт.

Публикуется только папка `site/` — это **отдельный git-репозиторий** с удалённым адресом `github.com/d1-d57/digest`, GitHub Pages включён из корня ветки `main`. Внутри лежат `index.html` (оглавление), `1/index.html` и `2/index.html`.

Выпуск 3 собран в `vypuski/2026-08-08-vypusk-03/` — источник `vypusk.md`, готовая страница `vypusk.html`. Вычитан владельцем 09.08.2026, гейты зелёные, пересобран после последней правки.

## 2. ГРАНИЦЫ

- Трогать разрешено: `site/` и этот файл.
- **Не трогать:** `materials.db`, `docs/`, `vypuski/build_vypusk.py`, `vypuski/check_vypusk.py`, другие `ZAHOD-*.md`, корневой `.git`, опубликованные выпуски 1 и 2 в `site/`.
- Ничего из корня репозитория в публичный репозиторий не копировать, кроме названного в §3.

## 3. ЗАДАЧА

1. Скопировать `vypuski/2026-08-08-vypusk-03/vypusk.html` → `site/3/index.html` (папку создать). Пересобирать не нужно: `page_url` в шапке уже `https://d1-d57.github.io/digest/3/`, страница собрана после последней правки.
2. Дописать выпуск в оглавление `site/index.html`: новая строка идёт **первой** в блоке `<div class=list>`, по образцу лежащей там строки выпуска 2. Данные: `Выпуск 3 · 8 августа 2026`, ссылка `3/`, заголовок «Построена группа, которую нельзя приблизить конечными». Фраза-описание — сжать `teaser` из шапки `vypusk.md` до одного предложения того же регистра, что у выпуска 2.
3. Закоммитить и запушить из `site/`: сообщение `выпуск 3: Построена группа, которую нельзя приблизить конечными`.
4. Дождаться сборки Pages (`gh api repos/d1-d57/digest/pages/builds/latest` до `built`, не дольше 5 минут) и проверить curl-ом, что все страницы отдают 200:
   - `https://d1-d57.github.io/digest/`
   - `https://d1-d57.github.io/digest/1/`, `/2/`, `/3/`

## 3b. ОСОБОЕ ДЛЯ ЭТОГО ВЫПУСКА — проверить математику

В выпуске формул больше, чем в предыдущем, и они в строке, а не только выключные. Страница подтягивает **KaTeX** с `cdn.jsdelivr.net`. Аналитик проверить это не мог: из его песочницы нет сети.

- `curl -sI https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.js` → 200. Так же для `katex.min.css` и `contrib/auto-render.min.js`.
- В опубликованной `/3/` должно быть подключение KaTeX в `<head>` и один блок `<p class=disp>`.
- Если есть возможность отрендерить страницу (headless Chrome, Playwright) — скриншот главы **«2 · Софичность»** и посмотреть глазами. Там абзац с формулами в строке: `d(σ(gh),σ(g)σ(h)) = 0`, `d(σ(g),σ(h)) = 1`, `σ(g)^{-1}σ(h)`. Проверить именно последнее: должна быть математическая верхняя −1, а не текст `^{-1}` и не «-¹».

Если KaTeX не грузится или формулы не рендерятся — **не чинить самостоятельно**, записать в `## ВОПРОСЫ` что именно видно и остановиться на этом пункте; выпуск при этом всё равно оставить опубликованным.

## 4. ПРОВЕРКА ПЕРЕД ОТЧЁТОМ

- `git -C site ls-files` — ровно `index.html`, `1/index.html`, `2/index.html`, `3/index.html`, ничего лишнего.
- Выпуски 1 и 2 отвечают 200 по-прежнему.
- Корневой репозиторий изменён только в этом файле.

## 5. ОТЧЁТ → секция `## ОТЧЁТ`

Адреса, коды ответов, вывод `git -C site ls-files`, результат проверки KaTeX из §3b, что пошло не по плану, wall-clock.

---

## ПЛАН

_(заполняет исполнитель)_

## ВОПРОСЫ

_(заполняет исполнитель)_

## ОТЧЁТ

_(заполняет исполнитель)_
