# Канал исполнителя — git-podmesti-vse (один заход до конца)
> Твой единственный файл-заход. Читай ТОЛЬКО его и названные якоря; проект не изучай.
<!-- собран bootstrap_zahod.py -->
> План/вопросы/отчёт — в секции внизу. Метрика — КАЧЕСТВО. Часы — норма.
> **Модель: Opus 5** — механика git расписана, но решение «чья это работа и в какой коммит она идёт» рождается по ходу: репозиториев больше десяти, часть путей принадлежит чужим живым сессиям.

## СТАРТОВОЕ СООБЩЕНИЕ ВЛАДЕЛЬЦУ

> Это блок для владельца — то, чем тебя запустили. Исполнителю здесь делать нечего, твоё задание ниже.

```
Модель: Opus 5 — механика git расписана, но «чья это работа и в какой коммит она идёт» решается по ходу.

Ты исполнитель в репозитории /Users/ivanyakovlev/Documents/GitHub/matemdigest-map.

Твой единственный вход — файл-заход:
/Users/ivanyakovlev/Documents/GitHub/matemdigest-map/zhurnal/2026-08-15_vypusk-04/kod_git-podmesti-vse.md

Прочитай его ЦЕЛИКОМ и работай строго по нему. Он самоценный: в нём назван
контракт зоны, ветка, что читать, задача, критерий готовности и форма отчёта.
Проект самостоятельно НЕ изучай и никаких других файлов по своей инициативе
не открывай — заход сам скажет, что читать.

Ничего сверх задачи не трогай. Оговорка, без которой два указания
противоречат друг другу:
«ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

Первым ходом: git branch --show-current, затем чтение захода, затем секция
## ПЛАН внутри него — до всяких действий.
```

── СЧЁТ НЕЗАКРЫТОГО (печать, не гейт) ──
🔴 снимок при сборке 2026-08-17, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `python3 _generator/tools/schet_nezakrytogo.py zhurnal/2026-08-15_vypusk-04`
Область: «zhurnal/2026-08-15_vypusk-04» — сужены пункты 1, 3, 4; долги (2) глобальны намеренно (DOLG.md не размечен по записям).
Приоритет владельца: разобрать инциденты важнее, потом закрыть долги — неразобранный инцидент это повторяющаяся ошибка, долг может подождать.
  1. инцидентов без вердикта             : 0
  2. долгов СТАТУС: ЖИВ                  : н/д — /sessions/trusting-sharp-faraday/Documents/GitHub/disciplina/skills/slajdy/DOLG.md недоступен (другой git-репозиторий)
  3. уроков фабрике без ВЕРДИКТ          : 0
  4. пунктов очереди «ДОСТАВЛЕНО: нет»   : 0

КОНТЕКСТ. «Матемдайджест» — рассылка про математику; вокруг неё десяток репозиториев под
`~/Documents/GitHub`, и Cowork из песочницы в `.git` писать не может. Поэтому работа копится вне git
и оформляется ЗАЯВКАМИ, которые закрывает заход вроде тебя. Прошлый этап: заявок накопилось четыре
(три в `matemdigest-map`, одна кросс-репозиторная), последняя из них — от 17.08 вечера.
ЦЕЛЬ: закрыть ВСЕ открытые заявки во ВСЕХ репозиториях, вывезти на GitHub и задеплоить сайт.
Приёмка — по ОТЧЁТУ, без построчной сверки.

🔴 **ТЫ НИЧЕГО НЕ ПИШЕШЬ ПО СУЩЕСТВУ.** Ни строки текста, ни правки кода, ни «заодно почищу». Твой
предмет — СОСТОЯНИЕ репозиториев: коммиты, ветки, вывоз, деплой. Увидел содержательный дефект —
строкой в отчёт, руками не трогай.

⚠ **ГЕЙТ СБОРКИ ДАЁТ ОДНО ЛОЖНОЕ КРАСНОЕ, И ЭТО ПРОВЕРЕНО, А НЕ СПИСАНО НА ШУМ.** `check_sborki.py`
печатает «ветки `rabota` НЕ СУЩЕСТВУЕТ». Он ищет её в СВОЁМ репозитории (`materials`), а не в том,
где заход будет работать: `GIT_ZONA_REPO` он не слушает. В `matemdigest-map` ветка есть —
`git --no-optional-locks rev-parse --verify rabota` → `f329815`, rc=0, снято при сборке 17.08.
Проверь той же командой первым ходом; красное на С1 при зелёной команде игнорируй, а сам факт
занеси в `## УРОКИ ФАБРИКЕ` — гейт слепнет на каждом кросс-репозиторном заходе.

## ЧТО ФИНАЛИЗИРОВАНО НА ИНТЕРВЬЮ

ИНТЕРВЬЮ ПРОВЕДЕНО: да (2026-08-17) — флаг `--intervyu da` при сборке. ⚠ Он доказывает, что аналитик не ЗАБЫЛ про интервью, и НЕ доказывает, что разговор был.

1. канал запуска — Claude Code в десктопе (app)
2. охват — все репозитории подряд, не только matemdigest-map
3. сайт деплоить в конце, как просит ZAYAVKA-2026-08-17-intervyu-i-deploy.md

## КОНТРАКТ ЗОНЫ (обязателен — не удалять; вписан Cowork)
- **МЕСТО РАБОТЫ:** ветка `rabota` в основной папке. 🔴 Она должна УЖЕ стоять. НЕ на ней — СТОП, НЕ делай `git checkout`: в общей папке он МОЛЧА откатывает дерево к состоянию ветки (цена 27→28.07: файл сильно откатился ночью, поймал владелец вручную; след в git НЕ остаётся). Тогда заход пересобрать с `--worktree`. Ветку не переключай, в другие НЕ коммить.
- **ЗОНА (можно менять):** `zhurnal/_INFRA-git` `zhurnal/2026-08-15_vypusk-04/ZAYAVKA-NA-KOMMIT.md` `zhurnal/2026-08-15_vypusk-04/kod_git-podmesti-vse.md`. Всё вне — **READ-ONLY**: не править, не двигать, не удалять, не рефакторить «заодно».
- 🔴 **ЗАВЁЛ НОВЫЙ `.md` — РЕГИСТРИРУЕШЬ ЕГО САМ, ТЕМ ЖЕ ХОДОМ, ОДНОЙ КОМАНДОЙ:** `python3 _generator/tools/register_doc.py <путь> "<описание>"` (из корня репо). `_studio/docs/` тебе по-прежнему READ-ONLY **для правки руками** — дверь ровно одна, и это она. Дверь идемпотентна (повторный вызов дубля не заведёт) и отказывает на пути вне `_studio/`, на несуществующем файле и на пустом описании. Свой файл-заход регистрировать не нужно: он рождается зарегистрированным из `bootstrap_zahod.py`. **Красный хук на ТВОЁМ новом `.md` — это не повод для `--no-verify`, а повод позвать дверь.** *Почему правило существует и почему оно теперь исполнимо: 26.07 оно записано с ценой в пять документов-сирот и через два дня повторилось дословно. Дальше стало хуже: до 30.07 указания «зарегистрируй» и «`docs/` только на чтение» противоречили друг другу, выход был ровно один — обойти хук, и по автологу `_INFRA-git/INCIDENTY.md` это 28 обходов `--no-verify` из 56 срывов коммита, 27 из них по одной этой причине (48 % всей боли с коммитами, тринадцать исполнителей подряд). Обходить больше нечего.*
- **КОММИТ:** два хода — `add` по своим путям, затем `commit` **с теми же путями после `--`** (полная форма и цена каждого хода — §4). НИКОГДА `-A` / `.` / `commit -am`, и никогда `commit` без путей. Субагенты не коммитят. **`--no-optional-locks` обязателен:** обычный git переписывает индекс, берёт `.git/index.lock` и роняет параллельный ручной коммит владельца.
- 🔴 **Звал `register_doc.py` — допиши `_studio/docs/KARTA.md` к своим путям В ОБОИХ ходах.** Строка регистрации лежит физически в нём. Ворота 5 читают `§6` **с диска**, а не из индекса: коммит без этого файла пройдёт ЗЕЛЁНЫМ, документ уедет сиротой, а строка умрёт при первом `checkout` (дата данных 2026-07-30, найдено верификацией захода `kod_registracia-bez-obhoda.md`).
- **ЗАПРЕТ:** ничего за пределами зоны, даже если «мешает» или «чинится в одну строку». Нашёл проблему вне зоны → в отчёт, не трогай.

## 0. ПЕРВЫЙ ХОД
### 0.1 🔴 ГИТ-КОНТУР — ДО ВСЕГО ОСТАЛЬНОГО, И ПЕРВЫМ ХОДОМ ЦЕЛИКОМ

🔴 «ничего сверх задачи» относится к СОДЕРЖАНИЮ работы; git-контур §0.1 — законное исключение, он про состояние репозитория и исполняется целиком.

🔴 **ПОРЯДОК ЗДЕСЬ — ЧАСТЬ УСТРОЙСТВА, А НЕ ОФОРМЛЕНИЕ. Сначала субагент вливает В ОСНОВНУЮ ветку, и только ПОТОМ ты заводишь свою рабочую папку.** Пока влитие шло в ветку захода, а влитие в основную было ходом приёмки (которая из песочницы в `.git` писать не может), работа копилась лестницей в последней ветке цепочки, а основная не получала ничего — замер 14.08: 14 невлитых веток, цепочка из пяти внутри последней, 84 невывезенных коммита, и генератор в основной папке не знал о собственных улучшениях. Заводя ветку ПОСЛЕ влития, ты отпочковываешь её от основной, которая уже всё содержит: инструмент оказывается на диске сам, дотаскивать нечего.

**1. ВЕСЬ КОНТУР — В СУБАГЕНТА, ОДНИМ ХОДОМ, ДО СВОЕЙ ПАПКИ.** Очередь заявок, влитие названных веток в ОСНОВНУЮ, что забрать в git по ходу и что погасить после — на содержание твоей задачи не влияют. Запусти ОДНОГО субагента, дождись его и вставь его шесть строк в `## ОТЧЁТ` дословно; сам эти пункты не исполняй. У него ОТДЕЛЬНЫЕ ПРАВА, объявленные в его же промпте: главная папка, основная ветка, вывоз — и ничего сверх; зону захода он не коммитит, её коммитишь ты последним ходом. 🔴 ОТВЕТ ЛЮБОГО субагента, которого ты запускаешь (не только этого), обязан КОНЧАТЬСЯ строкой «выдано N позиций из M найденных»: канал мог оборвать его молча, и без этой строки усечение неотличимо от честного «мало нашлось». Нет строки — ответ усечён, в `## ОТЧЁТ` не вставляй, перезапроси. Полный текст задания печатает команда:
```
python3 _generator/tools/bootstrap_zahod.py --zadanie-subagentu --zone zhurnal/_INFRA-git --zone zhurnal/2026-08-15_vypusk-04/ZAYAVKA-NA-KOMMIT.md --kommitit 'всё, что окажется вне git на момент старта, во ВСЕХ репозиториях под ~/Documents/GitHub' --zakryt 'зелёные ветки zahod/* через zakryt-vetku --vse-zelenye; рабочие папки *-wt не трогать при незакоммиченном'
```

**2. ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ** (команда — в блоке «МЕСТО РАБОТЫ» выше) и работай в ней как обычно. Её ветка отпочкована от свежей основной, поэтому инструмент, которым ты работаешь, уже на диске — отдельного «влить перед работой» больше нет.

вливать нечего, проверено командой `git branch --no-merged` — но проверено ПРИ СБОРКЕ, а не сейчас: невлитых `zahod/*`-веток было 0. 🔴 снимок при сборке 2026-08-17, ПРОВЕРЬ ПЕРВЫМ ХОДОМ: `git --no-optional-locks branch --no-merged | grep -c 'zahod/'`. Число могло устареть между сборкой и твоим прогоном — 14.08 заход нёс ровно этот ноль, а к прогону невлитых было три.



- Проверь ветку: `git branch --show-current` — обязано быть `rabota`. Не она — СТОП, `git checkout` НЕ делай (§4 GIT-disciplina), нужен `--worktree`.
- Точка отката: `git add `zhurnal/_INFRA-git` `zhurnal/2026-08-15_vypusk-04/ZAYAVKA-NA-KOMMIT.md` `zhurnal/2026-08-15_vypusk-04/kod_git-podmesti-vse.md`` → commit (или zip), если зона не чиста в HEAD (не фабрикуй, если чиста).
- Прочитать ТОЛЬКО эти файлы, поимённо, и ничего больше. Проект не изучай:
  1. `zhurnal/2026-08-15_vypusk-04/ZAYAVKA-NA-KOMMIT.md` — три раздела; **последний, «ДОПОЛНЕНИЕ —
     сессия 2026-08-17, вечер», самый свежий и самый важный**: в нём точный список путей и то,
     чего касаться НЕЛЬЗЯ.
  2. `zhurnal/_INFRA-git/ZAYAVKA-2026-08-17-intervyu-i-deploy.md` — «закоммитить всё и задеплоить».
  3. `zhurnal/_INFRA-git/ZAYAVKA-2026-08-16-mesyachnyy-dolg.md` — свести проект в один репозиторий;
     ⚠ её §3 УСТАРЕЛ, о чём сказано в её же шапке; бери из неё только таблицу сообщений коммитов §4.
  4. `zhurnal/_INFRA-git/INCIDENTY.md` — чем уже падало здесь.
  Адрес живой очереди заявок — командой, а не памятью:
  `python3 $MAT/_generator/tools/git_zona.py zayavki`
- ПЛАН — в `## ПЛАН` перед действиями.

## 1. ДИСЦИПЛИНА (Карпатов)
🔴 **Код возврата — ПЕРВЫМ, до содержательного вывода команды.** «Отработала» и «упала, а я читаю прошлое состояние» выглядят одинаково; сначала `echo $?`, потом выводы. То же с гейтами. *Цена 21.07: `rc=128` (сбой прав окружения) четырежды прочитан как результат — едва не откатили верное правило по ложным данным.*
Предпосылки/развилки назвать вслух; минимум без спекуляций; хирургия (строка → к заданию); критерий, который может провалиться. Якорные замены — abort при ≠1. Сохранять по умолчанию. **Оспорить ложную предпосылку — включая КРИТЕРИЙ ГОТОВНОСТИ: считаешь его кривым — скажи в `## ПЛАН`, ДО работы, и предложи поправку.** Субагенты: ≤5, рейт-лимит = отступить + доложить (не слепой ретрай).

🔴 **Пишешь содержательный текст — термин НЕ употребляется раньше, чем определён**, включая заголовки, подводки и формулировки теорем. «Определение в тексте есть» не считается: если оно ниже первого рабочего употребления, читатель встаёт ровно там. Чинится ПЕРЕСТАНОВКОЙ определения вверх, не дописыванием пояснения. Гейт: `python3 _generator/tools/check_termin.py <src>` (exit 1 при нарушении). Канон — `../docs/kak-delat/STANDART-teksta.md` правило 11. *Цена 30.07: теорема пользовалась словом «ординал», определение стояло строкой ниже; поймал владелец, ни один гейт не увидел, раздел переписан дважды.*

## 2. ЗАДАЧА
🔴 **ИНСТРУМЕНТ ЖИВЁТ В `materials`, А РАБОТАЕШЬ ТЫ ВО ВСЕХ РЕПОЗИТОРИЯХ.** Путь к инструменту и
репозиторий работы — РАЗНЫЕ переменные; буквальный `python3 _generator/tools/git_zona.py` вне
`materials` даёт «can't open file» при живом и работающем инструменте. Форма вызова везде такая:

```
export MAT=~/Documents/GitHub/materials
GIT_ZONA_REPO=$PWD python3 $MAT/_generator/tools/git_zona.py <команда>
```

**Шаг 1 · ПЕРЕПИСЬ. Read-only, до единой правки.** Пройди все каталоги первого уровня в
`~/Documents/GitHub`, у которых есть `.git`. По КАЖДОМУ три числа — вне git · невлитых · невывезенных:

```
cd ~/Documents/GitHub
for R in */; do [ -d "$R/.git" ] || continue; echo "== $R"
  git -C "$R" --no-optional-locks status --porcelain | wc -l
  git -C "$R" --no-optional-locks branch --no-merged | wc -l
  git -C "$R" --no-optional-locks log --oneline @{u}.. 2>/dev/null | wc -l
done
```
Вывод целиком — в `## ГИГИЕНА ВХОДА`, дословно. Это снимок ДО работы; без него «всё закрыто»
непроверяемо.

**Шаг 2 · РАЗБОР ВЛАДЕЛЬЦА.** По каждому пути реши, чей он, и раздели на три ведра:
  — **названный заявкой** → коммить с сообщением из заявки;
  — **не названный, но явно чей-то** (журнал, выпуск, пост) → коммить отдельным коммитом со своим
    сообщением; свальный коммит «разное» запрещён;
  — **порождаемое или мусор** → в `.gitignore`, не в историю. Сомневаешься — НЕ коммить и назвать
    строкой в отчёте.

🔴 **ЧЕГО НЕ ТРОГАТЬ — проверено при сборке 17.08 19:00, но ПРОВЕРЬ ЗАНОВО, состояние живое:**
  — `vypuski/.korpus/` — 132 МБ сырья, стоит в `.gitignore`. В историю не пускать: вытащить
    обратно можно только переписыванием истории.
  — `zhurnal/2026-08-15_vypusk-04/kod_korpus-wiki.md` — **в него ПИШЕТ работающий исполнитель**,
    прогон Википедии идёт с 16.08. Файл под чужой рукой: коммить его как есть, но НЕ править и не
    ждать его завершения. Живой ли прогон — видно по времени `vypuski/.korpus/wiki_teksty.json`:
    `ls -la vypuski/.korpus/wiki_teksty.json` (растёт — прогон идёт).
  — рабочие папки `*-wt` (`materials-wt/**`, `disciplina-wt/**`) — там незакоммиченное чужих
    заходов; `worktree drop --force` НЕ звать ни при каких условиях, восстанавливать будет нечего.

**Шаг 3 · КОММИТЫ, ПОТОМ ВЕТКИ, ПОТОМ ВЫВОЗ.** Порядок жёсткий и назван владельцем: обратный не
работает технически — влитие отказывает на грязном дереве, вывоз на невлитом. Ветку-витрину `main`
`matemdigest-map` НЕ вливать: влитие туда есть публикация.

**Шаг 4 · ДЕПЛОЙ САЙТА.** Решение владельца 17.08 — деплоить. Публикация есть ПЕРЕНОС содержимого
`sayt/`, а не слияние веток:
```
GIT_ZONA_REPO=~/Documents/GitHub/matemdigest-map python3 $MAT/_generator/tools/git_zona.py opublikovat
GIT_ZONA_REPO=~/Documents/GitHub/matemdigest-map python3 $MAT/_generator/tools/git_zona.py opublikovat --yes
```
Сначала предпросмотр, потом `--yes`. ⚠ «Вывезено» не равно «опубликовано»: дождись исхода Actions и
назови его. Actions упали или деплой отвергнут — это законный отчёт, чинить настройки репозитория
на GitHub ты не можешь.

**Шаг 5 · ЗАКРЫТЬ ЗАЯВКИ.** В каждой закрытой заявке допиши хеш коммита и смени `status` на
`закрыта`. Заявка без хеша считается открытой.

**КРИТЕРИЙ ГОТОВНОСТИ (может ПРОВАЛИТЬСЯ).** Прогони цикл шага 1 ПОВТОРНО, после всей работы. Все
три числа по каждому репозиторию — нули, ЛИБО каждое ненулевое названо поимённо с причиной, почему
оно непроходимо твоими правами. Плюс: `status --porcelain` в `matemdigest-map` пуст; ни одна из
четырёх заявок не осталась со `status: открыта` без объяснения; страница сайта отдаёт свежую
сборку.

🔴 **Числа в критерии — те, что напечатает КОМАНДА, а не те, что я вписал бы сюда сейчас.** Между
сборкой этого захода и твоим прогоном рядом работают чужие сессии, и снимок устареет.

**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «всё закоммичено», а «репозиториев проверено X из
Y», где Y — число каталогов с `.git`, посчитанное циклом шага 1 ДО работы.
**Отрицательный вердикт несёт ОХВАТ В СЕБЕ:** не «дыр не найдено», а «дыр не найдено, проверено X из Y». Без охвата вердикт не принимается — «проверено 2 из 9» и «проверено 9 из 9» выглядят одинаково.

## 3. ВЕРИФИКАТОР (если двигаем/теряем/жмём)
🔴 **ВЕРИФИКАТОР ЗДЕСЬ НУЖЕН, И ОН ТИПА «ДО», А НЕ «ПОСЛЕ».** Публикация сайта и гашение веток —
лоссовые операции: они уничтожают ровно то состояние, которое надо было проверить, поэтому проверка
после них неисполнима.

**ДО-верификатор — свежий субагент, ПЕРВЫМ ходом, до любой правки.** Его вопрос: что пропадёт.
Метод у него ДРУГОЙ, чем у тебя, — он читает не `status`, а `poteri` по каждой ветке:
`GIT_ZONA_REPO=<репо> python3 $MAT/_generator/tools/git_zona.py poteri --branch <ветка>` — команда
печатает ФАЙЛЫ, а не число коммитов. Его вердикт: «при закрытии названных веток теряется файлов N,
поимённо». Ответ обязан кончаться строкой «выдано N позиций из M найденных» — без неё считай ответ
усечённым и перезапроси.

**ПОСЛЕ-верификатор — второй свежий субагент, после деплоя.** Его вопрос: доехало ли. Метод другой:
он не смотрит в git, а открывает опубликованную страницу и сверяет её с `sayt/` на диске. Вердикт с
охватом: «страниц сверено X из Y, расхождений 0».

## 4. 🔴 КОММИТ СВОЕЙ ЗОНЫ — ПОСЛЕДНИЙ ХОД ЗАХОДА, НЕ ОПЦИЯ
Ты работаешь host-side и в `.git` ПИШЕШЬ — значит коммитишь САМ, никому не передавая:
```
git --no-optional-locks add -- zhurnal/_INFRA-git zhurnal/2026-08-15_vypusk-04/ZAYAVKA-NA-KOMMIT.md zhurnal/2026-08-15_vypusk-04/kod_git-podmesti-vse.md                     # вводит НОВЫЕ пути в индекс
git --no-optional-locks commit -m "<зона>: <что сделано>" -- zhurnal/_INFRA-git zhurnal/2026-08-15_vypusk-04/ZAYAVKA-NA-KOMMIT.md zhurnal/2026-08-15_vypusk-04/kod_git-podmesti-vse.md   # отсекает всё чужое
python3 _generator/tools/git_zona.py check --zone <зона>   # из корня репо; должен быть ✅
git --no-optional-locks show --stat                        # обязаны быть ТОЛЬКО твои пути
```
🔴 **ОБА хода обязательны, ни один не лишний** (полное «почему» и цена — `../docs/kak-delat/GIT-disciplina.md §3`):
- **`add`** — pathspec-коммит знает только **отслеживаемые** пути; новый файл без `add` даёт `did not match any file(s) known to git`.
- **`-- <пути>` в самом `commit`** — иначе `commit` забирает индекс ЦЕЛИКОМ, вместе с чужим, застейдженным кем угодно рядом с тобой (репо `materials/` общий, писателей трое). *Обе половины оплачены 21.07 в один вечер: голый `commit` подмёл чужой индекс — коммит на 89 файлов вместо трёх; «починка», убравшая `add`, завалила все 10 коммитов повторно.*
⚠ **Хук `pre-commit` покраснел — сначала посмотри, на ЧЬИХ путях.**
- Красное на ТВОИХ путях (новый `.md` не зарегистрирован в `../../docs/KARTA.md §6`, битая ссылка) — **чини, не обходи**: там только твоё, обходить нечего.
- Красное на ЧУЖОМ, унаследованном долге (ворота дают сотни ❌ старых нарушений) — законный обход, но ТОЛЬКО с причиной; голый `--no-verify` инструмент отклонит, а причина сама уедет в `INCIDENTY.md`:
```
python3 _generator/tools/git_zona.py commit --zone <зона> \
    --no-verify "чужой долг: <что именно покраснело>" -m "<что и зачем>" --push
```
Ту же причину назови отдельной строкой отчёта долгом. *Урок 9: обход был законен по канону и не существовал в инструменте — первый же коммит владельца встал на чужом долге.*
**Коммит не прошёл по ВНЕШНЕЙ причине** (чужой лок, конфликт, detached HEAD) — чужое состояние репозитория НЕ чини: зафиксируй файлы и напиши в отчёт отдельной строкой «коммита нет, причина такая-то, нужно ваше действие». Это законный отчёт. Молчаливое «сделано» при незакоммиченной зоне — брак: приёмка гоняет тот же гейт первым ходом и завернёт отчёт, не читая (`RUKOVODSTVO §Приёмка`, гейт 0).

## 4.1 🔴 ГИГИЕНА — ПРОВЕРКИ ПЕРЕД СДАЧЕЙ (вшито `bootstrap_zahod.py`; исполнителю НЕ удалять)
> Раздел про СОСТОЯНИЕ РЕПОЗИТОРИЯ после твоей работы, а не про правильность
> этого файла и не про планы: правильность файла судят С1/С3/С7 `check_sborki.py`
> ДО прогона, намерение «что влить/коммитить/закрыть» — блок §0.1 ГИТ-КОНТУР.
> Здесь не повторяется ни то, ни другое. Каждый пункт — КОМАНДА; её вывод, а не
> пересказ, уходит в `## ОТЧЁТ`. Пункт неприменим — так и напиши: «неприменимо,
> потому что …». **Молчание читается как «не сделано», а не как «всё чисто».**
> ⚠ `Г1`–`Г6` ниже — пункты ЭТОГО раздела. Гейты `priyomka.py` (`Г7` в секции
> `## ВОПРОСЫ`) — ЧУЖАЯ семья с той же буквой: их гоняет приёмка, не ты.
> Столкновение нумерации нашёл свежий исполнитель, читавший только этот файл.

**ЗОНА ГИГИЕНЫ:** `zhurnal/_INFRA-git` `zhurnal/2026-08-15_vypusk-04/ZAYAVKA-NA-KOMMIT.md` `zhurnal/2026-08-15_vypusk-04/kod_git-podmesti-vse.md`

- **Г1. Зона доехала в git.** `python3 _generator/tools/git_zona.py check --zone zhurnal/_INFRA-git` → ✅; `python3 _generator/tools/git_zona.py check --zone zhurnal/2026-08-15_vypusk-04/ZAYAVKA-NA-KOMMIT.md` → ✅; `python3 _generator/tools/git_zona.py check --zone zhurnal/2026-08-15_vypusk-04/kod_git-podmesti-vse.md` → ✅. Красное на любой из команд — отчёт не принимается: приёмка гоняет их все первым ходом.
- **Г2. Второй репозиторий.** **неприменимо, и это проверено при сборке, а не предположено:** все пути зоны лежат внутри репозитория `matemdigest-map` (тот же критерий, что у С2 `check_sborki.py`). Зона расширилась за его пределы по ходу — пункт снова применим; команда та же, что в применимом случае: `cd ../<репозиторий> && git --no-optional-locks status --porcelain` → пусто. *Команда названа и здесь нарочно (находка верификатора): пункт, который объявлен неприменимым и не говорит, ЧТО делать, когда станет применим, исполнить в этот момент нечем.*
- **Г3. Невлитых веток не прибавилось.** `git --no-optional-locks branch --no-merged rabota` — число сравни с тем, что было на входе. Выросло — назови, чьи ветки и почему они законны.
- **Г4. Новый инструмент имеет живую точку вызова.** Завёл `.py` в `_generator/**` — `python3 _generator/tools/check_tool_contract.py <свои новые файлы>` → rc=0. Ни одного нового `.py` — так и напиши. *Инструмент без точки вызова зелен ровно потому, что его никто не звал.*
- **Г5. Новый `.md` зарегистрирован.** Завёл — звал ли ты `register_doc.py` и лежит ли строка на диске: `grep -c '<имя файла>' <карта своего корня>` → 1. Карту своего корня называет `korni.карта_для('<путь>')`, руками её не угадывай.
- **Г6. В коммите нет чужих путей.** `git --no-optional-locks show --stat` — только твои пути. Чужой путь в своём коммите — это чужая работа, унесённая твоим `commit` без `--`.

## 5. ОТЧЁТ → секция `## ОТЧЁТ` внизу
Что сделал + ЗАЧЕМ / как проверил / что НЕ трогал / вопросы / результат верификатора / открытое «возвращаться» / **время прогона + токены — на канале `app` НЕПРИМЕНИМО, снимать неоткуда** (лог прогона существует только на канале `terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет). Строку не заполнять числом и не извиняться за его отсутствие — это не недосмотр исполнителя, а свойство канала) / **ПОВТОРЯЕМОСТЬ находок (строка обязательна — см. ниже)** / **АРТЕФАКТ (строка обязательна)** / **КОММИТ (строка обязательна, см. §4)**.

🔴 **АРТЕФАКТ — АБСОЛЮТНЫЙ ПУТЬ К СОБРАННОМУ ФАЙЛУ, отдельной строкой.** Не «колода пересобрана», не «см. `dist/`», а путь, который владелец скопирует и откроет. *Цена 31.07: за сессию собрано три артефакта, ни один путь не был назван в отчёте — владелец не нашёл ни одного и сказал прямо: «я всё время не могу найти твои новые файлы». Хуже: он открыл СТАРУЮ колоду, потому что сборка молча не запустилась, и решил, что правка не сработала; ушёл целый круг на диагностику того, чего не было. Отчёт без адреса артефакта — это отчёт о работе, которую нельзя посмотреть.*

🔴 **НЕОБРАТИМОЕ — ОТДЕЛЬНЫМ СПИСКОМ, даже если оно стояло в задании.** Удаление, перезапись, переименование, перемещение, `git reset`/`checkout` поверх несохранённого, любая правка, вышедшая за зону, — каждое ОДНОЙ строкой: **что · где · чем восстанавливается** (хэш коммита, путь к бэкапу). Ты запущен без запроса разрешений (`--dangerously-skip-permissions`): владелец НЕ видел ни одного из этих действий в момент, когда оно происходило, и этот список — единственное место, где он о них узнаёт. **Необратимого не было — напиши «необратимого нет».** Молчание от пустоты не отличается, и приёмка прочитает его как пустоту.

🔴 **ПОВТОРЯЕМОСТЬ находок — назови, какие из них повторятся на СЛЕДУЮЩЕЙ единице работы** (лекция/слайд/заход). Критерий вычислимый, не про приоритет: повторится — это НЕ пункт очереди, а заход ДО следующего прогона (правило «класс НЕМЕДЛЕННОЕ», `../../docs/kak-delat/RUKOVODSTVO-zahodami.md`). Не повторится — законно уходит в `## ВОПРОСЫ` пунктом очереди. *Пример владельца: белый фон иллюстраций повторился бы тринадцать раз, каждый раз ценой переделки картинки — заход, а не запись; число, вписанное аналитиком не глядя, на следующих слайдах не повторяется — запись, а не заход.* Находка сделана ПРОБНЫМ прогоном — чинится ДО следующего прогона: «проба, после которой ничего не починили, — потраченные токены».

## ⚠️🔴 WARNING · ПОСЛЕДНИЙ ХОД ПЕРЕД ОТЧЁТОМ — ПОЛНАЯ ГИТ-ГИГИЕНА. НЕ ПРОПУСКАТЬ 🔴⚠️

**СТОП. Прежде чем писать хоть строку в `## ОТЧЁТ` — прогони это целиком.** Требование владельца
2026-08-16, поводом стал заход, который сделал работу и не закоммитил НИЧЕГО: *«он ничего не
коммитит… всегда, когда собираешь по генератору, там должна быть фраза, что начинать надо с полной
проверки гигиены»*. Гит-контур §0.1 стоит в НАЧАЛЕ и разбирает то, что накопилось ДО тебя; этот блок
стоит в КОНЦЕ и разбирает то, что накопил ты сам. Один другого не заменяет.

**ПОРЯДОК ЖЁСТКИЙ, ОН НАЗВАН ВЛАДЕЛЬЦЕМ: сначала все коммиты → потом все ветки → потом вывоз.**
Обратный порядок не работает технически: влитие отказывает на грязном дереве, вывоз — на невлитом.

**1 · ВСЕ КОММИТЫ.** Ничего не осталось вне git — ни в рабочем репозитории, ни в соседних, до
которых ты дотянулся по ходу работы:
```
for R in <репозитории, которых ты касался>; do
  echo "== $R"; git -C $R --no-optional-locks status --porcelain
done
```
Своя зона — своими путями (`add` + `commit -- <пути>`). Чужая содержательная работа — НЕ твоя:
называешь строкой в отчёте и оставляешь. Пусто у всех — так и напиши числом «вне git 0».

**2 · ВСЕ ВЕТКИ.** Невлитого не осталось: `git --no-optional-locks branch --no-merged <основная>`.
Каждая оставшаяся ветка названа поимённо с причиной, почему она жива. 🔴 Ветку-витрину (`main` там,
где с неё публикуется сайт) НЕ вливать — влитие туда есть публикация и решение владельца.

**3 · ВЫВОЗ.** `git --no-optional-locks log --oneline @{u}.. | wc -l` → 0 по каждому репозиторию.
Ненулевое означает, что работа существует только на этом диске.

**4 · ПРОВЕРКА ФАКТОМ, А НЕ ПАМЯТЬЮ.** Три числа по каждому репозиторию — вне git · невлитых ·
невывезенных — печатаются командой и уходят в `## ОТЧЁТ` дословно. Ненулевое число, оставленное без
объяснения, приёмка читает как несделанную работу: она гоняет те же команды первым ходом.

🔴 **Отчёт без этих чисел не принимается.** «Я закоммитил» — не то же самое, что `status --porcelain`
пустой: за одну сессию работа не доезжала трижды, каждый раз с честным «сделано» в отчёте.

## УРОКИ ФАБРИКЕ — (заполняет исполнитель; пусто — нормальный исход)

### Гейт сборки ищет ветку в СВОЁМ репозитории и слепнет на каждом кросс-репозиторном заходе
ЦЕНА: одно ложное красное на С1 в каждом таком заходе. Здесь оно было предсказано аналитиком и
проверено командой (`rev-parse --verify rabota` → `f329815`, rc=0), поэтому стоило одного хода.
Но заход, который этого предупреждения не несёт, встанет на С1 и либо остановится, либо приучится
игнорировать красное гейта — а это дороже самого гейта. `check_sborki.py` не слушает
`GIT_ZONA_REPO`.

### Один и тот же файл-заход называет два разных адреса инструмента, и один из них не существует
ЦЕНА: первая же команда первого хода упала — `python3 _generator/tools/schet_nezakrytogo.py …` →
`can't open file`, rc=2. В `matemdigest-map` каталог `_generator/` есть, но в нём лежит один файл
`KANON-KOREN`, а `tools/` нет вовсе. §2 того же захода говорит прямо: «инструмент живёт в
`materials`, путь к инструменту и репозиторий работы — РАЗНЫЕ переменные», и даёт верную форму
через `$MAT`. При этом §0, §4 и весь §4.1 печатают локальную форму. То есть **вся секция гигиены
Г1–Г5, как она написана, в этом репозитории неисполнима**: `git_zona.py check --zone`,
`check_tool_contract.py`, `register_doc.py` — все по несуществующему пути. Исполнитель обязан
молча переписать команды гейта, которым его же и будут судить. Чинится подстановкой `$MAT` в
шаблон `bootstrap_zahod.py` там же, где он подставляет пути зоны.

### Задание субагенту противоречит само себе в одном пункте
ЦЕНА: ход рассуждения исполнителя и риск прямо противоположного исполнения. Пункт 3 задания несёт
🔴 ГРАНИЦУ «индекс и автологи ты уже забрал, всё ОСТАЛЬНОЕ — содержательная работа — НЕ коммить,
доложи заходу», а подставленная в тот же пункт строка `--kommitit` гласит «всё, что окажется вне
git на момент старта, во ВСЕХ репозиториях». Два указания в одном абзаце требуют
взаимоисключающего. Субагент, прочитавший строку буквально, закоммитил бы 37 путей чужой
содержательной работы одним махом — ровно то, от чего граница защищает. Подстановка `--kommitit`
попадает не в тот пункт: она описывает ОХВАТ поиска, а не мандат на коммит.

### §0.1 отсылает к команде, которой в названном месте нет
ЦЕНА: одна развилка, которую пришлось решать исполнителю без опоры. Пункт 2 говорит «ТЕПЕРЬ ЗАВОДИ
СВОЮ РАБОЧУЮ ПАПКУ (команда — в блоке «МЕСТО РАБОТЫ» выше)». В блоке «МЕСТО РАБОТЫ» команды нет —
там сказано противоположное: ветка `rabota` должна УЖЕ стоять, а `git checkout` запрещён под угрозой
молчаливого отката дерева. Заход, понявший пункт 2 буквально, полез бы заводить worktree или, хуже,
делать `checkout` — то самое действие, цена которого уже оплачена 27→28.07.

### Заявок было четыре, а список «прочитать ТОЛЬКО эти файлы» называл три
ЦЕНА: без живой очереди заход не узнал бы о `ZAYAVKA-2026-08-17-post-vypusk-04.md`, а именно она
несёт §3 «КОНФЛИКТ С СОСЕДНЕЙ ЗАЯВКОЙ» — обе заявки просят один и тот же `vypusk.md` под своё
сообщение. Конфликт решался бы вслепую, то есть первый же коммит унёс бы чужой файл без объяснения.
Спасло то, что заход велит брать адрес очереди КОМАНДОЙ, а не памятью, — и очередь назвала
четвёртую заявку сама. Урок в том, что поимённый список файлов устаревает между сборкой и прогоном
так же, как числа, и должен нести ту же оговорку, что и числа.

### «Предпросмотр не нужен» — указание, которое отличает «сделано» от «уже было сделано»
ЦЕНА: один ход, который окупился сразу. Заявка на деплой велит: «предпросмотр не нужен, решение
владельца 16.08, подтверждено 17.08» — то есть звать `opublikovat --yes` вслепую. Предпросмотр был
снят вопреки указанию и напечатал: «Витрина `main` уже совпадает с `sayt/` — публиковать нечего».
Слепой `--yes` отработал бы штатно, ничего не изменив, и заход отрапортовал бы «сайт задеплоен» —
формально правду, фактически пустышку. Дороже другое: только этот ход показал, что выпуска 4 в
`sayt/` нет ВООБЩЕ, и что «задеплоить» без переноса выпуска не даёт того, чего ждёт владелец.
Отмена предпросмотра экономит ход и покупает риск отрапортовать о работе, которой не было.
> Находка не про эту сессию, а закономерность про саму фабрику, годная другим заходам, — оформи как пункт очереди в `## ВОПРОСЫ` (формат там же) с `ДОМ: <эта арка>/UROKI-FABRIKE.md`, а не пиши прямо сюда неструктурированной строкой.
> **Не про задачу — про САМУ ФАБРИКУ.** Ты работаешь с пустым контекстом и потому видишь то, чего не видит аналитик: он писал этот заход и ему приятно, что заход хорош. Сломался ВХОД (издание не то, id врёт, зона не содержит файла с ответом)? Критерий готовности кривой? Инструкция канона противоречит живому файлу? — сюда, строкой.
> Формат жёсткий (по нему гейт): `### <что произошло>` / `ЦЕНА: <что сломалось и сколько стоило>`.
> **ЦЕНА обязательна.** Без неё это наблюдение, а не урок, и в канон оно не пойдёт. Не знаешь цены — не пиши.
> **Не сочиняй.** Пустая секция — законный отчёт. Выдуманный урок хуже отсутствующего: он попадёт в канон, который читают ВСЕ будущие проекты.

## ПЛАН — (заполняет исполнитель)

**Проверки первого хода — сделаны до плана, потому что заход требует их первым ходом.**

| проверка | команда | исход |
|---|---|---|
| ветка | `git branch --show-current` | `rabota` — совпадает с контрактом, `--worktree` не нужен |
| ложное красное С1 | `git --no-optional-locks rev-parse --verify rabota` | `f329815…`, rc=0 — ветка есть, красное гейта игнорирую (урок вписан) |
| невлитые `zahod/*` | `git --no-optional-locks branch --no-merged \| grep -c 'zahod/'` | `0`, rc=1 (rc=1 у `grep` — это «совпадений нет», не сбой) |
| счёт незакрытого | `python3 _generator/tools/schet_nezakrytogo.py …` | **rc=2, «can't open file»** — инструмента по этому пути нет; он живёт в `materials` (§2). Урок фабрике |
| живая очередь | `git_zona.py zayavki` | открыта **1** заявка, `2026-08-17T2029-4…`, она указывает на четвёртый файл-заявку |

**Оспариваю предпосылки — три, все до работы.**

1. **§0.1 п.2 «ТЕПЕРЬ ЗАВОДИ СВОЮ РАБОЧУЮ ПАПКУ (команда — в блоке МЕСТО РАБОТЫ выше)» — команды
   в том блоке нет.** Блок «МЕСТО РАБОТЫ» говорит обратное: ветка `rabota` должна УЖЕ стоять в
   основной папке, а `checkout` запрещён. Она стоит. Читаю это как «рабочая папка уже заведена,
   заводить нечего» и работаю в основной папке на `rabota`. Новых worktree не завожу.
2. **Внутри задания субагенту (§0.1) два указания противоречат друг другу.** Его пункт 3 несёт
   🔴 ГРАНИЦУ «индекс и автологи ты уже забрал, всё ОСТАЛЬНОЕ — содержательная работа — НЕ коммить,
   доложи заходу», а подставленная строка `--kommitit` под тем же пунктом говорит «всё, что окажется
   вне git… во ВСЕХ репозиториях». Разрешаю в пользу ГРАНИЦЫ, потому что её же держит §2 захода:
   разбор владельца, коммиты по вёдрам и закрытие заявок (Шаги 2, 3, 5) прямо названы моей работой,
   не субагентской. Субагент делает контур (хвост Cowork, влитие, вывоз, гашение) и **докладывает**
   список содержательного; коммичу его я.
3. **Заявок четыре, а прочитать заход велел три.** Четвёртая — `ZAYAVKA-2026-08-17-post-vypusk-04.md`
   — названа не мной, а живой очередью, читать её велит сама очередь. Прочитал: она несёт §3
   «КОНФЛИКТ С СОСЕДНЕЙ ЗАЯВКОЙ», решать который заявка передаёт заходу, то есть мне.

**Развилка, которую решаю сам (обе заявки берут один файл).** `vypuski/2026-08-15-vypusk-04/vypusk.md`
назван и в `ZAYAVKA-NA-KOMMIT.md` (дополнение 17.08 вечер), и в `ZAYAVKA-2026-08-17-post-vypusk-04.md`.
Построчно правки не пересекаются, но файл один и разделить его нельзя. Решение — **один коммит**,
сообщение соседней сессии дополняется строкой про три правки с интервью (ровно то, что предлагает
§3 четвёртой заявки), смешение называется в отчёте. Порождённые виды (`vypusk.html`,
`.telegram.txt`, `.teaser.txt`) пересобираются `build_vypusk.py` ПЕРЕД коммитом и едут тем же
коммитом — они принадлежат источнику.

**Порядок работы.**

0. Перепись Шага 1 — read-only, **до всякой правки**, включая правки субагентов. Вывод дословно
   в `## ГИГИЕНА ВХОДА`… — нет, в `## ОТЧЁТ`: секцию гигиены заполняет субагент.
1. **ДО-верификатор** (свежий субагент, §3) — раньше контура, и это осознанная перестановка:
   контур ГАСИТ ветки (его пункт 4), а гашение и есть та лоссовая операция, ради которой
   верификатор существует. Пусти я контур первым — проверять было бы уже нечего. Верификатор
   read-only, поэтому §0.1 «контур до всего» не нарушается по существу.
2. **Субагент гит-контура** — один, задание печатает `bootstrap_zahod.py`, во влитии явно
   «вливать нечего» (проверено командой). Шесть строк его ответа вставляю дословно; нет строки
   охвата — перезапрашиваю.
3. **Шаг 2, разбор владельца.** Каждый путь вне git — в одно из трёх вёдер: названный заявкой /
   не названный, но явно чей-то / порождаемое и мусор. Свальный коммит запрещён.
   Не трогать (проверяю заново): `vypuski/.korpus/`, `kod_korpus-wiki.md` (под чужой рукой),
   рабочие папки `*-wt`.
4. **Шаг 3: коммиты → ветки → вывоз.** `main` в `matemdigest-map` не вливать — влитие туда есть
   публикация.
5. **Шаг 4: деплой** — `opublikovat`, затем `--yes`; дождаться Actions и назвать исход. ⚠ Здесь
   заявки расходятся: §2 захода велит «сначала предпросмотр», а заявка 17.08 §4 — «предпросмотр
   не нужен, решение владельца 16.08, подтверждено 17.08». Делаю предпросмотр всё равно: он
   ничего не портит, а заход — более поздний и более общий документ.
6. **Шаг 5:** в каждую закрытую заявку — хеш и `status: закрыта`; в очереди — `zayavka-zakryt`.
7. **ПОСЛЕ-верификатор** (второй свежий субагент) — сверяет опубликованную страницу с `sayt/`.
8. Критерий готовности повторным прогоном цикла Шага 1, полная гит-гигиена, коммит своей зоны
   двумя ходами (`add` + `commit --`).

**Критерий готовности не оспариваю** — он вычислим и может провалиться честно. Единственная
поправка: «страница сайта отдаёт свежую сборку» проверяется ПОСЛЕ-верификатором, а не мной, иначе
проверяющий и проверяемый — одно лицо.

**Чего не делаю.** Ни строки по существу: ни правок текста выпуска, ни кода, ни «заодно почищу».
Содержательный дефект — строкой в отчёт.

## ВОПРОСЫ — (заполняет исполнитель)
> Нашёл вещь, которая принадлежит чужому дому (термин/источник/урок/следующий заход) — не только вопрос владельцу? Оформи ПУНКТОМ ОЧЕРЕДИ, тремя строками:
> ```
> N. <текст находки>
>    ДОМ: <путь от корня репозитория | владелец>
>    ДОСТАВЛЕНО: нет
> ```
> `ДОМ: владелец` — когда дома-файла нет вовсе (сам вопрос владельцу); для урока фабрике дом почти всегда `<эта арка>/UROKI-FABRIKE.md`. Аналитик при переносе меняет `ДОСТАВЛЕНО: нет` на `ДОСТАВЛЕНО: <имя-захода>#<N>` И дописывает ЭТУ ЖЕ строку-метку в файл по адресу ДОМ — `priyomka.py` (Г7) красным ловит только случай «доставлено» без метки на месте, недоставленное просто печатает.
> 🔴 **Метку ставь ТОЛЬКО одним ходом вместе с самим переносом содержания, никогда раньше.** Гейт проверяет факт «строка-метка на месте», а не смысл «содержание перенесено верно» — метка без содержания рядом даст ложно-зелёный Г7.

1. `materials.db` (984 КБ, sqlite) отслеживается в git с самого начала и правится. Держать рабочую
   базу в истории сомнительно, но снять её сейчас — потерять историю данных. Заявка
   `ZAYAVKA-2026-08-16-mesyachnyy-dolg.md` §6 просила задать этот вопрос владельцу ТРИ круга подряд,
   и все три раза он не был задан. Задаю.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

2. Обложки к выпускам: владелец 17.08 — «пока этих обложек нет у нас… надо просто заказывать
   условно коллажи или придумывать какие-то коллажи вместе с художником и каким-нибудь генератором
   изображений… но пока у меня нет на это сил. Когда разрастёмся, тогда конечно начнём делать».
   Дом решения — `docs/DOLGI.md`; вписать его этим заходом нельзя (заход не пишет по существу, и
   файл в этот момент правили две параллельные сессии).
   ДОМ: docs/DOLGI.md
   ДОСТАВЛЕНО: нет

3. `matema-fest`: три бинарных предмета арки «квиз фракталы» остаются вне git и требуют решения —
   `zhurnal/2026-07-09_kvyz-fraktaly/novye-kartinki/` (23 МБ, 11 PNG от генератора изображений,
   выданные по `TZ-kartinki.md`), `zi7uuFeL` (18.8 МБ, права 0600 — это и есть скачанный
   `paket-kartinki-pravki.zip` с испорченным именем) и сам `paket-kartinki-pravki.zip` (0 байт).
   Три исхода: пустить в историю, отправить в `.gitignore` как порождаемое, снести. Решение
   владельца, потому что репозиторий публичный и 42 МБ вынимаются обратно только переписыванием
   истории.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

4. `materials`: ветка `zahod/chistka-hvostov` невлита, и на ней лежит ЕДИНСТВЕННАЯ копия пяти
   файлов, включая `_generator/tools/git_zona.py` и `bootstrap_zahod.py` — инструмент, которым
   работает вся фабрика. Ветка вычекаучена в живой рабочей папке. Пока она не влита, улучшения
   инструмента не видит основная папка; влить её вслепую заход не вправе.
   ДОМ: владелец
   ДОСТАВЛЕНО: нет

5. Шесть уроков фабрике этого захода — в `## УРОКИ ФАБРИКЕ` выше, с ценами. Два из них
   (несуществующий путь инструмента в §4.1 и самопротиворечие в задании субагенту) повторятся
   на СЛЕДУЮЩЕМ же кросс-репозиторном заходе, потому что живут в шаблоне `bootstrap_zahod.py`.
   ДОМ: zhurnal/2026-08-15_vypusk-04/UROKI-FABRIKE.md
   ДОСТАВЛЕНО: нет

6. `disciplina`: `register_doc.py` в репозитории нет (КТЧ-6), поэтому два `.md` арки
   `2026-08-16_kt-channel-editor` уехали в git незарегистрированными. Регистрировать за автора
   заход не стал.
   ДОМ: doma/zahody/POKRYTIE.md
   ДОСТАВЛЕНО: нет

## ГИГИЕНА ВХОДА — (заполняет СУБАГЕНТ гит-контура, не исполнитель)
> 🔴 **Каждый заход — ДВЕ независимые работы.** Первая — навести полную гигиену со всем, что
> накопилось к этому моменту. Вторая — собственно заход. Друг от друга они не зависят, но
> **первая обязательна ВСЕГДА**: без заполненной секции отчёт не принимается (гейт Г12 `priyomka.py`).
>
> 🔴 **ГАЛОЧКА — НЕ ИСТОЧНИК ИСТИНЫ.** Приёмка прогоняет те же команды заново и сравнивает
> с заявленным; расходится — красный НЕЗАВИСИМО от галочки. *Цена: исполнитель добросовестно
> написал «вливать не моя задача, это работа субагента, уже выполнена» при ВОСЬМИ невлитых ветках.*
>
> 🔴 **СНИМОК ВХОДА снимается ДО работы.** Без него «все долги закрыты» непроверяемо: неизвестно,
> какие были. Пустой снимок = красный.

**СНИМОК ВХОДА** *(команды и их ВЫВОД, а не пересказ; снять ПЕРВЫМ ходом, до всякой работы)*

Снят исполнителем ДО запуска субагента и до единой правки — циклом шага 1 по всем каталогам
первого уровня с `.git`. Три числа: вне git · невлитых · невывезенных.

```
== disciplina/      11  0  0
== matema-fest/     25  0  0
== matemdigest-map/ 34  1  0
== materials/        0  1  0
```

Каталогов первого уровня — 10, с `.git` — **4**. Остальные шесть: `london-avgust-2026`,
`nlogn-otsluzhka`, `spetsmat` (репозиториями не являются) и `disciplina-wt`, `materials-wt`,
`matemdigest-map-wt` — это РОДИТЕЛЬСКИЕ каталоги рабочих папок; `.git` у них нет ни файлом, ни
каталогом, и цикл шага 1 их не видит по построению. Сами рабочие папки лежат уровнем ниже
(`materials-wt/modeli` и т.д.), их снял ДО-верификатор своим методом.

```
open заявки, снято git_zona.py zayavki по каждому репозиторию:
  matemdigest-map  1   2026-08-17T2029-4-zhurnal-infra-git-zayavka-2026
  disciplina       2   2026-08-16-kt-channel-editor, 2026-08-16-hvost-priyomki
  materials        1   2026-08-16T0515-kod-odin-repozitorij…  (blokiruet, про matemdigest-map)
  matema-fest      0
  + 7 заявок в разделе «ждут захода-разработчика» — не гит-контура, не тронуты
```

**ЧТО СДЕЛАНО** *(с хэшами)*

🔴 **Шесть строк субагента гит-контура — вставлены ДОСЛОВНО, как требует §0.1. Строка охвата у
него на месте («выдано 16 позиций из 16 найденных»), ответ не усечён.**

1. заявок было 4, закрыто 1, застряло 0, не бралось 3: закрыта `2026-08-16T0515-kod-odin-repozitorij-users-ivanyakovlev-documents` (materials) — `--rezultat`, П2 отпал фактом: в `matemdigest-map` `git --no-optional-locks diff --cached HEAD --name-only` = 0 путей (rc=0), `ls-files | grep -cE '\.fuse_hidden|\.DS_Store|db-shm|db-wal'` = 0 (grep rc=1 — совпадений нет), сторож с неё снят; НЕ брал как содержательные (§3): `2026-08-17T2029-4-zhurnal-infra-git-zayavka-2026` (matemdigest-map, просит коммит подводки к выпуску 4 и трёх правок самого выпуска), `2026-08-16-hvost-priyomki` (disciplina, просит `doma/**/POKRYTIE.md` ×5 и `skills/kt-channel-editor/tools/cep.py`), `2026-08-16-kt-channel-editor` (disciplina, просит зоны `skills/` и `doma/` — по факту они уже в git, а сам файл заявки объявляет себя закрытым 16.08 заходом `kod_vyvoz-i-git-gigiena.md`; закрыть его — дело захода).
2. влито в основную 0 веток из 0 названных — влитий не было и это законный исход: невлитых `zahod/*` нет в `disciplina`, `matema-fest`, `matemdigest-map`; в `matemdigest-map` невлита только `main` (витрина, вливать запрещено), в `materials` — только `zahod/chistka-hvostov` (единственная копия `git_zona.py`, живая рабочая папка, решение владельца). Основные ветки, снятые командой `doctor`: `disciplina` — `main`, `matema-fest` — `main`, `matemdigest-map` — `rabota`, `materials` — `arka/mat-kostyak`.
3. хвост Cowork ЗАБРАН тремя коммитами: `matemdigest-map` 58d09e3 — `_studio/zhurnal/_INFRA-git/zayavki/2026-08-17T2029-4-zhurnal-infra-git-zayavka-2026.md`; `disciplina` 33fa39e — `_studio/zhurnal/_INFRA-git/zayavki/2026-08-16-hvost-priyomki.md`; `disciplina` 54cb23d — `_studio/zhurnal/2026-08-16_kt-channel-editor/{SESSIYA.md, UROKI-FABRIKE.md, VYGRUZKA-2026-08-16.md, HANDOFF-2026-08-17-vecher.md}`. ОСТАЛОСЬ вне git (списки командой `check`, коммитит заход): `disciplina` 6 путей — `doma/kt-channel/POKRYTIE.md`, `doma/meta/POKRYTIE.md`, `doma/meta/kachestvo/POKRYTIE.md`, `doma/priyomka/POKRYTIE.md`, `doma/zahody/POKRYTIE.md`, `skills/kt-channel-editor/tools/cep.py`; `matemdigest-map` 37 путей — `vypuski/**` (16), `posty/**` (8), `zhurnal/**` (11, включая `zhurnal/_INFRA-git/ZAYAVKA-2026-08-17-post-vypusk-04.md`), `docs/DOLGI.md`, `docs/fazy/stil.md`; `matema-fest` 35 путей — `zhurnal/2026-07-09_kvyz-fraktaly/**` (21), `zhurnal/{PRO-ETU-PAPKU.md,SVODKI.md}`, `.pagespeed/**` (6), `ARKA.md`, `CLAUDE.md`, `COWORK.md`, `RUKOVODSTVO-zahodami.md`, `.claude/CLAUDE.md`, `.gitignore` — там нет ни `KARTA.md`, ни `INCIDENTY.md`/`VERDIKTY.md`/`UROKI-FABRIKE.md`, ни файлов очереди, то есть моего в нём нет вовсе; `materials` — вне git 0 путей. Незарегистрированные чужие `.md`, названные строкой, а не зарегистрированные мной: `disciplina/_studio/zhurnal/2026-08-16_kt-channel-editor/VYGRUZKA-2026-08-16.md` и `HANDOFF-2026-08-17-vecher.md` (`register_doc.py` в `disciplina` нет, КТЧ-6).
4. погашено 4 ветки, все в `disciplina`, все с зелёной `poteri` (rc=0) и без рабочих папок: `zahod/arhitektura-slajdov` (надгробие f6b7c8d7), `zahod/fazy-3-7` (818a4c96), `zahod/format-kartochki-faza-1` (c0875d96), `zahod/verifikatory-stilya` (c24d6213) — каждая rc=0, «закрыто 1, отказано 0». НЕ погашено: `materials` `zahod/dovodka-fazy-intervyu` и `zahod/uborka-pered-pauzoj` — команда отказала сама (rc=0, «закрыто 0, отказано 1»): надгробия `mogila/...` уже заняты другими ветками (8fc9951 и f7c8cd7), `-f` не звал. `--vse-zelenye` НЕ звал ни разу: он захватил бы поимённо запрещённые ветки с живыми рабочими папками (`materials-wt/modeli`, `dovodka-solvera`, `obratnyj-progon`, `solver-v3-dyhanie`, `kadr-uzor-i-vmeshchenie`, `polya-i-uzor`, `sluzhebnye-slajdy`, `chistka-hvostov`, `disciplina-wt/etap-20-i-perenos`) — гасил поимённо. Ни одна `*-wt` папка не тронута, `worktree drop --force` не звался. На origin НЕ вывозил (сверх пяти пунктов): `disciplina` впереди на 2 коммита, `matemdigest-map` — на 1.
5. необратимое: удалены 4 локальные ветки в `disciplina` (см. п.4) · где: `~/Documents/GitHub/disciplina` · восстанавливается `GIT_ZONA_REPO=~/Documents/GitHub/disciplina python3 $MAT/_generator/tools/git_zona.py voskresit --branch <имя>` по надгробию `mogila/<имя>` (надгробия живут только на этом диске, push их не вывозит). Содержимого не пропало: `poteri` по каждой дала «история целиком есть в других локальных ссылках».
6. выдано 16 позиций из 16 найденных

**Дальше — работа исполнителя, а не субагента. Коммиты по вёдрам §2 Шага 2, поимённо:**

| # | коммит | репо | ведро |
|---|---|---|---|
| 1 | `94c5bac` | matemdigest-map | выпуск 4 на витрину: `sayt/static/4/index.html` + пять строк `vypuski.yaml` — решение владельца в сессии |
| 2 | `1125fa3` | matemdigest-map | названо заявкой `ZAYAVKA-NA-KOMMIT` (доп. 17.08 вечер) + три правки с интервью по `post-vypusk-04` |
| 3 | `0534836` | matemdigest-map | названо заявкой `ZAYAVKA-2026-08-17-post-vypusk-04`: зона поста |
| 4 | `8d5e480` | matemdigest-map | не названо, явно чьё-то: утренний пост про интервью Вязовской |
| 5 | `2cf7d93` | matemdigest-map | не названо, явно чьё-то: срезы корпуса и стиля + `check_stil.py` |
| 6 | `b6ec726` | matemdigest-map | не названо, явно чьё-то: журнал арки «выпуск 04» |
| 7 | `f3fc082` | matemdigest-map | появилось ПО ХОДУ прогона: `docs/podvodka-v-kanal.md`, автор не установлен |
| 8 | `7a91bf5` | disciplina | названо заявкой `2026-08-16-hvost-priyomki` |
| 9 | `ef89aed` | disciplina | названо заявкой `2026-08-17T2050-…`, заведённой ВО ВРЕМЯ прогона |
| 10 | `a3be9de` | disciplina | хвост очереди: три заявки перенесены в `sdelano/` |
| 11 | `972bc5e` | matema-fest | не названо, явно чьё-то: рамка репозитория и руководство заходами |
| 12 | `8809268` | matema-fest | не названо, явно чьё-то: журнал арки «квиз фракталы» + замеры PageSpeed |

**Заявки закрыты — все шесть, что были открыты в очередях (4 на входе + 2 заведённые по ходу):**

| заявка | репо | чем |
|---|---|---|
| `2026-08-16T0515-kod-odin-repozitorij…` | materials | субагентом, П2 отпал фактом |
| `2026-08-17T2029-4-zhurnal-infra-git-zayavka-2026` | matemdigest-map | `0534836` + `1125fa3` |
| `2026-08-16-hvost-priyomki` | disciplina | `7a91bf5` |
| `2026-08-16-kt-channel-editor` | disciplina | зоны уже были в git, проверено фактом (39 + 4 + 10 файлов) |
| `2026-08-17T2050-kt-channel-editor-matemdigest-17-08` | disciplina | `ef89aed` — заявка ЗАВЕДЕНА в 20:50, во время этого прогона |
| — | — | плюс четыре файла-заявки в `zhurnal/_INFRA-git` и арке размечены `status: закрыта` с хешами |

**Вывоз:** `disciplina` 5 коммитов, `matema-fest` 2, `matemdigest-map` 6+ — все `push прошёл`,
`log @{u}..` → 0 по каждому.

**Деплой:** `opublikovat --yes` → витрина `main` `6953d885`, «✅ Прогон зелёный — сайт опубликован»,
`https://github.com/d1-d57/digest/actions/runs/32052098538`.

**ВСЕ ДОЛГИ ВХОДА ЗАКРЫТЫ:** `нет` — и вот полный список того, что осталось, с причиной по каждому.

1. **`materials`, ветка `zahod/chistka-hvostov` — невлита.** Непроходимо не по сложности, а по
   мандату: ДО-верификатор показал, что на ней живут 5 файлов, которых больше нигде нет, и среди
   них сам `_generator/tools/git_zona.py` и `bootstrap_zahod.py` — инструмент, которым сделан весь
   этот заход. Она вычекаучена в живой рабочей папке `materials-wt/chistka-hvostov`. Аналитик
   просудил «вливать нечего», субагенту прямо запрещено пересматривать список влития. Влить её
   вслепую — рискнуть инструментом; решение владельца.
2. **`matemdigest-map`, ветка `main` — невлита, и вливать её НЕЛЬЗЯ.** Это витрина: влитие в неё
   есть публикация, и делается оно `opublikovat`, а не `merge`. Число «невлитых 1» здесь —
   нормальное состояние, а не долг.
3. **`matema-fest`, три бинарных предмета — не взяты.** `novye-kartinki/` (23 МБ, 11 PNG),
   `zi7uuFeL` (18.8 МБ, безымянный архив с правами 0600 — след скачивания `paket-kartinki-pravki.zip`),
   `paket-kartinki-pravki.zip` (0 байт, пустой). Ни один не назван заявкой, очередь `matema-fest`
   пуста, решения владельца нет. §2 Шага 2 предписывает при сомнении НЕ коммитить и назвать
   строкой — сделано. 42 МБ в публичную историю вытаскиваются обратно только переписыванием
   истории, поэтому цена ошибки несимметрична.
4. **`materials`, две ветки не погашены** — `zahod/dovodka-fazy-intervyu` и
   `zahod/uborka-pered-pauzoj`: команда отказала сама, надгробия `mogila/…` заняты. `-f` не звался.
5. **`disciplina`, два `.md` не зарегистрированы** — `register_doc.py` в этом репозитории нет
   (КТЧ-6, известный долг). За автора не регистрировал.

## ОТЧЁТ — (заполняет исполнитель)

**АРТЕФАКТ:** `/Users/ivanyakovlev/Documents/GitHub/matemdigest-map/sayt/static/4/index.html` — браузером; он же живёт по адресу **https://d1-d57.github.io/digest/4/**

*(Это единственный файл, который заход собрал, а не переложил. Открывать стоит именно живой адрес:
локальный файл и опубликованная страница сверены ПОСЛЕ-верификатором и совпадают байт в байт.)*

**КОММИТ:** зона едет ДВУМЯ коммитами, и это не оплошность, а следствие критерия готовности:
финальные три числа печатает команда, которую можно прогнать только ПОСЛЕ коммита зоны. Первый
коммит несёт отчёт, второй — честные числа. Хэши обоих — в строке «КОММИТ ЗОНЫ» ниже.
`git_zona.py check --zone …` → ✅ по всем трём путям зоны (вывод — в разделе «КАК ПРОВЕРИЛ»).

---

### ЧТО СДЕЛАЛ И ЗАЧЕМ

Заход про СОСТОЯНИЕ репозиториев, а не про их содержание. Работа развалилась на три независимые
части, и разделять их было важнее, чем делать быстро.

**1. Гигиена входа (субагент гит-контура).** Забрал хвост Cowork тремя коммитами, закрыл
кросс-репозиторную заявку, погасил четыре мёртвые ветки в `disciplina`. Зачем отдельным
субагентом: у него ДРУГИЕ права — главная папка, основная ветка, вывоз, — и смешивать их с правами
исполнителя нельзя, иначе получается второй исполнитель без зоны.

**2. Разбор владельца и коммиты (мои).** 34 пути на входе в `matemdigest-map`, 11 в `disciplina`,
25 в `matema-fest` разложены по трём вёдрам §2 Шага 2 и уехали двенадцатью коммитами. Свального
коммита «разное» нет ни одного: каждое ведро получило своё сообщение, а два ведра — сообщения
дословно из заявок. Зачем так дробно: заявки прямо помечают чужие пути как чужие, и коммит,
подметающий их скопом, стирает границу между сессиями ровно там, где она нужна для разбора.

**3. Публикация.** Выпуск 4 выложен на сайт по прямому решению владельца, полученному в ходе
прогона. Заявки этого не просили — они просили задеплоить то, что есть.

**Развилка, которую решил сам и о которой стоит знать.** Обе заявки про выпуск 4 просят один и тот
же `vypusk.md` под своё сообщение. Построчно правки не пересекаются (одна сессия правила прозу
блоков, другая — две строки `- areas:` и одну `- link:`), но файл один. Решение — один коммит
`1125fa3`, сообщение соседней сессии дополнено строкой про интервью, смешение названо и в коммите,
и в обеих заявках. Ровно это предлагает §3 самой заявки; своего изобретения здесь нет.

**Развилка, которую НЕ решил сам и вынес владельцу.** Публикация выпуска 4 потребовала завести
`sayt/static/4/index.html`, которого не существовало. Это выходило за «ничего не пишу по существу»,
поэтому заход остановился и спросил — вместо того чтобы либо тихо сделать, либо тихо не сделать и
отрапортовать «задеплоено».

### КАК ПРОВЕРИЛ

Всё числами и кодами возврата, `echo $?` первым.

- **Ветка** — `git branch --show-current` → `rabota`. `rev-parse --verify rabota` → `f329815`, rc=0.
  Ложное красное С1, предсказанное аналитиком, подтверждено ложным.
- **Порождённые виды** пересобраны ДО коммита: `build_vypusk.py` → «✓ линтер ок: 7 материалов в
  7 рубриках», три вида переписаны.
- **Гейты `disciplina`, которых требовала заявка**, прогнаны ДО коммита: `cep.py` → «разрывов нет»,
  инцидентов 22, рычагов 20; `ocenka_skilla.py` → «с красным 0 из 1, проверено осей 17 из 17».
- **Каждый коммит** — `git show --stat` сразу после: чужих путей нет ни в одном.
- **Публикация** — предпросмотр ДО `--yes` (вопреки заявке, и он окупился, см. уроки), затем
  `--yes` → витрина `6953d885`, «✅ Прогон зелёный — сайт опубликован».
- **Два верификатора, оба свежими субагентами, оба с ДРУГИМ методом, чем у меня.**

**ДО-верификатор** (до любой правки, метод — `poteri` по каждой ветке, а не `status`):
> ВЕРДИКТ: при закрытии названных веток теряется файлов 5, поимённо — все с ветки
> `zahod/chistka-hvostov` в `materials`: `_generator/tools/bootstrap_zahod.py`,
> `_generator/tools/fixtures/faza-priyomki/PROGNAT.sh`, `_generator/tools/fixtures/git_zona/PROGNAT.sh`,
> `_generator/tools/git_zona.py`, `_studio/docs/kak-delat/DOLG-ISKLYUCHENIA.md`.
> Остальные 18 веток `zahod/*` — 0 файлов. **выдано 46 позиций из 46 найденных**

Этот вердикт изменил работу, а не украсил отчёт: он поимённо запретил девять рабочих папок с чужим
незакоммиченным (`materials-wt/modeli` — 843 пути!) и снял с повестки `zakryt-vetku --vse-zelenye`,
который эти папки захватил бы.

**ПОСЛЕ-верификатор** (после деплоя, метод — открыть страницы и сверить с диском, в git не смотреть):
> ВЕРДИКТ: страниц сверено 6 из 6, расхождений 0. `/digest/4/` — HTTP 200, diff со скачанным против
> `sayt/static/4/index.html` ПУСТ, байт в байт, 42 001 байт / 213 строк, 7 разделов `<h2>` онлайн и
> на диске. `/1/`, `/2/`, `/3/` — 200, публикация прежние выпуски не сломала. `/vyazovskaya/` — 200,
> 18 601 знак текста. Выпуск 4 на корневой виден и стоит ПЕРВЫМ.
> **выдано 6 позиций из 6 найденных**

### ЧТО НЕ ТРОГАЛ

- **`vypuski/.korpus/`** — 142 МБ сырья (за час прогона выросло со 132), стоит в `.gitignore`.
- **`zhurnal/2026-08-15_vypusk-04/kod_korpus-wiki.md`** — под чужой рукой. Прогон Википедии был ЖИВ
  в момент коммита: `wiki_teksty.json` менялся за минуту до (`Aug 17 20:36` при коммите в 20:37).
  Закоммичен КАК ЕСТЬ, не правлен, завершения не ждал.
- **Рабочие папки `*-wt`** — ни одна не тронута, `worktree drop --force` не звался ни разу.
- **`main` в `matemdigest-map`** — не вливалась: влитие в витрину есть публикация.
- **Содержание чего бы то ни было.** Ни строки текста выпуска, ни правки кода. Единственный файл,
  который заход СОЗДАЛ, — `sayt/static/4/index.html`, и он побайтовая копия собранного
  `vypusk.html`; единственные строки, которые заход НАПИСАЛ в чужом файле, — пять строк данных в
  `vypuski.yaml`, и все значения в них взяты из шапки самого выпуска (`subtitle`, `teaser`, `date`).

### НЕОБРАТИМОЕ — отдельным списком

1. **Удалены 4 локальные ветки** `zahod/arhitektura-slajdov`, `zahod/fazy-3-7`,
   `zahod/format-kartochki-faza-1`, `zahod/verifikatory-stilya` · где: `~/Documents/GitHub/disciplina`
   · восстанавливается: `git_zona.py voskresit --branch <имя>` по надгробиям `mogila/<имя>`
   (`f6b7c8d7`, `818a4c96`, `c0875d96`, `c24d6213`). Содержимого не пропало — `poteri` по каждой
   дала 0 файлов до удаления.
2. **Опубликован сайт** — публикация публична и в этом смысле необратима · где: витрина `main`
   репозитория `d1-d57/digest`, `https://d1-d57.github.io/digest/` · откатывается переносом
   предыдущего состояния витрины: до публикации `main` был на `5f3f754`, сейчас `6953d885`.
3. **Три заявки перенесены** из `zayavki/` в `zayavki/sdelano/` в `disciplina` и одна в
   `matemdigest-map` · восстанавливается коммитами `a3be9de` и коммитом зоны этого захода.
4. **Ветки в `materials` НЕ удалялись**, чужие рабочие папки НЕ трогались, `--force` не звался
   нигде.

### ПОВТОРЯЕМОСТЬ НАХОДОК

**Повторятся на следующем же заходе — значит это НЕМЕДЛЕННОЕ, заход, а не запись в очередь:**

1. **Несуществующий путь инструмента в §0, §4 и всём §4.1.** Живёт в шаблоне
   `bootstrap_zahod.py`, поэтому повторится в КАЖДОМ заходе, собранном вне `materials`. Стоит
   упавшей первой команды и молчаливого переписывания гейтов исполнителем.
2. **Самопротиворечие в задании субагенту** (граница пункта 3 против подставленного `--kommitit`).
   Тот же шаблон, то же «каждый раз».
3. **Слепота `check_sborki.py` на кросс-репозиторной ветке.** Каждый такой заход, одно ложное
   красное.

**Не повторятся — законно уходят пунктами очереди в `## ВОПРОСЫ`:** вопрос про `materials.db`,
долг про обложки, три бинарных предмета `matema-fest`, невлитая `zahod/chistka-hvostov`.

### ОТКРЫТОЕ «ВОЗВРАЩАТЬСЯ»

Пять долгов входа не закрыты, каждый назван поимённо с причиной в `## ГИГИЕНА ВХОДА`. Ни один из
них не «сложный» и не «не моя тема»: два требуют решения владельца (ветка с единственной копией
инструмента; 42 МБ бинарного в публичном репозитории), один — нормальное состояние (`main` как
витрина), один — отказ самой команды (занятые надгробия), один — отсутствующий инструмент
(`register_doc.py` в `disciplina`).

**ВРЕМЯ ПРОГОНА И ТОКЕНЫ:** неприменимо на канале `app` — лог прогона существует только на канале
`terminal`, где его кладёт `tee`; сессия в приложении Claude Code такого следа не оставляет.

---

### КРИТЕРИЙ ГОТОВНОСТИ — ФИНАЛЬНЫЕ ЧИСЛА, НАПЕЧАТАННЫЕ КОМАНДОЙ

Цикл шага 1 прогнан ПОВТОРНО, после всей работы. Три числа: вне git · невлитых · невывезенных.

```
== disciplina/       0  0  0
== matema-fest/      3  0  0
== matemdigest-map/  0  1  0
== materials/        0  1  0
```

**Было на входе:** `11 0 0` · `25 0 0` · `34 1 0` · `0 1 0`. Из 70 путей вне git закрыто 67.

**Каждое ненулевое — поимённо, с причиной:**

| число | что это | почему непроходимо моими правами |
|---|---|---|
| `matema-fest` вне git **3** | `novye-kartinki/` (23 МБ), `zi7uuFeL` (18.8 МБ), `paket-kartinki-pravki.zip` (0 байт) | Решение владельца. Заявкой не названы, очередь `matema-fest` пуста. §2 Шага 2 велит при сомнении НЕ коммитить и назвать строкой. 42 МБ в публичную историю вынимаются обратно только переписыванием истории — цена ошибки несимметрична. Пункт очереди №3 |
| `matemdigest-map` невлитых **1** | ветка `main` | Это ВИТРИНА. Влитие в неё есть публикация и делается `opublikovat`, а не `merge` — прямой запрет и §2 захода, и заявки. Единица здесь — норма, а не долг |
| `materials` невлитых **1** | ветка `zahod/chistka-hvostov` | Единственная копия пяти файлов, включая сам `git_zona.py`. Вычекаучена в живой рабочей папке. Аналитик просудил «вливать нечего», пересматривать не мандат захода. Пункт очереди №4 |

**Остальные пункты критерия:**

- `status --porcelain` в `matemdigest-map` — **пуст**. ✅
- Заявки: открытых **0** во всех четырёх репозиториях (`disciplina`, `matema-fest`,
  `matemdigest-map`, `materials`). Закрыто **восемь**: четыре, что были на входе, и четыре,
  заведённые параллельными сессиями ПО ХОДУ прогона. Ни одна не осталась `status: открыта`. ✅
  *(В `materials` остаются 7 заявок в разделе «ждут захода-разработчика» и 1 постоянное
  исключение — они адресованы не гит-контуру и в счёт открытых не входят, что команда и печатает.)*
- Страница сайта отдаёт свежую сборку — **проверено ПОСЛЕ-верификатором, не мной**: 6 из 6
  страниц, расхождений 0. ✅

🔴 **ОХВАТ, БЕЗ КОТОРОГО ВЕРДИКТ НЕ ПРИНИМАЕТСЯ: проверено 4 репозитория из 4.** Четыре — это
число каталогов первого уровня под `~/Documents/GitHub`, у которых есть `.git`, посчитанное циклом
шага 1 ДО работы. Остальные шесть каталогов репозиториями не являются (три обычные папки и три
родительских каталога рабочих папок).

### ⚠ РЕПОЗИТОРИИ ЖИВЫЕ — И ЭТО НЕ ОГОВОРКА, А ЗАМЕР

Пока шёл заход, параллельные сессии дописали работу **четыре раза**, и каждый раз после того, как
я снимал «чисто»:

| когда | что появилось | чем закрыто |
|---|---|---|
| после переписи входа | `docs/podvodka-v-kanal.md` | `f3fc082` |
| после коммитов `disciplina` | заявка `2026-08-17T2050` + три пути её зоны | `ef89aed` |
| после первого «чисто» | `skills/kt-channel-editor/DOLG.md` (Д7) + дополнение к той же заявке | `ba44548` |
| после второго «чисто» | пять путей закрытия сессии 5 + заявка `2026-08-17T2106` | `30456ae`, `e4ca9a7` |

Все четыре волны подобраны, но **это состояние на момент отчёта, а не навсегда**: следующая волна
может прийти через минуту. «Вне git 0» здесь означает «ноль в момент, когда команда напечатала
ноль», и проверять его надо командой, а не этим отчётом. Владелец в ходе прогона сказал прямо, что
арка закрывается и правки ещё приедут.

**КОММИТ ЗОНЫ:** `f040013` — «git-podmesti-vse: подметены четыре репозитория, закрыты шесть заявок,
выпуск 4 опубликован» (отчёт), плюс второй коммит с этими финальными числами.
`git_zona.py check --zone …` → ✅ по всем трём путям зоны:
«зона `zhurnal/_INFRA-git`: работа доехала в git, вне git ничего нет», то же по
`ZAYAVKA-NA-KOMMIT.md` и по `kod_git-podmesti-vse.md`, rc=0 на каждой.
Форма вызова — через `$MAT`, а не локальная: локального `_generator/tools/` в этом репозитории нет
(урок фабрике выше).

### 4.1 ГИГИЕНА — ПО ПУНКТАМ

- **Г1.** ✅ три зелёных `check --zone`, вывод выше.
- **Г2.** ⚠ **применим, вопреки заготовке «неприменимо».** Заготовка утверждает, что все пути зоны
  лежат в `matemdigest-map`; по факту заход писал в ТРИ репозитория — `disciplina` (5 коммитов) и
  `matema-fest` (2 коммита) сверх своего. Команда, названная в самом пункте на такой случай, и
  прогнана: `cd ../disciplina && git status --porcelain` → **пусто**;
  `cd ../matema-fest && git status --porcelain` → **три пути**, названные в таблице выше.
- **Г3.** ✅ `branch --no-merged rabota` → `main`, одна. На входе было столько же — не прибавилось.
- **Г4.** Ни одного нового `.py` заход не заводил — так и пишу. Неприменимо.
- **Г5.** Ни одного нового `.md` вне своей зоны заход не заводил; файл-заход рождается
  зарегистрированным из `bootstrap_zahod.py`, регистрировать его не нужно. `register_doc.py` не
  звался, `_studio/docs/KARTA.md` в коммит не добавлялся — и это правильно, а не пропуск.
- **Г6.** ✅ `show --stat` снят после КАЖДОГО из четырнадцати коммитов; чужих путей нет ни в одном.
  Все коммиты сделаны формой `add` + `commit -- <пути>`, ни одного `-A`, `.` или `commit -am`.

## ФАЗА ПРИЁМКИ — (заполняет АНАЛИТИК, не исполнитель)
> 🔴 **Без этого раздела заход НЕ ЗАКРЫТ.** Гейт — `python3 _generator/tools/priyomka.py <этот файл>` (Г13): пока раздел пуст или несёт плейсхолдеры, приёмка красная, и это единственное место, где вердикт остаётся ЗАПИСАННЫМ, а не сказанным в чат.
> Заполняется ПОСЛЕ отчёта исполнителя. Исполнителю сюда писать нечего — его половина выше.

**ВЕРДИКТ:** `принято` — числовые утверждения отчёта перегнаны приёмкой по живым артефактам, а не
прочитаны: артефакт `sayt/static/4/index.html` существует, 42 001 байт, `cmp` с `vypusk.html` даёт
побайтовое совпадение; коммит `1125fa3` существует и несёт заявленные пути; коммит `a3be9de`
существует в `disciplina`; страница `https://d1-d57.github.io/digest/4/` отдаёт 200 и содержит ВСЕ
девять правок владельца от 17.08 («Кеплер подошёл к вопросу», «Результат независимо получили», «Его
выписали», «Её удалось взять», «накладывается ещё одно ограничение», «довела модель Gauss», «Здесь
удалось то, что не удалось», «Глава требует функционального анализа», исчезнувшее «Дальше по
годам») — то есть деплой не просто прошёл, а вывез правильную редакцию.

**ОХВАТ ПРОВЕРКИ, у положительного вердикта тоже:** перегнано 5 числовых утверждений из 5 названных
в отчёте · репозиториев сверено 4 из 4 · страниц сайта приёмка открывала 1 из 6 (остальные пять
сверял ПОСЛЕ-верификатор захода, заново я их не открывал).

⚠ **Расхождение финальных чисел — моё, не исполнителя.** Отчёт сдал `disciplina 0 0 0`; на момент
приёмки там 3 пути вне git. Это три `POKRYTIE.md`, которые правила закрывающая сессия арки ПОСЛЕ
прогона захода. Та же причина у `matemdigest-map`: 4 вместо 0 — новая арка и заявка. Гейт Г0 по
`ZAYAVKA-NA-KOMMIT.md` красный по той же причине: файл правил я, уже после захода.

**ЧТО ЗАСЧИТАНО ОТДЕЛЬНО — дисциплинированное НЕ-действие.** Заход остановился и спросил вместо
того, чтобы завести `sayt/static/4/index.html` молча; не тронул `kod_korpus-wiki.md` под живой
рукой; не звал `worktree drop --force`; не влил `main`; не закоммитил 42 МБ бинарного в публичный
репозиторий, а вынес решение владельцу. ДО-верификатор изменил работу, а не украсил отчёт: снял с
повестки `zakryt-vetku --vse-zelenye`, который захватил бы девять чужих рабочих папок.

**ВОПРОСЫ ИСПОЛНИТЕЛЯ ДОСТАВЛЕНЫ ВЛАДЕЛЬЦУ** — все четыре с `ДОМ: владелец` заданы в чате 17.08, а
не оставлены лежать здесь: владелец файлы-заходы не читает по устройству.

**ХЕНД-ОФФ-ДОЛГ ЗАКРЫТ ТЕМ ЖЕ ХОДОМ:** урок 12 арки получил дом — заведён
`disciplina/doma/vypusk/POKRYTIE.md`, туда же вписаны ВЫП-002 и ВЫП-003 из этой сессии.

**ВЕТКА РАБОТЫ:** `rabota`
*(проверяется фактом, не словом: ветка обязана существовать и быть либо ВЛИТА в основную, либо названа в открытой заявке на влитие. Ни того, ни другого — Г14 краснеет. Снять состояние: `python3 _generator/tools/git_zona.py poteri --branch <ветка>`)*

**ЗАЯВКИ, ПОСТАВЛЕННЫЕ ЭТОЙ ПРИЁМКОЙ — ПРОДУБЛИРУЙ СЮДА ТО, ЧТО УЖЕ ЛЕЖИТ В СПИСКЕ:**
> Адрес списка: `/Users/ivanyakovlev/Documents/GitHub/matemdigest-map/_studio/zhurnal/_INFRA-git/zayavki`
> Читается командой (из любой папки, в том числе из worktree): `python3 _generator/tools/git_zona.py zayavki`
> Ставится командой: `python3 _generator/tools/git_zona.py zayavka --rod <git-operaciya|pravka-koda> "<текст>"`
> 🔴 Вопрос здесь НЕ «что ты хочешь сделать», а «что ты УЖЕ положил в очередь». Дубль сверяется с очередью по id машинно; намерение сверить не с чем.

- заявок нет: эта приёмка ни одной из пяти операций (влитие · коммит · вывоз · деплой · гашение) не
  заказывала. Всё, что осталось вне git после захода, накопила Cowork уже ПОСЛЕ его прогона, и это
  не заявка, а дозадание тому же исполнителю через тот же файл — раздел `## ДОЗАДАНИЕ` ниже.

*(Заявок эта приёмка не ставила — так и напиши строкой «заявок нет: <почему ни одна из пяти операций не понадобилась>». Пустая строка и прочерк не принимаются: молчание неотличимо от «забыл».)*

---

## ДОЗАДАНИЕ — 2026-08-17, тот же канал, тот же исполнитель

> **Отчёт принят** (вердикт выше). Это НЕ доработка: замечаний к сделанному нет. Это второй заход в
> том же файле, потому что Cowork накопила работу ПОСЛЕ твоего прогона и её надо забрать тем же
> контуром. Пиши в `## ОТЧЁТ ДОЗАДАНИЯ` внизу, прежний отчёт не трогай.

🔴 **ГРАНИЦА ТА ЖЕ: ты не пишешь по существу.** Ни строки текста, ни правки кода. Предмет — состояние
репозиториев.

### Часть 1 · ЗАБРАТЬ НАКОПЛЕННОЕ (это и есть задача)

После твоего прогона Cowork сделала четыре вещи, и все они вне git. Пути названы поимённо; сверь их
живым `status --porcelain`, потому что между написанием этого текста и твоим прогоном могло
прибавиться.

**`matemdigest-map`, ветка `rabota`:**

```
zhurnal/2026-08-17_instrument-stilya/            новая арка целиком (5 файлов): NAVIGATOR, PLAN,
                                                  SESSIYA, TZ, UROKI-FABRIKE
zhurnal/2026-08-15_vypusk-04/PLAN.md             баннер «🔒 АРКА ЗАКРЫТА» — арка выпуска 04 закрыта
zhurnal/2026-08-15_vypusk-04/UROKI-FABRIKE.md    9 вердиктов проставлены; маркер ЦЕНА нормализован
zhurnal/2026-08-15_vypusk-04/ZAYAVKA-NA-KOMMIT.md  дополнение 2
zhurnal/2026-08-15_vypusk-04/kod_git-podmesti-vse.md  фаза приёмки + это дозадание
```
Сообщение: `арка: выпуск 04 закрыт, заведён преемник instrument-stilya; приёмка гит-захода`

**`disciplina`, три файла — их коммитит ЭТОТ репозиторий, не `matemdigest-map`:**

```
doma/vypusk/POKRYTIE.md      🔴 НОВЫЙ ДОМ, восьмой. Заведён по требованию владельца
doma/meta/POKRYTIE.md        §12 — два правила из уроков закрытой арки
doma/zahody/POKRYTIE.md      §3 — четыре правила оттуда же
doma/priyomka/POKRYTIE.md    §3 — два правила оттуда же
```
Сообщение: `дома: заведён дом vypusk; правила из уроков арки выпуска 04 вписаны в четыре дома`

⚠ **Почему это важнее, чем выглядит.** В `UROKI-FABRIKE.md` арки стоят вердикты вида
`вписан → doma/<дом>/POKRYTIE.md`. Пока эти четыре файла вне git, вердикты указывают в никуда, а
арка формально закрыта. Некоммит здесь равен потере девяти разобранных уроков.

⚠ **`disciplina` не имеет `register_doc.py`** — ты сам это назвал (КТЧ-6). `doma/vypusk/POKRYTIE.md`
новый; если хук покраснеет на его регистрации, это ЧУЖОЙ долг, обходи с причиной по форме §4, а не
чини.

### Часть 2 · ОПИСЬ КОРНЯ — read-only, НИЧЕГО НЕ ДВИГАТЬ

Владелец 17.08: *«структура папки дайджеста должна быть устроена разумно»*. В корне
`matemdigest-map` **64 файла**: 23 `.md` (из них 16 — `ZAHOD-*.md` эпохи до журнала), 17 `.py`,
13 `.json` (12 — `*_scores.json`), 6 `.html`, база `materials.db`, `.zip`, `.csv`, два `.yaml`.

🔴 **ПЕРЕМЕЩЕНИЕ ФАЙЛОВ — ОТДЕЛЬНЫЙ ЗАХОД, И ЭТО НЕ ОСТОРОЖНОСТЬ, А ДИСЦИПЛИНА.** Скрипты импортируют
друг друга (`config.py`, `db.py`), сборка сайта читает пути, `.gitignore` написан под нынешнюю
раскладку. Двигать вслепую — сломать конвейер молча. Поэтому здесь только ОПИСЬ, по которой
следующий заход будет двигать.

Дай таблицу: **файл → к какой линии относится → кто его зовёт**. Последний столбец — не мнение, а
грепом:

```
grep -rn "<имя без .py>" --include=*.py --include=*.yaml --include=*.html . | grep -v "^./<сам файл>"
```

Линий, судя по именам, три; проверь это, а не поверь:
1. **карта года** — ранняя линия сбора и оценки важности: `fetch_*.py`, `score.py`, `dozor_a.py`,
   `*_scores.json`, `materials.{db,csv,json}`, `sources*.yaml`, `karta-goda.html`, 16 `ZAHOD-*.md`;
2. **выпуски** — живой конвейер: `vypuski/`, `docs/`, `zhurnal/`, `sayt/`;
3. **интервью Вязовской** — разовая работа: `build_intervyu.py`, `Vyazovskaya-*`,
   `настройка-перевода.md`.

Назови отдельно **файлы-сироты**: те, кого не зовёт никто и кто сам никого не зовёт. И отдельно —
**чем рискует переезд**: какие пути зашиты в код строкой, а не вычисляются.

### КРИТЕРИЙ ГОТОВНОСТИ ДОЗАДАНИЯ (может провалиться)

1. `git status --porcelain` в `matemdigest-map` и в `disciplina` — **пуст**, либо каждое ненулевое
   названо поимённо с причиной. Проверяется командой, а не словом.
2. `grep -c '^ВЕРДИКТ: вписан' matemdigest-map/zhurnal/2026-08-15_vypusk-04/UROKI-FABRIKE.md` — и по
   КАЖДОМУ вписанному вердикту дом существует в git: `git -C disciplina ls-files doma/<дом>/POKRYTIE.md`
   не пуст. Вердикт, указывающий в некоммиченный файл, — красное.
3. Опись корня: **64 файла из 64** разнесены по линиям, ни один не «прочее». Число 64 пересчитай
   сам (`ls -p | grep -v / | wc -l`) — оно снято 17.08 и могло измениться.
4. Файлов ты **не двигал**: `git status` не содержит ни одного переименования (`R`).

**Отрицательный вердикт несёт охват:** не «всё закоммичено», а «путей забрано X из Y», где Y —
число из `status --porcelain` ДО работы.
